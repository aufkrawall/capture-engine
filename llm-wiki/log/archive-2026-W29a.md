# llm-wiki Log — Archive 2026-W29a

### 2026-07-19 - Make injected video imply DLL injection

- **Simpler profile contract:** `video_capture=inject` now implies the full injected video/overlay/graphics route without requiring the redundant `dll_injection=when_needed` line. WGC, DXGI Duplication, and no-video profiles remain non-injected by default. `dll_injection=always` is needed only to add the injected overlay/graphics overrides to those sources; `never` remains an optional safety lock, and `when_needed` remains parser-compatible as an explicit spelling of the new default.
- **Safety and edge cases:** global `auto` plus `video_capture=inherit` normally resolves to injected capture; `never` makes auto choose WGC and blocks any explicitly resolved inject route. Title-only WGC/DXGI profiles no longer produce a spurious process/injection warning under the default policy. Profiles that omit `video_capture` still create no video route, including overlay-only `always` profiles.
- **Config/examples:** the fresh reference explains source-driven injection before its optional override. Its three examples use distinct executables: HotS DXGI with an explicitly labeled optional `never` lock, injected capture with no `dll_injection` line, and DXGI capture plus `always`. The ignored installed config was migrated equivalently: redundant `when_needed` lines and the Brave no-video `never` line were removed, while the HotS/Fortnite safety locks remain.
- **Coverage / verification:** focused tests prove injected video implies the full injector route, WGC/DXGI default to non-injected routing, `always` provides overlay-only injection, `never` blocks injected video, old `when_needed` remains compatible, and generated examples contain no redundant line. Incremental product build `0.1.5102` passed x64/x86 hooks, CaptureEngine/MediaEngine, both Vulkan layers, packaging, import closure, PE mitigations, effective CFG, architecture, and PDB verification. Its exact-build gate passed all 1,747 native tests in 135 suites and all six Python tool self-tests.
- **Source anchors:** `captureengine/config.ini.template`, `common/config.{h,cpp}`, `tests/{test_config,test_config_override}.cpp`, `tools/run_av_sync_matrix.py`, and `llm-wiki/{configuration,current,dx12-injection-bootstrap,log/recent}.md`.

### 2026-07-19 - Make the sequence-lock pause intrinsic self-contained for MinGW GCC

- **Follow-up CI evidence:** Ubuntu MinGW GCC now completes common x64, all 65 x64 hook objects and the hook link/verification, MediaEngine compilation/linking, common x86, and the expected x86-hook skip. Unit-test compilation then failed only in `common/sequence_lock.h`: `SequenceLock_ReadBegin()` called `_mm_pause()` without declaring it. Native Clang had accepted the header through unrelated transitive includes, while the GCC test translation units correctly exposed the missing direct dependency.
- **Fix/coverage:** `sequence_lock.h` now directly includes the established Windows/MinGW `<intrin.h>` contract used by the other `_mm_pause()` call sites. The adjacent stress test still invokes `SequenceLock_ReadRetry()` but no longer creates the unused local GCC reported. A build-policy regression locks the header's self-contained intrinsic declaration, and the repository scan confirms every `_mm_pause()` source now has a direct intrinsic include.
- **Verification:** all 50 build/PE policy tests passed. The focused incremental gate recompiled the two affected test translation units and passed all five `SequenceLockTest.*` / `SequenceLockStressTest.*` cases. Final incremental product build `0.1.5101` passed x64/x86 hooks, CaptureEngine/MediaEngine, both Vulkan layers, test apps, packaging, import closure, PE mitigations, effective native CFG, architecture, and PDB verification. Its exact-build no-build gate passed the complete native unit suite and all six Python tool self-tests. A fresh GitHub Linux runner remains the end-to-end confirmation boundary beyond the supplied failure point.
- **Source anchors:** `common/sequence_lock.h`, `tests/test_sequence_lock_stress.cpp`, `test_build_flags.py`, and `llm-wiki/build.py.md`.

### 2026-07-19 - Preserve DX12 behavior when Linux MinGW headers lag the Windows SDK

- **Follow-up CI evidence:** the restored compiler-specific hardening policy reached and completed all 15 common x64 objects, proving the original GCC `-mguard=cf` failure was fixed. The next Ubuntu runner exposed three independent older-header assumptions in the hook: its D3D12 header lacked `D3D12_ENCODE_BASIC_FILTER`, lacked DRED v1 settings/data declarations, and supplied a non-`constexpr` enum `operator|` for format-support masks. The pre-build Python diagnostic test also returned before its mocked process invocation because the Linux branch correctly required Wine, and the multiline PowerShell step then continued into the build despite that test failure.
- **Header-capability fix:** DX12 sampler policy now encodes the documented filter bit layout locally, preserving the exact bilinear/trilinear semantics without relying on the absent convenience macro. The two format-support masks combine integer representations before casting back to `D3D12_FORMAT_SUPPORT1`, which is constant-evaluable under both Windows and MinGW headers. DRED selects Settings1/Data1 only when declared; older headers and older runtimes retain page-fault and breadcrumb diagnostics through the base settings/data interfaces instead of dropping DRED altogether. Stable numeric breadcrumb/allocation ABI values avoid referencing newer enum members absent from older headers.
- **CI/test behavior:** the diagnostic-artifact regression explicitly models Linux with a mocked `wine64`, so its intended failed-test subprocess runs on every host. Linux policy tests and cross-compilation are separate Actions steps, making a policy failure stop the job before compilation. Source-policy coverage locks the missing-helper and DRED capability boundaries.
- **Verification:** all 49 build/PE policy tests passed. Direct native compiles of `dx12_dred.cpp` with DRED Settings1/Data1 forced off and with all DRED declarations forced off validated both compile-time fallback boundaries without warnings. Required clean native build `0.1.5100` passed x64/x86 hooks, CaptureEngine/MediaEngine, 149 unit-test objects, 30 test apps, both Vulkan layers, packaging, import closure, PE mitigations, effective native CFG, architecture, and PDB verification. Its exact-build no-build gate passed the complete native unit suite and all six Python tool self-tests. A fresh GitHub Linux runner remains the final end-to-end cross-host confirmation boundary.
- **Source anchors:** `hook/common/{dx12_sampler_policy,dx12_dred}.cpp`, `hook/apis/{dx12_streamline_ui_overlay,dx12_ffx_suspend_overlay}.cpp`, `test_build_flags.py`, `.github/workflows/hardening-ci.yml`, and `llm-wiki/build.py.md`.

### 2026-07-19 - Restore Linux MinGW GCC builds without weakening native Windows hardening

- **Failure/root cause:** Ubuntu CI selected `/usr/bin/x86_64-w64-mingw32-g++`, but the July hardening policy appended Clang-only `-mguard=cf` to every x64 compile; GCC rejected it before the first common source compiled. Static continuation showed four later host/toolchain failures: GNU linkers do not accept the guard/no-guard CFG switches, GCC has no `-ffp-model=strict`, final verification tried to execute downloaded Windows `llvm-readobj.exe` on Linux, and cross-builds were required to produce Windows-only PDB sidecars.
- **Compiler-specific policy:** `make_cpp_cflags()` now receives the selected compiler. Native Windows Clang keeps `-mguard=cf`, `--guard-cf`, `-Wno-microsoft-exception-spec`, and `-ffp-model=strict`; Linux MinGW GCC omits the unsupported CFG/diagnostic options, does not receive `--no-guard-cf` on x86, and maps strict FP to explicit `-fno-fast-math`, `-ffp-contract=off`, `-frounding-math`, and `-fsignaling-nans`. GCC still receives CET, stack protection, fortify, ASLR, NX, x64 high-entropy VA, W^X, and the existing baseline CPU policy. Unit-test links now receive the shared mitigation link flags too, making native unit-test CFG effective.
- **Host-correct verification:** Linux resolves a host `llvm-readobj`; hardening CI installs `llvm`. The PE verifier gained explicit `--allow-missing-x64-cfg` and `--allow-missing-pdb` boundaries used only for MinGW GCC/Linux cross outputs. Architecture, ASLR, NX, high-entropy VA, writable/executable sections, and runtime import closure remain fail-closed. Native Windows still requires effective x64 CFG and first-party PDBs; the existing x86 CFG exception is unchanged.
- **Coverage/verification:** 37 build-policy and 11 PE-verifier regression tests pass, plus 19 FFmpeg dependency/patch tests. The PE-verifier suite is now part of the ordinary unfiltered Python self-test gate, and Linux hardening CI runs both cross-host policy suites before compiling. Final required clean native build `0.1.5098` passed both hook architectures, MediaEngine/CaptureEngine, 149 unit-test objects, all 30 test apps, both Vulkan layers, packaging, import closure, PE mitigations, effective native CFG, and PDB checks. The exact-build no-build gate passed the complete native suite and all six Python tool self-tests. A fresh GitHub Linux runner is still required to provide end-to-end cross-host confirmation; the attached CI failure path itself is regression-covered without needing the compiler binaries on Windows.
- **Source anchors:** `build.py`, `tools/verify_pe_hardening.py`, `.github/workflows/hardening-ci.yml`, `test_build_flags.py`, `test_pe_hardening.py`, and `llm-wiki/build.py.md`.

### 2026-07-19 - Separate application video selection from DLL injection policy

- **User-facing model:** fresh `[Profile.*]` sections now use `video_capture=inherit|inject|wgc|dxgi_dup|none` for the video source and the separate `dll_injection=when_needed|always|never` policy. `when_needed` injects only for injected video, `always` preserves the injected overlay/graphics overrides with WGC, DXGI Duplication, or no video, and `never` stays non-injected. The reference explains both choices in ordinary language and includes distinct commented examples for DXGI/no injection plus DesktopOverlay/app audio, injected capture, and DXGI with injected overlay/graphics support.
- **Resolution and compatibility:** global `auto` plus `inherit` selects injected video when injection is permitted and WGC otherwise. New profiles that specify only DLL injection do not accidentally create a video target. Invalid injection values and explicit inject-with-never combinations fail closed. Existing `video_capture=global|default`, `injection_mode`, `injection`, legacy `[App.*]`, and whitelist inputs retain their historical behavior; the new `dll_injection` key wins when schemas are mixed.
- **Installed migration:** the ignored installed `config.ini` was migrated without restoring or changing unrelated custom settings. Existing HotS/Fortnite non-injected DXGI, Strange Brigade/test injected-auto, and Brave audio-only behavior remains equivalent, and the two additional commented examples use different placeholder executables.
- **Coverage / verification:** focused config/profile/pseudo-overlay/injected-overlay tests and the A/V matrix self-test passed. Incremental product build `0.1.5096` passed x64/x86 hooks, CaptureEngine/MediaEngine, both Vulkan layers, test applications, packaging, imports, PE mitigations, architecture, and PDB verification. Its exact-build no-build gate passed all 1,746 native tests in 135 suites and all five Python tool self-tests; a first AVIF atomic-publication attempt hit transient Win32 error 1175, then the isolated test and complete rerun passed unchanged.
- **Source anchors:** `captureengine/config.ini.template`, `common/config.{h,cpp}`, `tests/{test_config,test_config_override}.cpp`, `tools/run_av_sync_matrix.py`, and `llm-wiki/{configuration,current,dx12-injection-bootstrap,process-ipc,wgc-capture,log/recent}.md`.

### 2026-07-19 - Keep mixed-content delay events contextual and honor attached metadata-only analysis

- **Runtime evidence:** three-recording DXGI/Fortnite session `20260718_235749` had exact in-process A/V finalization, no encoder/mux/pool/ingress pressure, and source-limited lower-bound playout. The third recording accumulated 22 near-cap accepted selections over about 18.8 minutes and crossed the analyzer's session-total threshold, but the timestamped cadence events proved they were dispersed across desktop setup, gameplay, and post-game focus transitions: every event was accounted for and the maximum rolling 10-second total was only 7. The aggregate realized delay remained 38.133-53.972 ms with 3 ms residual p95, zero policy-added/excess repeats, and no post-selection or soft-late fault. The old classification therefore emitted a misleading `dxgi_dup_active_delay_realized_delay_unstable` verdict for mixed-content context rather than sustained capture behavior.
- **Window-aware classification:** cadence-event parsing now retains timestamps and computes fully accounted rolling 10-second near-cap pressure. Fewer than 10 per window may participate in the existing strict source-limited contextual downgrade; 10 or more in one window, incomplete/legacy timestamp accounting, soft-late acceptance, and every existing policy/coverage/timestamp fault remain strict. Console output exposes `near_cap_10s=<max>/<accounted>`.
- **Metadata-only root fix:** session-attached `--capture --metadata-only` previously called `analyze_completed_capture_exact` unconditionally, causing full packet/audio-tail work and requiring ffmpeg despite the selected mode. One dispatch now selects exact or metadata analysis consistently. Metadata reports are explicitly non-authoritative, perform only stream/container probing, and cannot manufacture exact-sync success or audio/visual faults; default/full scan remains unchanged and authoritative.
- **Coverage / verification:** analyzer self-tests cover the supplied dispersed 22-event/7-per-window shape, a clustered 12-event strict counterexample, incomplete per-window accounting, exact-vs-metadata dispatch, and non-authoritative attachment. Re-analysis of `r0003` now reports source-limited delay variation context and `near_cap_10s=7/22` with zero CE faults. The fixed metadata-only command probed the 17.6 GB external-SSD MKV in about two seconds without ffmpeg or a full-file scan. Final incremental product build `0.1.5095` passed x64/x86 hooks, CaptureEngine/MediaEngine, both Vulkan layers, test applications, packaging, imports, PE mitigations, architecture, and PDB verification; its exact-build no-build gate passed the complete native suite and all five Python tool self-tests.
- **Source anchors:** `tools/analyze_capture_av.py` and `llm-wiki/{wgc-capture,regression-testing-and-logging,log/recent}.md`.

### 2026-07-18 - Keep clean desktop source limitation and within-slack app latency contextual

- **Runtime evidence:** DXGI desktop/browser session `20260718_232903` preserved one unambiguous `r0001` media log/manifest and completed 47,627 exact 120-fps CFR frames. All three AAC tracks decoded cleanly to exactly 19,050,800 samples and identical endpoints. DXGI supplied only 16,814 changed desktop frames; 31,350/47,626 live ticks repeated (65.8%), but source/delivery lower-bound evidence accounted for every repeat, the longest contiguous hold was 15 frames/125 ms, and encoder/mux/pool/selection pressure was zero. The previous analyzer nevertheless emitted residual/audio-late/realized-delay/CE-visual faults because rare residual maxima and spread crossed fixed output-rate thresholds during prolonged low-update desktop delivery. It also treated 154 live app-latency warnings as a verdict even though final target-relative excess averaged 1 ms, maxed at 77 ms, and all overrun/underrun/trim/catastrophic counters were zero.
- **Diagnostic fix:** complete maximal-playout source evidence now scales the contextual delay/residual ceiling to the worst observed delivered-source interval. This downgrade requires matching requested/effective delay, complete smoothness verdicts, source-limited holds, no policy-added repeats, no mixed policy, no safe-candidate miss, no post-selection rejection, no soft-late accepted content, no incomplete evidence, and bounded source-relative residual/spread. Genuine policy, timestamp-domain, excess-repeat, candidate, and evidence faults remain strict. App-latency warnings are adjudicated against the final target-relative distribution and integrity counters: proven within-slack warning chatter becomes `app_audio_latency_within_slack`; elevated excess, queue overrun, underrun, catastrophic recovery, and legacy warning-only evidence remain `audio_app_latency_elevated`.
- **Result / scope:** the supplied session now reports only DXGI source/upstream starvation plus best-effort source coverage, with delivery/startup/source-delay/app-latency contexts and `encoder_or_mux=0 audio=0 visual=0`. This changes analyzer classification and console evidence only; capture scheduling, CFR, audio correction/mixing/finalization, encoder configuration, DXGI acquisition, and anti-cheat/injection behavior are unchanged.
- **Coverage / verification:** analyzer self-tests include the supplied 8-fps-minimum/65.8%-repeat desktop shape, benign live app-warning plus clean stop distribution, and the same latency shape with a concrete queue overrun to prove it stays strict. Existing historical policy/residual/timestamp/overload fixtures remain green. Incremental installed product build `0.1.5093` passed x64/x86 hooks, CaptureEngine/MediaEngine, both Vulkan layers, test applications, packaging, imports, PE mitigations, architecture, and PDB verification; its exact-build no-build gate passed the complete native suite and all Python tool self-tests.
- **Source anchors:** `tools/analyze_capture_av.py`, `llm-wiki/{current,index,cfr-capture-sync,wgc-capture,regression-testing-and-logging,log/recent}.md`, and `AGENTS.md` for the separately user-authored external-drive access rule.

### 2026-07-18 - Preserve every recording's evidence and attribute non-inject screen-grab targets

- **Supplied-session root cause:** session `20260718_214353` contained two controller recording starts but only the second, short Fortnite media run. Every disposable media child opened fixed `media.log` with truncation and every child rewrote `session_manifest.txt`, so the first long HotS media evidence was unrecoverable and the analyzer silently treated the later run as the session. The analyzer now reports `recording_evidence_missing_or_overwritten` for that exact legacy shape.
- **Recording evidence fix:** the controller allocates strict `rNNNN` recording identities before media spawn. Media writes `media_<recording-id>_<pid>.log` and a paired recording manifest, while only the controller owns the session manifest. Session triage discovers legacy/new evidence, refuses ambiguous defaults, supports `--recording-id`, `--media-log`, and `--all-recordings`, derives selected-recording QPC bounds before reading session-spanning performance CSVs, and suppresses unscoped gap attribution when those bounds are unavailable. The A/V matrix snapshots the new files without misclassifying them as hook logs.
- **Identity and anti-cheat boundary:** the old `PROCESS_QUERY_INFORMATION | PROCESS_VM_READ`/`GetModuleBaseNameA` helper could partially overwrite `unknown` into `Cnknown`. Identity now uses only documented `PROCESS_QUERY_LIMITED_INFORMATION` plus `QueryFullProcessImageName`, returns empty plus the Windows error on failure, and retains the PID/name confirmed by exact application-profile window selection. No target-memory read, concealment, injection change, or anti-cheat bypass was added; `injection_mode=none` remains genuinely non-injected.
- **Non-inject sensors:** exact ABI 37 adds a media-owned seqlocked screen-grab target PID/capture-device LUID snapshot that is separate from hook-owned `sourcePid`. Sensors prefer it only when no hook source exists, preserve capture-device provenance, and clear it at stop/failure/exit. This restores meaningful target CPU/GPU/VRAM attribution for profiled WGC/DXGI sessions without triggering injection.
- **Coverage / verification:** focused identity/IPC/shared-target/metrics regressions passed. Analyzer and matrix self-tests passed, including ambiguous-session selection, selected-recording QPC scoping, and legacy overwritten-evidence fixtures. Clean build `0.1.5090` passed x64/x86 hooks, MediaEngine/CaptureEngine, test apps, both Vulkan layers, packaging, import/PE/PDB verification; its exact-build no-build gate passed the complete native suite and all Python tool self-tests. Final incremental product build `0.1.5092` passed the same binary/package verification after the explicit hook-source-separation regression and final analyzer scoping were added. Fresh runtime validation remains pending and requires fresh target processes because of the ABI bump.
- **Source anchors:** `common/{process_identity,process_ipc,shared_defs}.*`, `captureengine/{main,inject_main,media_main,pseudo_overlay,sensor_service,host_metrics,host_metrics_policy}.*`, `tools/{analyze_capture_av,run_av_sync_matrix}.py`, `tests/{test_process_identity,test_process_ipc,test_shared_runtime_state,test_host_metrics_policy}.cpp`, and `llm-wiki/{current,index,process-ipc,wgc-capture,overlay-rendering,regression-testing-and-logging,log/recent}.md`.

### 2026-07-18 - Restore the tray icon after Explorer restarts

- **Runtime evidence / root cause:** in supplied session `20260718_223556`, build `0.1.5088` created its tray icon normally and the controller continued processing at roughly 1 Hz without a CaptureEngine crash. Explorer foreground ownership changed from PID 9084 to a new PID 16712 at 22:37:16, matching Explorer's restart. Explorer owns notification-area entries and discarded CaptureEngine's icon with its process state; `TrayIcon` added the icon only once at startup and had no handler for the registered `TaskbarCreated` broadcast.
- **Root fix / diagnostics:** the existing hidden top-level tray owner now registers and handles `TaskbarCreated`, then republishes the unchanged `NOTIFYICONDATA` with `NIM_ADD`. This preserves the current idle, recording, or shutdown icon and tooltip. The `TrayIcon` instance is attached during `WM_NCCREATE` so there is no post-creation broadcast gap, and intentional icon removal disarms later recovery. Initial add and taskbar-recovery success/failure now emit high-signal controller diagnostics; no polling, delay, background thread, or steady-state work was added.
- **Coverage / verification:** `TrayIconSourceTest.RestoresCurrentIconStateWhenExplorerRecreatesTaskbar` locks the registered-message wiring, early instance binding, current-state reuse, and removal guard. Its focused tests-only gate passed. Incremental installed product build `0.1.5089` passed CaptureEngine/MediaEngine, x64/x86 hooks, test apps, both Vulkan layers, packaging, import closure, PE mitigations, architecture, and PDB checks. The exact-build no-build gate passed the complete native suite and all five Python tool self-tests. Because the supplied running controller is still build `0.1.5088`, a CaptureEngine restart and then a real Explorer restart remain the runtime confirmation boundary.
- **Source anchors:** `captureengine/tray.{h,cpp}`, `tests/test_tray_source.cpp`, `llm-wiki/{current,index,process-ipc,log/recent}.md`.

### 2026-07-18 - Make mip-filter overrides reliable across legacy D3D, Vulkan, and OpenGL

- **Unified contract:** `[Graphics] mip_mapping` is now case-normalized and accepts only `default`, `nearest`, `bilinear`, or `trilinear`; invalid input logs once and falls back to `default`. One shared policy maps those modes to point/linear MIN, MAG, and mip-filter triples and covers all four OpenGL mip MIN enums without enabling mipmapping on a non-mip object.
- **Mutable Direct3D coverage:** D3D6/7 hooks are installed on every actual returned device vtable instead of assuming the bootstrap HAL class, and DX8 sampler originals are also retained per vtable. DX6/7 refresh config at EndScene; DX8/9 retain their Present refresh. D3D7/8 ApplyStateBlock and D3D9 state-block Create/End/Apply are intercepted so a successful Apply refreshes physical state while retaining tracked logical state, then immediately reapplies the override. D3D9 config-disable transitions now pass through the state engine even when the new config is already `default`. Pure DirectDraw 2D has no sampler state; its hosted mip coverage is the D3D6/7 device path.
- **Vulkan/OpenGL coverage:** Vulkan mip eligibility is independent of the stricter safe-AF material heuristic, so ordinary point and clamp-to-edge samplers receive the requested mode while comparison, special-reduction, border, unnormalized, nonstandard, and no-mip-range descriptors remain protected; rejected modified descriptors retain the original transactional retry. OpenGL scalar/vector/float, bound, sampler-object, core/EXT DSA, allocation/storage/copy/view/mipmap, and cached bind paths now share the mip mapping. Bind caches are context/config/object-generation scoped, and texture/sampler deletion invalidates reused names across contexts.
- **Coverage / verification:** policy tests lock normalization, all OpenGL mip enums, discrete D3D triples, and Vulkan mip-vs-AF eligibility; config and source-wiring regressions cover invalid input, per-vtable legacy hooks, state blocks, bind/delete hooks, and transactional Vulkan fallback. The focused gate passed. The first product build exposed one enum-to-DWORD template mismatch and correctly failed; after the narrow cast fix its required resume completed as `0.1.5087`. Final incremental installed product build `0.1.5088` passed x64/x86 hooks, CaptureEngine/MediaEngine, test apps, both Vulkan layers, packaging, import closure, PE mitigations, architecture, and PDB checks. The exact-build no-build gate passed the complete native suite and all five Python tool self-tests. Native legacy/OpenGL/Vulkan runtime validation remains pending.
- **Source anchors:** `common/{config,mip_mapping_policy}.*`, `hook/apis/{ddraw_hook,dx8_hook,dx9_hook,dx9_sampler_state,legacy_d3d_sampler_state,opengl_sampler_override}.*`, `hook/vulkan_layer/{vulkan_layer,vulkan_sampler_policy}.*`, `tests/{test_config,test_inject_capture_source,test_mip_mapping_policy,test_vulkan_sampler_policy_source}.cpp`, and `llm-wiki/{current,cross-api-forced-af,graphics-overrides-and-frame-pacing,index,log/recent}.md`.

### 2026-07-18 - Suppress the encoder priming capacity false alarm

- **Runtime evidence / root cause:** DX12 inject session `20260718_194307` finalized 950/950 contiguous 120-fps frames with zero skips, duplicates, backpressure, or encoder/mux overload and exact audio endpoints, but its first encoded frame seeded the encode-time average at `20.19 ms` and immediately logged `Encoder approaching capacity`. The bottleneck state already ignored the 1.5-second startup window; the separate warning bypassed that guard.
- **Fix / coverage:** the warning now shares the established startup-window policy, while the encoder continues learning its average during priming. Genuine post-startup pressure retains the 85% threshold and five-second rate limit, and real warnings use the normal warning severity. A focused policy test covers the observed first-frame sample plus post-startup threshold behavior.
- **Verification:** both focused regression tests and the complete capture-policy/coordinator suites passed. Incremental product build `0.1.5086` passed x64/x86 hooks, MediaEngine/CaptureEngine, both Vulkan layers, packaging, import/PE/PDB verification, and test applications; the exact no-build gate passed the complete native suite and all five Python tool self-tests.

### 2026-07-18 - Make ordinary media stop explicit and keep expected latency telemetry plain

- **Runtime evidence:** HotS session `20260718_191043` selected the process profile, DesktopOverlay warning/pending/live transitions, DXGI Duplication, and app-audio tracks 1/2 correctly. Video and all three audio endpoints finalized at exactly `8,533,333 us`, with no encoder/mux/pool pressure or excess repeats. Its two alarming-looking diagnostics were not capture failures: the controller intentionally closed media IPC on the normal stop path (`ERROR_BROKEN_PIPE`), and process loopback returned `E_NOTIMPL` from `GetStreamLatency` while the automatic endpoint probe still produced high-confidence latency.
- **Graceful stop root fix:** ordinary video and audio-only stops now send the authenticated media `StopRecording` command first. Media clears hook-facing state and stale shared commands, acknowledges before lengthy encoder/mux finalization, then finalizes and exits; the controller releases its endpoint without blocking. Inject publishes the shared-memory stop only if media did not accept the private command, so normal stop no longer looks like fatal IPC loss and cannot leave a stale stop flag for the next media child. Recording-failure and pre-live cancellation paths use the same cleanup helper. The old immediate inject-command retry was removed because a failed `SendCommand` has already invalidated that endpoint.
- **Audio diagnostic wording:** `E_NOTIMPL` from endpoint/process-loopback `GetStreamLatency` is now identified as unsupported telemetry. Process loopback explicitly states that the automatic render-endpoint probe remains authoritative; other HRESULT failures remain visible as unavailable telemetry. Capture and synchronization behavior are unchanged.
- **Practical fresh-config example:** the application-profile reference now ends with a fully commented HotS example using `injection_mode=none`, `video_capture=dxgi_dup`, profile-local DesktopOverlay, and app audio on tracks 1/2. Resource parity and the exact fields are regression-checked.
- **Coverage / verification:** the focused `ProcessIPCTest.*:AudioCaptureSourceTest.*:ConfigTest.*` gate passed 89 tests. Incremental product build `0.1.5085` passed both hook architectures, CaptureEngine/MediaEngine, packaging, both Vulkan layers, import closure, PE mitigations, and PDB verification. The exact no-build gate passed all 1,728 native tests in 132 suites and all five Python tool self-tests.

### 2026-07-18 - Make DesktopOverlay a first-class application-profile consumer

- **Real per-app settings:** the controller now resolves every `[DesktopOverlay]` key for each process-backed `[Profile.*]` and supplies the compact results to the dedicated overlay thread. While idle, foreground process identity selects the effective profile; recording start pins it so Alt+Tab cannot change mode, position, size, or enabled state midway through a session. An actual injected-video source PID can replace the provisional foreground choice.
- **One application target model:** every process-backed profile with a video route now triggers the `NOT RECORDING` warning automatically. Global `[DesktopOverlay] process_list` remains readable only as a compatibility/extra-unprofiled-process fallback; `video_capture=none` audio profiles do not become warning targets. Profile-local `DesktopOverlay.process_list` is deliberately not a second route list.
- **Template and keepalive:** the fresh profile example documents qualified `DesktopOverlay.*` overrides and explains that video profiles are warning targets without `process_list`. The touched idle keepalive path now writes its documented one-pixel alpha surface rather than publishing a completely transparent bitmap.
- **Full annotation audit:** the template now explains independent output folders, keyframe units, Media Foundation quality/hardware behavior, per-source audio overrides, the start/stop hotkey fallback, overlay layout and alpha, desktop-overlay geometry/keepalive/overload behavior, the two FPS limiters, graphics and DLSS value sets, audio-sync timing, and injection/anti-cheat risk in direct user-facing language. `nvidia_smooth_motion_compat` was removed from the parser and template: compatibility remains automatic and code defects must be fixed rather than exposed as a switch.
- **Profile selector correction:** `window_match` now affects only title/class matching. Canonical WGC/DXGI profile process selectors remain exact and case-insensitive even when title matching uses `contains`; legacy route lists retain their compatible partial-process behavior.
- **Coverage / verification:** the focused config/profile/pseudo-overlay gate passes 101 tests across six suites, covering exact resource annotations, exact canonical process selection, all DesktopOverlay override fields, case-insensitive profile selection, automatic video-profile warning targets, compatibility-list fallback, visibility policy, and dedicated-thread shutdown. Clean product build `0.1.5084` passed both hook architectures, CaptureEngine/MediaEngine, packaging, both Vulkan layers, and binary/PDB verification; the exact no-build gate passed all 1,726 native tests in 132 suites and all five Python tool self-tests.

### 2026-07-18 - Replace fresh-config whitelists with named application routes

- **One application model:** fresh configs now use unlimited named `[Profile.*]` sections for `video_capture=global|inject|wgc|dxgi_dup|none`, `injection_mode=capture|overlay|none`, optional plain `window_title` / `window_match`, app audio, and qualified per-app overrides. The old `[Injection] whitelist`, `overlay_whitelist`, and `wgc_window_detection` blocks are absent from the template but remain readable compatibility inputs. Canonical profiles remove overlapping legacy process/window entries before generating the injector and WGC runtime adapters; overlapping profiles are diagnosed and the later canonical profile wins.
- **Routing and safety:** profile WGC and DXGI candidates use the existing foreground/fullscreen/title/process/area scoring, are compared across both backends, and load the selected process profile before recording so media overrides apply. `video_capture=inject` requires `injection_mode=capture`; invalid combinations fail closed to no route. Injection process matching remains exact even when a profile uses title-contains matching. Existing profiles without `video_capture`, legacy `[App.*]`, and `[AppAudio.N]` keep their old behavior. A profile-local `none` capture method rejects an ordinary video start while audio-only recording remains available.
- **Unbounded named audio:** profile enumeration and profile-local app audio are no longer limited to slots 1-8. Numeric profiles still shadow the corresponding compatibility `[AppAudio.N]` when they contain `audio_enabled`; empty invalid `[Profile.N]` sections still fall back to legacy `[App.N]` as before. The A/V matrix generator now emits canonical profiles rather than the three old route lists. The user-owned ignored installed `config.ini` was deliberately not read or changed.
- **Coverage / verification:** 82 focused config/resource/override/legacy-route tests pass, including named routes, WGC/DXGI/inject/none resolution, legacy precedence, invalid injection permission, duplicate resolution, plain window matching, dynamic app audio, and empty-canonical legacy fallback. The A/V matrix self-test and Python syntax checks pass. Final incremental product build `0.1.5081` passed both hook architectures, MediaEngine/CaptureEngine, packaging, 30 test apps, both Vulkan layers, PE/import/PDB verification; the exact no-build gate passed all 1,721 native tests in 131 suites and all five Python tool self-tests.

### 2026-07-18 - Make profile injection mode self-explanatory

- The fresh `[Profile.N]` example now uses `injection_mode` instead of the vague `injection` key. Its documented values are also self-describing: `capture` permits injected video plus overlay/graphics overrides, `overlay` injects overlay/graphics overrides without injected video capture, and `none` does not inject while profile app-audio remains usable; omission also means `none`.
- Existing `[Profile.N] injection=...` files and the old `normal`/`inject` value spellings remain compatible. The canonical `injection_mode` value wins when both keys are present, and the A/V matrix generator now emits the canonical key.
- Focused `ConfigTest.*:ConfigOverrideTest.*` passed all 65 tests. Final product build `0.1.5080` passed both hook architectures, CaptureEngine/MediaEngine, packaging, both Vulkan layers, and binary verification; the exact no-build gate passed all 1,716 native tests and all five Python tool self-tests.

### 2026-07-18 - Unify application profiles and finish the default annotations

- **NVENC/audio wording:** the fresh template now states why `lookahead` uses `off`/`auto`/a numeric depth while `spatial_aq` and `temporal_aq` are Booleans, what `aq_strength=0` means and when it is ignored, and how `sample_rate=default`, `bit_depth=default`, and `downmix=false` resolve. The A/V matrix generator now emits the same canonical NVENC spellings instead of the invalid/legacy `lookahead=false` and `aq=false` pair.
- **Unified application profiles:** `[Profile.N]` is now the fresh-config home for one executable's injection policy, optional `audio_*` process-loopback source, and qualified video/graphics/FPS overrides. Profile audio inherits `[Audio]`, is enumerated literally so the active override cannot contaminate other profiles, and takes precedence over same-numbered legacy `[AppAudio.N]` when `audio_enabled` is present. `[AppAudio.N]` and `[App.N]` remain compatibility forms. The ignored installed config is intentionally left at the new fresh default; restoring prior local custom values was not requested.
- **Template scope:** the application-profile example moved to the final section after diagnostics. `msaa_samples`, `sgssaa`, and `disable_auto_mip_bias` were removed from fresh and installed config text; their parser/runtime paths remain intact for existing specialized configs. Focused coverage checks profile audio inheritance/overrides, cross-profile isolation, canonical precedence, exact template generation, final-section placement, annotations, and omission of those legacy controls.
- **Verification:** focused `ConfigTest.*:ConfigOverrideTest.*` passed all 64 tests. Final incremental product build `0.1.5078` passed x64/x86 hooks, CaptureEngine/MediaEngine, 145 test objects, 30 test apps, both Vulkan layers, packaging, and binary verification. The exact-build no-build gate passed all 1,715 native tests and all five Python tool self-tests.

### 2026-07-18 - Reorganize config by user task and make profile injection explicit

- **Layout and wording:** the fresh template now separates `[Capture]`, `[WGC]`, `[Output]`, `[VideoScaling]`, `[SystemAudio]`, `[DesktopOverlay]`, `[DLSS]`, `[AudioSync]`, `[Logging]`, and `[Diagnostics]`. Stable WGC behavior is no longer mixed with logging/audio/debug settings in `[General]`; developer overlay probes and the WGC flush/VRAM experiments are at the end under troubleshooting. Comments were rewritten in short ordinary language. Fresh logging is `debug` rather than trace, system output uses track 1, and the disabled microphone is prepared for track 2. The build helper's test-time logging insertion and the A/V matrix's generated scenario configs use the new sections too.
- **Compatibility and profile safety:** `common/config.cpp` reads the new location first and preserves the old `[General]`, `[Scaling]`, `[Screenshot]`, `[pseudo-overlay]`, `[Audio.N]`, Graphics/DLSS, Performance/copy-queue, and other moved locations as aliases. New `[Profile.N]` sections require `injection=normal|overlay|none`; legacy `[App.N]` keeps implicit normal injection. Profile injection targets are merged after manual whitelist parsing and de-duplicated, fixing the loader-order bug that previously pushed `[App.N]` targets and then cleared them. Manual section matching recognizes canonical/legacy desktop-overlay names case-insensitively.
- **Audio source model:** `[Audio]` now supplies common encoding defaults, while `[SystemAudio]` / `[SystemAudio.N]`, `[Microphone]` / `[Microphone.N]`, and `[AppAudio.N]` describe sources. System and microphone sources can now override codec, bitrate, sample rate, bit depth, and downmix just as the template says. Legacy `[Audio]` source keys and `[Audio.N]` remain functional.
- **Local migration and coverage:** the ignored installed config was rewritten on the same plain-language layout while preserving the user's WGC targets, injection lists, output/encoder/audio choices, app-audio sources, desktop warning list, and graphics/FPS profiles; converted profiles use explicit `injection=normal`. Focused `ConfigTest.*:ConfigOverrideTest.*` passes 62 tests covering canonical precedence, legacy aliases, both desktop-overlay spellings, canonical/legacy numbered audio overrides, explicit profile modes, no-mode safety, and legacy App auto-injection. Required clean product build `0.1.5075` and final incremental product build `0.1.5076` passed both hook architectures, CaptureEngine/MediaEngine, 145 test objects, 30 test apps, both Vulkan layers, packaging, and binary verification. The exact `0.1.5076` no-build gate passed all 1,713 native tests and all five Python tool self-tests.
- **Repeat-run test blocker fixed:** a second full-suite run could fail `ScreenshotAvifTest` before encoding because its PID/sequence temp directory could survive an older process with a recycled PID. Both screenshot tests that create such directories now remove only their own test-scoped path before setup. The focused AVIF test and the subsequent full 1,713-test gate pass.

### 2026-07-18 - Make the first-run config single-source, complete, and self-validating

- **Drift/root cause:** first-run creation was a 17 KB raw string in `common/config.cpp`, while integration tests read `captureengine/config.ini.template`. The two 439-line profiles had independently drifted: the template exposed two options missing from generated files, generated files retained other WGC/audio documentation, and both omitted supported overlay/graphics/performance controls or described log levels, screenshot paths, audio track limits, injection routing, pseudo-overlay mode 2, and App-profile side effects incorrectly. The installed config also retained deprecated WGC/NVENC aliases and ambiguous bare App override keys.
- **Single source / migration boundary:** the UTF-8 template is now embedded as `RCDATA` in CaptureEngine and the unit-test executable. Missing-config creation writes those exact bytes with `CREATE_NEW` and removes partial writes; there is no hardcoded fallback profile. Tests prove byte-for-byte source/resource/generated parity, and the integration harness fails closed if the template is absent. Existing configs remain untouched automatically, so explicit shipped-profile values and older-file missing-key compatibility fallbacks can intentionally differ. The locally installed ignored config was manually rewritten on the new annotations while preserving its customized capture, output, encoder, audio, process, and per-app policy; deprecated aliases became canonical keys and App overrides became section-qualified.
- **Defaults/annotations/validation:** fresh microphone capture is disabled for privacy. The profile now documents every stable user-facing WGC/cadence/latency, overlay styling/HDR, hotkey, audio/AppAudio, performance, graphics/DLSS, pseudo-overlay, screenshot, and App-profile control while keeping intentional legacy/internal VFR and Smooth Motion knobs hidden. It calls out video-method/injection independence, App-profile auto-whitelisting, logical track fanout/mixing and duplicate system/app capture, shared render versus microphone timing domains, actual `captures` output behavior, mode-2 warning-only behavior, and the historical `copy_queue_priority` name. Malformed booleans, track IDs, colors, overlay bounds, and pseudo-overlay bounds now use documented fallbacks with rate-limited diagnostics; valid track IDs are unique `1..255`.
- **Coverage / verification:** focused `ConfigTest.*` passed with exact creation parity and new invalid-value regressions. Required clean/default build `0.1.5074` passed x64/x86 hooks, MediaEngine/CaptureEngine, 145 unit-test objects, 30 test apps, both Vulkan layers, packaging, import closure, PE mitigations, and PDB verification. The exact-build no-build gate passed the complete native suite and every Python tool self-test.
- **Source anchors:** `captureengine/{config.ini.template,captureengine.rc}`, `common/{config_resource.h,config.h,config.cpp}`, `tests/{config_template.rc,test_config.cpp}`, `testapp/run_tests.py`, `build.py`, and `llm-wiki/{configuration,current,index,log/recent}.md`.

### 2026-07-18 - Show recording startup immediately in both overlays

- **State/lifecycle contract:** both recording hotkeys now publish a controller-owned `Video` or `AudioOnly` intent in unused atomic runtime-flag bits before media or limiter readiness waits. The intent survives calibration and hidden-frame warm-up without a timeout. Media clears it only when live `isRecording` is published, stop/cancel occurs, or startup reaches a terminal failure; live state wins over a stale pending bit. Synchronous initialization failures and deferred WGC encoder/device/mux prewarm failures publish `RecordingStartFailed`, while controller readiness, command-delivery, required-child-loss, stop, failure, and shutdown paths clear their local/shared ownership deterministically.
- **Inject/pseudo presentation:** the inject overlay honors existing `overlay_enabled`/`show_recording`, reserves pending-label width, invalidates its cache on resolved state changes, renders amber `STARTING RECORDING...` / `STARTING AUDIO...`, suppresses encoder warnings until live, then preserves the red `REC`/`AUDIO` timer. The pseudo-overlay preserves enabled/mode/process-list/anchor/focus/inject-handoff policy: modes 0/1 show an amber pending circle, modes 1/2 show amber startup text, pending suppresses competing notifications and `NOT RECORDING`, and established mode 2 remains windowless except its existing notifications.
- **Thread ownership:** pseudo window classes, windows, GDI objects, timer, shared mappings, polling, and rendering now live on a dedicated message thread. Public controller calls publish synchronized state and post refresh messages. `Init` uses a bounded readiness handshake without closing the event under the initializing thread; `Shutdown` posts exit, joins, and tears UI/shared resources down on their owner. Controller stalls can still delay tray/hotkey handling before a message is dispatched, but no longer starve existing pseudo-overlay windows.
- **Coverage/verification:** focused state/layout/visibility/focus/thread/source regressions passed, including atomic flag preservation, live precedence, exact labels, config gating, pending notification priority across all modes, child/failure cleanup contracts, and disabled-window cross-thread refresh/joined shutdown. Final required clean/default build `0.1.5073` passed x64/x86 hooks, MediaEngine/CaptureEngine, 145 unit-test objects, 30 test apps, both Vulkan layers, packaging, import closure, PE mitigations, and PDB verification. The exact-build no-build gate passed the complete native suite and every Python tool self-test. Fresh game/runtime visual validation remains pending.
- **Source anchors:** `common/{shared_defs,recording_indicator_policy,pseudo_overlay_visibility}.h`, `captureengine/{main,inject_main,media_main,pseudo_overlay}.{h,cpp}`, `hook/common/{overlay_adapter,overlay_layout_policy}.*`, `tests/{test_shared_runtime_state,test_overlay_system,test_pseudo_overlay_visibility,test_pseudo_overlay_thread,test_recording_start_feedback}.cpp`, `build.py`, and `llm-wiki/{overlay-rendering,pseudo-overlay,index,log/recent}.md`.

### 2026-07-18 - Suppress Windows launch-feedback cursors for internal CaptureEngine processes

- **Root cause / scope:** the installed `captureengine.exe` is a Windows GUI-subsystem image. The shared restricted-child launcher initialized `STARTUPINFOEXW` without `STARTF_FORCEOFFFEEDBACK`, so Windows could show its GUI process-start feedback cursor for every otherwise hidden media/logger/sensor/limiter/inject child and for disposable process-loopback app-audio workers created when application sessions appeared, disappeared, or recycled. `CREATE_NO_WINDOW` is ignored for GUI-subsystem applications and did not solve this. CaptureEngine contains no matching explicit wait-cursor set; its cursor renderer reads rather than changes the desktop cursor.
- **Fix / boundary:** all restricted internal children now opt out through `STARTF_FORCEOFFFEEDBACK`. The injected hook's hidden same-image external dump helper uses the same opt-out. Controller mode clears externally requested startup feedback at the earliest point after private-worker/dump dispatch and creates its hidden tray owner before Vulkan registration or child startup can extend the feedback interval. Private worker modes no longer reset the user's current cursor. Direct user game/launcher starts retain normal Windows feedback. Feedback applied by an external shell before CaptureEngine reaches `WinMain` cannot be prevented internally, only minimized by this earliest controller path.
- **Coverage / verification:** source regressions lock the shared launcher flag, Controller-only cursor ordering, lack of an unconditional pre-dispatch cursor reset, early hidden-tray ownership, and the hook dump-helper flag. A nested restricted-child probe calls `GetStartupInfoW` and verifies that Windows delivered `STARTF_FORCEOFFFEEDBACK`, while retaining the existing handle-inheritance proof. The focused `ProcessIPCTest.*:RestrictedChildLauncherProbe.*:CrashHandlerBinaryTest.*:CrashHandlerSourceTest.*` gate passed. Required incremental installed product build `0.1.5070` passed x64/x86 products, hooks, CaptureEngine/media, test apps, both Vulkan layers, packaging, import closure, PE mitigations, and PDB verification; the exact-build no-build gate passed the complete native and Python tool suites. At the user's request, no interactive capture harness was launched; fresh ordinary startup and application-audio churn observation remains pending.
- **Source anchors:** `common/restricted_child_process.cpp`, `captureengine/main.cpp`, `hook/main.cpp`, `tests/{test_process_ipc,test_crash_handler}.cpp`, and `llm-wiki/{process-ipc,current,index,log/recent}.md`.

### 2026-07-18 - Preserve the initial process-loopback epoch marker and close recovery epochs in order

- **Runtime proof / stop ownership:** supplied session `installed/captureengine/logs/20260718_024700` was not user-stopped. Controller diagnostics remained `hk=0` and at 03:07:15.927 it automatically reported `Recording failed with integrity code 1`. Brave worker generation 8 had just selected PID 17464, started exact 48 kHz/stereo/float32 capture at epoch 1, and qualified its first 480-frame packet. Protocol 4 then reported `ProducerPacketRejected/ProducerEpochValidation`, sequence and descriptor/byte cursors all zero, private lifecycle `0/0`, failed data epoch 1, and committed sequence zero. No crash dump or `crash.log` exists in the literal session inventory. Finalization remained exact: the 144,661-frame CFR endpoint was `1,205,508,333 us`, all three AAC input/target counts were 57,864,400 samples, and the post-mux probe reported the same video/audio endpoint with zero inter-audio delta.
- **Root cause:** `ActivateClientForPID` successfully enqueued `EpochStart(1)`, but its caller then entered `StartCaptureThreadForCurrentClient`, which unconditionally cleared `packetQueue` before launching `CaptureLoop`. That erased the only lifecycle record. The first data packet therefore reached the strict producer with no open epoch and was correctly rejected. This was deterministic for a newly activated late app source and was exposed, rather than caused, by protocol 4's evidence-complete validation.
- **Fix / adjacent lifecycle hardening:** capture-thread launch no longer clears the already-committed queue, so the ordered start marker necessarily precedes every data packet. One exception-contained marker helper now publishes both start and end records and logs type, epoch, reason, and queue depth; capture-thread creation failure closes the start it already published. Before mid-stream WASAPI recovery abandons the old client, `CaptureLoop` commits `EndOfStream(old epoch)`, then a successful replacement commits `EpochStart(new epoch)`. Local open/closed state prevents duplicate EOS after failed retries or final loop exit. Marker-allocation failure defers reactivation instead of escaping the capture thread or publishing a replacement epoch out of order.
- **Coverage / verification:** new source regressions reproduce the erased-start boundary and require close-before-reactivation plus non-duplicated final EOS; the complete strict protocol suite remains included. Focused `AudioCaptureSourceTest.*:ProcessLoopbackProtocolTest.*` passed. Required incremental installed product build `0.1.5069` passed x64/x86 products, MediaEngine/CaptureEngine, test apps, both Vulkan layers, packaging, imports, PE mitigations, and PDB verification. The exact-build no-build gate passed the complete native suite and all Python tool self-tests. At the user's request, no interactive capture harness was launched; fresh real-world confirmation remains pending.
- **Source anchors:** `mediaengine/app_audio_capture.{h,cpp}`, `tests/test_audio_capture_source.cpp`, and `llm-wiki/{current,index,multi-audio-capture,log/recent}.md`.

### 2026-07-18 - Make first process-loopback epoch consumption transactional after the repeat automatic stop

- **Runtime proof / corrected diagnosis:** neither supplied session `installed/captureengine/logs/20260718_004211` nor `20260718_022031` was user-stopped (`hk=0`); both stopped automatically with process-loopback integrity code 1 and finalized their media cleanly. The first protocol-v2 trace could not distinguish producer rejection from consumer corruption, so the earlier producer-format attribution was not proven. The protocol-v3 Heroes of the Storm reproduction did distinguish it: `status=3 stage=6/ConsumerEpochValidation sequence=0 lastError=0xd`, `produced=1 consumed=0`, followed by an exact requested 48 kHz/stereo/float32 480-frame packet. Thus the first `EpochStart` descriptor was still at sequence 0 while shared consumer lifecycle state rejected it as already active or stale. The former multi-atomic protocol made that partial combination representable; the logs do not retain the exact interruption or corruption mechanism that produced it.
- **Root invariant / protocol 4:** consumer lifecycle state no longer lives in the cross-process header. Each worker owns a generation-bound producer state and the parent `WorkerInstance` owns its serialized consumer state; both track expected descriptor/byte cursors plus current/last epoch. Producer packet shape and lifecycle order are validated before commit. Consumer read materializes the output, publishes both shared read cursors, then advances only its private lifecycle state. `DiscardPackets` scans the complete snapshot into prospective locals and publishes nothing if any later descriptor or epoch is invalid. Endpoint state cannot be reused across a worker generation or a divergent cursor. The consumer still independently validates committed descriptors/epochs, so moving state private does not weaken corruption rejection.
- **Failure publication / diagnostics:** first failure ownership is now claimed by the fatal `PublishingFailure` transport status. The winner fills final status/stage names, failure descriptor sequence, read/write descriptor and byte cursors, current/last/packet epoch, record type, observed committed sequence, and error before publishing the final status. A publisher that exits halfway remains fatal instead of looking healthy. Worker and parent fatal/exit lines relay the complete evidence; producer packet, epoch, and endpoint-state failures are distinct from consumer/discard validation.
- **Coverage / verification:** focused `ProcessLoopbackProtocolTest.*` passed with new first-epoch coherence, producer/consumer epoch rejection, state-generation mismatch, evidence, incomplete-publication, transactional-discard rollback, and concurrent SPSC order coverage. Required clean/default build `0.1.5068` rebuilt and verified x64/x86 products, CaptureEngine/mediaengine, test apps, shaders, both Vulkan layers, packaging, imports, PE hardening, and PDBs. The complete no-build native unit suite and all Python tool self-tests passed. At the user's request, no interactive capture harness was launched; a fresh real late-join capture is the remaining runtime gate.
- **Source anchors:** `mediaengine/{process_loopback_protocol,process_loopback_protocol_types,process_loopback_capture,process_loopback_worker}.*`, `tests/test_process_loopback_protocol.cpp`, and `llm-wiki/{current,index,multi-audio-capture,log/recent}.md`.

### 2026-07-18 - Host disposable process-loopback workers in CaptureEngine

- **Architecture / boundary:** every unique physical app-audio capture still owns one disposable process, but that process now runs the installed CaptureEngine image with the private exact-first-argument command `captureengine.exe --process-loopback-worker ...`. `WinMain` dispatches the role before dump handling, `ProcessMode`, configuration, logging, crash setup, or singleton/controller startup; a recognized command with bad arity or invalid transport arguments returns an error without controller fallback. The host preserves PID/name XOR, process-name, channel-mask, inherited-handle, initially-unsignaled-event, mapping-generation, requested-format, and protocol-3 validation, clears inheritance, establishes the restricted DLL search path, explicitly loads `mediaengine.dll`, and invokes `MediaEngine_RunProcessLoopbackWorker`.
- **Launch / lifecycle invariants:** `ProcessLoopbackCapture` resolves the current absolute executable and launches that same image with only the existing mapping, packet event, and stop event allowlisted. Protocol 3, safe late-join metadata canonicalization, first-failure stage/sequence/error publication, ordered epochs, ring draining, clean recycle, unexpected-exit restart, shutdown deadlines, fatal transport handling, and one physical capture per unique source are unchanged. Active diagnostics now consistently say worker/`workerPid`.
- **Packaging / cleanup:** the standalone helper source and build/link/PDB target are gone. Product builds fail closed while removing obsolete helper EXE/PDB/known temporary and cached outputs from the package/object roots, then assert none reappeared before final verification. The first focused build removed 80 legacy-named artifacts. Malformed private dispatch returned `ERROR_BAD_ARGUMENTS` and invalid inherited transport returned `ERROR_INVALID_PARAMETER`; neither entered Controller mode.
- **Coverage / verification:** the focused `AudioCaptureSourceTest.*:ProcessLoopbackProtocolTest.*:ProcessIPCTest.*:SecureDllLoading*` gate passed, including same-image/exact-handle-list, early-dispatch/non-`ProcessMode`, validation, secure-load, protocol-3 canonicalization, and staged-failure checks. All 34 `test_build_flags.py` tests passed, including stale-output cleanup and locked-artifact failure. Final required zero-cache product build `0.1.5067` passed x64/x86 products, CaptureEngine/media, test apps, both Vulkan layers, packaging, shaders, and PE/import/PDB verification; the canonical no-build gate passed the complete native and Python suites. Literal package/object inventory and process checks found no helper artifact/process. The interactive late-app matrix was canceled at the user's request and is not claimed; its seven started CaptureEngine processes were terminated immediately.
- **Source anchors:** `captureengine/{main,process_loopback_worker_host}.*`, `mediaengine/{mediaengine,process_loopback_capture,process_loopback_protocol,process_loopback_worker}.*`, `build.py`, `test_build_flags.py`, `tests/{test_audio_capture_source,test_process_ipc,test_process_loopback_protocol,test_secure_dll_loading}.cpp`, and `llm-wiki/{build.py,current,index,multi-audio-capture,process-ipc,log/recent}.md`.

### 2026-07-18 - Keep a late Brave audio join from falsely failing the process-loopback contract

- **Runtime proof / stop ownership:** supplied session `installed/captureengine/logs/20260718_004211` began recording at 00:49:14 and stopped automatically at 01:08:54 after 1,178,808 ms. The controller recorded `Recording failed with integrity code 1` while its cycle had `hk=0`, so no stop hotkey caused it. A generation-4 Brave name helper had waited since 00:51:39, selected the new PID 9368 at 01:08:54, started exact 48 kHz/stereo/32-bit capture, and received a WASAPI-reported 480-frame first packet. Its exit counters were `produced=1 consumed=0`: the epoch marker committed, then the producer rejected the first data packet before it could enter the shared ring. Status 3 conflated that local validation boundary with actual committed-descriptor/epoch corruption and the worker replaced the causal invalid-data error with buffer-overflow. Media/container finalization itself remained clean and exact.
- **Root fix / strict boundary:** process-loopback protocol 3 treats the explicitly initialized float32 WASAPI request as immutable. Before producer validation, only auxiliary `validBitsPerSample`, channel mask, and float classification are canonicalized, and only when rate/channels/bit depth/block alignment/nonempty payload shape already match the request. A block-alignment, byte-shape, epoch, or lifecycle mismatch remains fatal, as do descriptor/byte exhaustion and consumer committed-descriptor/epoch corruption. Producer rejection has its own status. First failure now atomically publishes stage, descriptor sequence, exact error, and status in evidence-safe order; helper/parent logs include the rejected packet and requested contract, and tail drain cannot overwrite the original exit cause.
- **Coverage / verification:** regressions reproduce a late zero-content packet with mismatched auxiliary metadata, prove it commits after safe normalization, and separately prove a block-alignment mismatch is not canonicalized. Producer and consumer failures assert distinct status/stage/error evidence. Focused `ProcessLoopbackProtocolTest.*` passed twice. Required clean/default build `0.1.5065` passed x64/x86 products, the process helper/media binaries, test apps, both Vulkan layers, packaging, imports, PE hardening, and PDB checks; the canonical no-build gate passed the complete native and Python tool suites. Fresh late-process runtime confirmation remains pending.
- **Source anchors:** `mediaengine/process_loopback_protocol.h`, `mediaengine/process_loopback_capture.cpp`, `mediaengine/process_loopback_worker.cpp`, `tests/test_process_loopback_protocol.cpp`, and `llm-wiki/{current,multi-audio-capture,index,log/recent}.md`.

### 2026-07-18 - Honor GTA's first confirmed Reflex menu suspend during FSR-to-DLSS startup

- **Runtime proof / root cause:** supplied GTA session `installed/captureengine/logs/20260717_235919` enabled native FSR FG at `00:00:58.438`, disabled it at `00:01:24.020`, then completed the first confirmed PostSL DLSS-G render at `00:01:35.226`. GTA turned its own Reflex pacing OFF at `00:01:35.229` and requested `slDLSSGSetOptions(OFF)` four milliseconds later while the menu remained open. CE rejected the coordinated suspend solely because PostSL startup bookkeeping was still settling/stabilizing, kept DLSS-G logically and physically active without Reflex, and suppressed the OFF request. Reflex returned at `00:01:54.314`, matching the reported pink `REFLEX-NOT-DETECTED` tint disappearing when the menu closed. A later fully settled suspend at `00:02:11.876` followed the existing authoritative path, isolating the missing boundary to the first confirmed startup epoch.
- **Root fix / invariants:** a game-input Reflex active-to-inactive edge now arms the existing one-shot authoritative suspend intent as soon as the current DLSS-G epoch has produced its first confirmed PostSL render, even while CE's startup settling, stabilization, stale-warmup, or literal startup window remains active. The next real active-to-inactive GetState/SetOptions edge bypasses only the generic stale-OFF guard, so Streamline receives the coordinated disable and DLSS-G cannot remain active without Reflex. Reflex churn before PostSL confirmation remains protected; activation-pending and active-but-unconfirmed paths still cannot arm the intent. CE does not synthesize or force Reflex OFF, and manual-limiter forwarding remains separate. The transition log now records startup-window, settling, and stabilization state at the authoritative Reflex edge.
- **Coverage / verification:** the policy regression reproduces the exact post-FSR shape where generic startup protection remains true but the first-confirmed game-owned Reflex suspend wins. Focused `StreamlineRuntimePolicyTest.*` passed. Required incremental product build `0.1.5064` passed x64/x86 hooks, controller/media products, test apps, both Vulkan layers, packaging, shaders, and PE/import/PDB verification; the canonical no-build gate passed the complete native and Python tool suites. Installed-build pixel session `20260718_001240` retained `0.767` of the stable overlay bright-R/G signal through FSR->DLSS, with zero visibility interruptions, uncovered streaks, device-removal markers, `0x887A002B`, or `REFLEX-NOT-DETECTED`.
- **Source anchors:** `hook/apis/streamline_hook.cpp`, `hook/common/streamline_runtime_policy.h`, `tests/test_streamline_runtime_policy.cpp`, and `llm-wiki/{current,frame-generation/guardrails,overlay-fg-status,log/recent}.md`.

### 2026-07-17 - Make screen-capture triage phase-aware and source-limit-aware

- **Misclassification/root cause:** DXGI Duplication sessions `20260717_225122` (capture-sync 120) and `20260717_230639` (varying/VSync-limited source into 120 CFR) had exact exported A/V endpoints, no encoder/mux/pool pressure, no policy-added repeat clusters, and maximal runtime smoothness verdicts, but triage emitted `ce_visual_timeline_fault`. The analyzer fell back to unscoped hook gaps because external-capture perf rows do not publish `source_frame_index`, treated partial startup reservoirs and sub-two-frame source-limited delay variation as faults, called solitary 10.7/10.9 ms raw residual maxima timestamp-domain/A/V-delay faults despite bounded avg/p95, and printed shared implementation names as WGC even for DXGI. The rigid final-summary regex also stopped parsing once anti-freeze fields appeared between `StarvedEpisodes` and `longest`, so final duplicate/source-limit totals displayed as zero.
- **Fix/safety boundary:** perf CSV `capture_phase=2` is now the preferred live Present window, with the older source-frame heuristic and timestamped hook interval as fallbacks. WGC and DXGI receive backend-correct user-facing labels while internal `wgc_*` evidence keys remain compatible. DXGI `DupMissed` is delivery-gap context unless overload, ingress pressure, pool exhaustion, or another consumer-pressure proof exists. Residuals require persistent avg/p95 evidence, policy-coupled >10 ms late evidence, or an isolated >25 ms excursion. Exact source-limited coverage, plus a tightly bounded non-clustered accounting excess corroborated by the complete runtime verdict, can classify partial startup reservoirs and no-more-than-two-output-interval delay spread as context; policy-added repeats, safe-candidate misses, post-selection rejection, incomplete evidence, pressure, persistent residuals, and material repeat clusters remain strict.
- **Regression/runtime proof:** self-tests include a sanitized 4K120 DXGI varying-FPS shape with a 757 ms pre-live gap, 228.852 ms live gap, 85/85 lower-bound repeats, partial reservoir, 14.456 ms delay spread, and 10.710 ms raw maximum, plus the earlier five-repeat non-clustered accounting-excess boundary. Historical strict residual/policy/rubber-band fixtures remain green. Real-session replay now reports live maxima 278.180/228.852 ms, backend-correct contexts, and encoder/mux/audio/visual faults all zero; current summary totals parse as 112/16,117 with lower bound 107 and 85/12,728 with lower bound 85.
- **Verification:** `python -m py_compile tools\analyze_capture_av.py` and analyzer self-tests pass. Required incremental build `0.1.5063` passed x64/x86 products, packaging, shaders, both Vulkan layers, PE/import/PDB verification in 42.3 seconds. The no-build gate reused `0.1.5063` and passed the complete native suite plus every Python tool self-test in 8.1 seconds.
- **Source anchors:** `tools/analyze_capture_av.py`, `llm-wiki/{cfr-capture-sync,wgc-capture,regression-testing-and-logging,current,log/recent}.md`.

### 2026-07-17 - Remove duplicate default-build work and split summary/detail diagnostics

- **Time root cause/fix:** full default build `0.1.5059` spent about 17.5 minutes rebuilding the same non-instrumented pinned dependency/FFmpeg closure in the sanitizer child and then again in the final product pass. Default/verify now prepares that closure once in the parent, makes the sanitizer child use the verified `--skip-updates` path, and reuses the preparation for the final product. The child also skips duplicate Python tool self-tests because the parent executes the identical suite. Plain source freshness policy remains unchanged: a no-skip default still performs one fresh closure build.
- **CLI/mode correctness:** concise console presentation is now the default. Presentation-only `--concise`, `--verbose-commands`, `--jobs`, and log-path options no longer disable no-argument default-quality mode. `build.log` retains stage summaries, warnings, and bounded failure tails; every exact command plus successful/failed subprocess output goes to `build.details.log`. Source dependency, FFmpeg, sanitizer-child, and generic compiler output now use the same durable capture boundary, so successful stderr warnings cannot disappear and a failed command cannot flood the summary/agent context.
- **Lint/artifact correctness:** mixed/default lint runs after the current 221-entry `compile_commands.json` is written. Full clang-format/flake8/pyright/clang-tidy diagnostics are separate artifacts; the manifest records database hash, exact format file/batch counts, top checks/subsystems, total/stage durations, and explicit integration/signature/sanitizer/test-app coverage. Advisory findings use `step.lint=warning`, while standalone `--lint` remains the explicit failing gate. Lint flows automatically bootstrap missing Python tools with the active interpreter; manual preinstallation is not required.
- **Warning cleanup:** crash-dump directory storage now uses catchable function-local string construction instead of a potentially throwing namespace-scope initializer; overlay integer-to-float intent is explicit; the moved-from lease contract test has one narrow analyzer annotation. Focused clang-tidy became clean for all three files, and the full count fell exactly from 1,139 to 1,124 without global suppression.
- **Verification/performance:** 32 build-policy tests pass. Final required clean build `0.1.5062` completed zero-cache x64/x86 products, 30 test apps, six shaders, both Vulkan layers, packaging/import closure, PE hardening, and PDB checks in 172.352 seconds. Its summary log was 6.1 KB/81 lines while its complete 1.15 MB detail remained available. Incremental verify `0.1.5061` passed the x64 ASan/UBSan child (127.8 seconds), all native/Python tests, restored normal products (92.6 seconds), and current-database lint (61.3 seconds) in 285.6 seconds overall. Its summary log was 8.3 KB/108 lines; complete parent, sanitizer, and lint details remained in dedicated artifacts. Known debt is 23 formatted files across nine batches plus 1,124 non-fatal clang-tidy warnings; Python style and types are clean.
- **Source anchors:** `build.py`, `test_build_flags.py`, `common/crash_handler.cpp`, `hook/common/custom_overlay.cpp`, `tests/test_inject_frame_ring_lease.cpp`, `AGENTS.md`, and `llm-wiki/build.py.md`.

### 2026-07-17 - Keep lint advisory during builds and isolate its bootstrap environment

- **Observed failure/root cause:** plain `python build.py` build `0.1.5056` stopped after lint before any compilation because a missing blank line in the newly parallelized Python self-test helper made `flake8` return nonzero and `main()` treated every non-standalone lint finding as fatal. The intended product, native tests, and sanitizer cadence never ran. The repo also has known C++ formatting debt, so using the aggregate lint result as a hard build gate makes unrelated style state capable of preventing authoritative compilation and tests.
- **Policy/fix:** lint remains an explicit failing gate only for exactly `python build.py --lint`. Default, `--verify`, and mixed build/test invocations record the lint step and its details, log that findings are advisory, and continue to the real build/test gates. The concrete Python `E306` finding was fixed. Regression coverage locks standalone-versus-mixed invocation classification.
- **Link-cache interaction:** the optional Python-tool bootstrap had also copied its user-scripts `PATH` extension back into `os.environ`. Because fail-closed unit-test link manifests intentionally hash the link search environment, adding lint to an otherwise identical `--no-build --run-tests` invocation falsely invalidated the link. The user-scripts extension now remains child-local to bootstrap commands; a regression proves the process link environment is unchanged.
- **Verification:** all 28 focused build-policy tests pass. Required clean build `0.1.5058` passed every x64/x86 product, 30 test apps, six shaders, both Vulkan layers, packaging, FFmpeg closure, PDB, import, and PE-hardening checks in 156.9 seconds. The final mixed `--lint --no-build --run-tests` gate returned success: Python style/types passed, the nine known C++ formatting batches plus 1,139 clang-tidy warnings were recorded without aborting, the unchanged unit-test link manifest validated, and the complete native plus parallel Python self-test suite passed.
- **Source anchors:** `build.py`, `test_build_flags.py`, and `llm-wiki/build.py.md`.

### 2026-07-17 - Remove validation-only LTO and add fail-closed link caches

- **Measured bottleneck/root cause:** build `0.1.5051` took 106 seconds incrementally; its unit-test link alone consumed 44 seconds because 142 validation objects carried full LTO and the generated product identity forced a fresh link on every product build. The 213-second clean reference spent about 89 seconds compiling/linking tests. All observed gaps were active compiler/linker processes; normal builds had no artificial sleeps, and LLD already used all hardware threads.
- **Safe validation profile/cache:** product hook, CaptureEngine, and Vulkan flags are unchanged and retain full LTO. Unit tests retain `-O3`, CFG/CET, hardening, strict-FP exceptions, sanitizers where requested, and PDBs, but omit LTO and use a deterministic test-only build identity. Unit-test and split-object single-source test-app links now use fail-closed manifests covering compiler/linker binaries, commands/search environment, object and resolved-library contents, and EXE/PDB hashes. Clean builds always rebuild. Incremental cache hits still undergo normal final PE/PDB/hardening verification; `--no-build` recomputes unit-test freshness and refuses stale/corrupted/pre-manifest outputs. Test-app build failures are now fatal instead of allowing an older executable to mask them.
- **Additional safe reductions:** tests-only builds reuse the current product identity; unit-test compilation uses one bounded mixed-flag queue; test apps omit pointless single-TU LTO and honor the propagated `--jobs` policy across x86 environments; Vulkan FG SPIR-V compilation is content-cached while all six outputs remain unconditionally validated and its generated header is write-if-changed; Python tool self-tests run concurrently with deterministic failure replay; identical FFmpeg runtime DLLs remain in place after SHA-256 equality instead of being recopied, while initial and final import-closure checks remain.
- **Verification/performance:** 26 focused build-policy tests pass. The first 142-object non-LTO test rebuild linked in under one-second log resolution; the immediate unchanged tests-only rerun passed in 3.1 seconds versus the prior roughly 48 seconds. Required clean build `0.1.5052` passed every x64/x86 product, 30 test apps, six validated shaders, both Vulkan layers, packaging, FFmpeg closure, PDB, and PE-hardening checks in 150.8 seconds versus 213.2 seconds. Validated incremental build `0.1.5053` then passed the same product boundary in 38.1 seconds versus 106 seconds, with exact-build product relinks retained and all unit-test/test-app cache hits proven. After the final runtime-sync/logging refinements, clean build `0.1.5054` passed the same complete boundary and its full native plus parallel Python no-build test gate passed in 7.8 seconds.
- **Source anchors:** `build.py`, `tests/test_build_identity.cpp`, `test_build_flags.py`, `AGENTS.md`, and `llm-wiki/build.py.md`.
