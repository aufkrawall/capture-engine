# llm-wiki Log — Archive 2026-W29b

### 2026-07-17 - Attribute isolated DX12 ProcessFrame capture hitches without changing pacing

- **Runtime verdict:** capture-synced 120 fps DX12 inject session `20260717_190534` completed 14,601/14,601 contiguous CFR packets, exact packet-level A/V endpoints across one video and three audio streams, zero encoder/mux overload or backpressure, and no stale/capacity trims. Its reported gameplay event at about 58.49 seconds was one 18.320 ms source interval: CFR emitted exactly one repeated tick (119 unique + 1 repeat in that second), the phase-preserving limiter advanced expired source-grid deadlines, and the next second returned to 120/120. The output response was optimal for a missing source frame.
- **Unresolved upstream attribution:** the source event coincided with `DX12 DIAG: ProcessFrame (overlay) SLOW 14.4ms`, while surrounding ECL timing stayed normal. A periodic missing-API wrapper initialization/retry also ran at that moment, but the old whole-call marker could not distinguish external policy/lock work, capture publication, overlay record/submit work, a graphics-driver call inside those stages, or unrelated cross-thread activity. One temporal coincidence is insufficient grounds to change wrapper retry behavior or any capture/pacing policy.
- **Diagnostic improvement only:** every top-level public DX12 `DX12_ProcessFrameExternal` call now retains a low-overhead stage snapshot and emits it only for the existing >=5 ms rate-limited slow marker; `HandleDX12ProcessFrame` and legacy wrapper/DX11-forwarding callers share that entry point. The log separates external work, inner/unaccounted work, capture publication, screenshots, command-queue lock wait, and overlay acquire/record/submit/post-submit time. A lock-free generation/active snapshot around `InitializeWrapperHooks` proves activity that starts, completes, or spans the slow call without adding a render-thread lock. Partial/early-exit samples are explicitly marked invalid rather than reporting misleading zero-cost overlay work. No CFR, capture-sync limiter, variable-cadence fallback, WGC/DXGI/Vulkan path, overlay rendering policy, audio, encoder, mux, buffer, wait, or retry behavior changed.
- **Coverage / verification:** focused `DX12ProcessFrameDiagnosticsTest.*` policy tests passed at metadata `0.1.5049`, covering inactive/spanning/in-call concurrent activity and exact/clamped stage accounting. The first product link correctly exposed two legacy callers of the public one-argument entry point; restoring that compatibility entry and routing it through the same diagnostics fixed the link without bypassing coverage. The guarded resume completed the original failed `0.1.5050` transaction. After final fast-path review moved the second wrapper-activity snapshot behind the already-slow branch, the required incremental build `0.1.5051` passed x64/x86 hooks, media/controller products, tests, both Vulkan layers, test apps, packaging, shaders, and PE/import/PDB verification. Canonical no-build verification reused `0.1.5051` and passed the complete native suite, every Python tool self-test, seven FFmpeg patch tests, and all 23 build-policy tests.
- **Source anchors:** `hook/common/dx12_process_frame_diagnostics.h`, `hook/apis/dx12_hook.cpp`, `hook/common/dxgi_shared.cpp`, `hook/wrappers/wrapper_hooks.{h,cpp}`, `tests/test_dx12_process_frame_diagnostics.cpp`, and `llm-wiki/{debug-tools,index,log/recent}.md`.

### 2026-07-17 - Make incremental and failed-build retry verification authoritative

- **Waste identified:** the canonical agent product command force-recompiled every object, including on a retry after an earlier clean attempt had already compiled unaffected x64/x86 hook/media units. The subsequent `--no-build --run-tests` command also minted a new `build_version.h` despite compiling no binary. Initial end-to-end measurement then exposed that `build_version.h` was included by high-fanout `shared_defs.h`/`config.h`, so even a validated incremental build recompiled most of the tree merely to change identity. Routine command/per-file console output duplicated the durable `build.log` and consumed agent context without adding verification coverage.
- **Safe workflow:** ordinary source changes now use `--incremental --skip-updates --concise` for the final product gate; link, packaging, shader, PE/import/PDB, and other product validation remain unconditional. Clean/default compilation remains mandatory for build-script, compiler/linker/hardening, dependency/toolchain/FFmpeg, generated-build, and shared ABI/layout changes, cache investigations, or explicit requests. Focused iteration uses incremental tests-only plus a GoogleTest filter. A clean attempt followed by a successful guarded resume is one complete clean transaction.
- **Hardening / retry identity:** incremental signatures now include the compiler executable's SHA-256 in addition to source, flags, dependency files, and project-header contents, and signature-evaluation errors fail closed to recompilation. Generated version macros now live behind stable `common/build_identity.*` accessors; only `build_identity.cpp` includes `build_version.h`, while discovery defaults/validation, logs, manifests, and Vulkan layer naming retain exact-build semantics. `--resume` implies incremental validation, requires `--skip-updates`, and reuses an identity only when `latest_manifest.json` proves the immediately preceding top-level run failed and matches both the current `build.py` SHA-256 and `build_version.h`; successful/no-build/sanitizer-child predecessors, build-script changes, and `--no-build`/`--force-rebuild` combinations are refused. `--no-build` and sanitizer children reuse the current identity.
- **Context efficiency / object correctness / coverage:** `--concise` hides routine command and compile-progress lines from the console but preserves them in `build.log`, preserves all errors/stage summaries, and propagates into the sanitizer child. The separately created x86 Vulkan environment now inherits `FORCE_REBUILD`, closing a clean-gate inconsistency revealed when three layer objects were cached during a nominal clean run. Measurement also exposed that product and unit-test phases shared x64 object paths despite differing flags: they invalidated each other's cache on every build, and the later CaptureEngine link could consume common objects most recently compiled by the test phase. Ordinary tests and sanitizer tests now have distinct namespaces separate from product objects. Build-policy tests cover compiler replacement with older mtime, fail-closed signature evaluation, generated-version and object-variant isolation, guarded resume identity, no-build version reuse, build-control propagation, and console-only log suppression.
- **Measured verification:** clean/default build `0.1.5047` passed in 213.2 seconds with zero cache hits in every x64/x86 product, ordinary-test, and Vulkan group. The immediately following validated incremental build `0.1.5048` passed in 96.0 seconds (54.97% less wall time): exactly five identity objects recompiled and all 365 ordinary objects were signature-proven cache hits, while every product link, all 30 test-app builds, six embedded Vulkan shaders, packaging, import closure, PE hardening, and PDB checks still ran. The final no-build gate reused `0.1.5048` and passed the complete native suite, all Python tool self-tests, seven FFmpeg patch tests, and all 23 build-policy tests.
- **Source anchors:** `build.py`, `common/{build_identity,build_version,shared_defs,config}.*`, exact-build identity call sites, `test_build_flags.py`, `AGENTS.md`, and `llm-wiki/{build.py,current,index,log/recent}.md`.

### 2026-07-17 - Preserve capture-sync phase through hitches and align CFR source selection

- **Runtime verdict / root cause:** DX12 inject session `20260717_165151` materially improved on the previous recording: 13,545 live ticks, 354 source repeats (2.6%), zero stale/capacity trims, exact decoded audio/video endpoints, and no encoder/mux overload. The analyzer's prior 787 ms headline was pre-live initialization; the real live maximum was 243.062 ms. A stable 59-second segment had only 3/7,080 repeats, but after a 204.9 ms source hitch/reset a later stable 25-second segment had 107/3,000 repeats despite roughly 120 fps source delivery and sub-millisecond jitter. The quality counters paired 267 holds with an available candidate against 255 superseded frames. The basic capture-sync limiter rebased overdue cadence to `now + interval`, which could permanently put the source near the immutable CFR selector's half-frame boundary and create alternating hold/drop churn after a hitch.
- **Root fix / cross-backend scope:** hook-local capture-sync late recovery now advances the original rational deadline by whole slots until it has half an interval of headroom; general limiting retains existing now-relative behavior. Media independently observes inject timestamps or smoothed shared WGC/DXGI timestamps, confirms stable integer-multiple cadence and coherent phase, then shifts only the timestamp-nearest source-selection target by at most half a source interval. Stable underfeed and capture-sync multipliers are supported. Genuinely varying or phase-incoherent cadence releases/fails to acquire the lock and falls back to the unchanged nearest-timestamp selector. CFR packet PTS, audio clocks/correction, requested content delay, NVENC, muxing, and output scheduling are unchanged.
- **Diagnostics / coverage:** limiter stats report whole `phaseSkipped` slots; media logs acquisition/rephase/release transitions plus `[CFR PHASE LOCK SUMMARY]`. Session triage derives source gaps from the live `source_frame_index` span, excluding startup and frozen post-stop rows. Target-policy churn now requires consecutive non-overloaded, source-rate-matched windows with paired candidate-holds/superseded frames, so one real hitch cannot hide a stable bad segment and honest varying-FPS resampling is not mislabeled. Unit coverage exercises phase-preserving deadline advance, acquisition across modulo wrap, multiplier/stable-underfeed behavior, stable rephase, varying/incoherent fallback, and elimination of simulated half-frame boundary churn. Analyzer self-tests cover live/pre-live gap separation, matched-rate post-hitch churn, varying-FPS non-fault behavior, and phase-summary parsing.
- **Verification:** analyzer syntax/self-tests pass, and the supplied session now classifies a 243.062 ms live source gap plus matched-rate inject target-policy churn while keeping encoder/mux/audio clean. The first required build exposed and prompted correction of a final-summary backend snapshot that was out of scope. The clean rerun passed as build `0.1.5039`, including x64/x86 hooks, media/controller binaries, both Vulkan layers, test apps, packaging, shaders, PE/import/PDB verification, and compile commands. Metadata `0.1.5040` passed all 1,674 native tests in 127 suites plus every Python analyzer/build/FFmpeg self-test; the seven focused new phase/cadence tests also passed.
- **Source anchors:** `hook/common/{fps_limiter,fps_limiter_policy}.h`, `common/capture_pipeline_policy.h`, `captureengine/media_main.cpp`, `tools/analyze_capture_av.py`, `tests/{test_fps_limiter,test_capture_pipeline_policy}.cpp`, and `llm-wiki/{cfr-capture-sync,graphics-overrides-and-frame-pacing,current,log/recent}.md`.

### 2026-07-17 - Replace inject rate-credit pacing with timestamp-nearest CFR playout

- **Runtime proof / root cause:** supplied limiter-stable 120 Hz session `20260717_155409` recorded 14,239 live ticks with 882 source repeats and 783 stale trims despite zero pace drops, zero deferred/timer/drain repeats, healthy encoder/mux capacity, and stable 116.20-122.08 source telemetry. Inject protected the four-frame render-loopback content delay as if it were a physical fence tail, leaving no useful timestamp candidate at the normal six-frame depth; wall-age pruning then deleted the delayed candidate and manufactured a trim/repeat cycle. The session's first publication-latency spike was startup backlog, its app-audio endpoints and live latency were exact/healthy, and Steam was external-overlay context.
- **Root fix:** inject CFR now selects the monotonic source frame nearest `scheduledOutputQpc - avContentDelayQpc`, with half-frame lead tolerance. Only the physical fence tail is unselectable, startup waits for `reserve + delay + 1`, pruning is relative to committed source lineage, and the input-rate EMA is telemetry only. Catch-up uses the same timestamp target and encoder-health gate. This preserves zero added repeats at matched cadence, honest evenly spaced repeats below target, timestamp decimation above target, and stable behavior through source-rate transitions. WGC/DXGI's shared nearest helper now advances to a successor only when it is actually at least as close, eliminating tolerance-induced raw-timestamp holds without changing its compositor tolerance or CFR/audio grid.
- **Diagnostics only:** `[Inject CFR QUALITY SUMMARY]` exposes target selections, superseded frames, holds with/without candidates, hard-cap trims, and maximum residual. Session triage recognizes legacy stable-source stale-trim/repeat churn. Startup-only publication backlog, stop-only app-audio forced-drain latency, and external overlays are contexts rather than faults. Media now excludes stop-force-drain samples from live app-latency buckets/warnings and logs them separately; no audio correction, Vulkan pacing, NVENC option, CFR slot scheduler, or relay size changed.
- **Coverage / verification:** deterministic policy tests cover matched 120-to-120 cadence, below/above-target resampling, source-rate transitions, shallow inject fence-tail depth, WGC raw/smoothed nearest selection, startup history, and recovery gating. Analyzer self-tests cover the supplied churn signature plus phase-aware contention/audio/overlay classification. Required build `0.1.5036` passed x64/x86 products, media/hook/controller binaries, both Vulkan layers, packaging, PE/PDB/import checks, shaders, test apps, and compile commands. Metadata `0.1.5037` passed all 1,667 native tests in 127 suites and every Python tool self-test.
- **Source anchors:** `common/capture_pipeline_policy.h`, `captureengine/media_main.cpp`, `mediaengine/mediaengine.cpp`, `tools/analyze_capture_av.py`, `tests/test_capture_pipeline_policy.cpp`, and `llm-wiki/{cfr-capture-sync,log/recent}.md`.

### 2026-07-17 - Remove the four-texture Vulkan recording deadlock and root integration summary

- **Runtime proof / root cause:** ordinary-account session `20260717_150205` loaded the build-specific Vulkan layer, initialized and rendered the overlay, but layer publication stopped permanently at four frames and media finished with `input=0 output=0`. At 120 fps, the measured 31.242 ms loopback A/V content delay retained four frames and the warmup fence reserve retained one more; the encoder requires a sixth buffered frame before releasing the oldest lease. The Vulkan producer's four hard-coded textures were therefore all outstanding, so it could never publish that sixth frame. This was independent of registry/elevation after startup repair succeeded.
- **Root fix:** both layer-owned Vulkan resources and DXVK encoder-owned zero-copy imports use `SHARED_TEXTURE_SLOT_COUNT`/`ENCODER_TEXTURE_SLOT_COUNT` across all 16 existing lease slots. No A/V delay, fence correctness, capture feature, per-frame copy, wait, sleep, or polling behavior is weakened. The shared layout/name advances to ABI 36 because `EncoderTextures` expands.
- **Harness/logging fix:** the integration runner now requires final encoder output statistics and uses that count for the recording threshold; overlay `perf_metrics` cannot make a zero-frame recording pass. Its default `integration_results.json` moves into the latest corresponding session directory, while an explicit caller path remains supported. The prior root summary was relocated into `20260717_150205`.
- **Coverage / validation:** regression units lock the 16-slot Vulkan/encoder pool invariant, forbid reintroduced four-slot layer arrays, parse final recording output, and require the default integration-summary path to be session-scoped. Required full build `0.1.5032` passed x64/x86 products, both Vulkan layers, packaging, PE/PDB/import checks, and compile-command generation. Metadata `0.1.5033` passed the complete native suite and every Python self-test. Fresh ordinary-account 4K/120-fps Vulkan session `20260717_152124` explicitly logged `elevated=false`, published 16 textures, rendered the overlay, and recorded `input=538 output=534` with no skips/backpressure and no >12 ms overlay-timing spikes. Every log/result was session-local, the global log root had no files, exact registry teardown left no CE values in all four architecture/hive views, and no process remained.
- **Source anchors:** `common/shared_defs.h`, `hook/vulkan_layer/layer_capture.cpp`, `mediaengine/video_encoder.cpp`, `testapp/run_tests.py`, `test_build_flags.py`, `tests/{test_capture_base_shm,test_inject_capture_source,test_shared_runtime_state}.cpp`, and `llm-wiki/{current,process-ipc,overlay-rendering,log/recent}.md`.

### 2026-07-17 - Make stale Vulkan registration recovery prompt-free for ordinary launches

- **Runtime proof:** ordinary session `20260717_142845` wrote the current HKCU manifest registrations but had no Vulkan-layer startup log or overlay. Session `20260717_143113` worked when CE was already elevated. A controlled ordinary CE plus `vulkaninfo` run exposed the difference: stale HKCU values from `captureproject_overlay_repeat_validation` advertised the same `VK_LAYER_CE_overlay` identities, and the Vulkan loader explicitly discarded both current manifests as later duplicates. Elevated CE worked because its current HKLM values were found before the stale HKCU entries. Swapchain cleanup in the working session was normal layer lifecycle, not the cause.
- **Privilege policy:** default CE remains non-elevated and never requests UAC. Runtime repair enumerates CE-owned manifest basenames in both HKCU registry views on every controller startup. Only when the process is already elevated does it add both HKLM views; this covers unrestricted/UAC-off accounts and explicit elevated starts without making a prompt part of ordinary operation. `build.py` remains registry-side-effect-free, so build harnesses cannot mutate live Vulkan state. Machine registration stays ephemeral because leaving a user-writable build path active for elevated applications would be unsafe.
- **Stale-installation isolation:** x64/x86 manifests now carry build-specific layer names and implementation versions. Discovery ABI 36 publishes `BUILD_NUMBER`, and every current consumer validates it; a stale same-ABI Vulkan DLL therefore stays dormant if multiple CE layer DLLs load, while an inaccessible old manifest no longer has the duplicate identity needed to suppress the current build. Startup repair is basename-limited; normal teardown still deletes only the retained plan's exact paths once.
- **Coverage / validation:** focused tests cover split HKCU/HKLM architecture views, the elevation gate around HKLM repair, owned manifest matching, repair-before-registration ordering, exact teardown, build-specific manifest identities, and exact discovery build validation. Full build/test verification passed at `0.1.5032`/`0.1.5033`. Fresh session `20260717_152124` ran ordinary HKCU mode with `elevated=false`, loaded the build-specific layer, rendered and recorded successfully, then removed both exact HKCU values; the final four-view audit found no CE registration and no process remained.
- **Source anchors:** `build.py`, `common/{shared_defs,vulkan_layer_registration}.{h,cpp}`, `captureengine/{main,inject_main}.cpp`, `hook/vulkan_layer/{layer_main,layer_ipc}.cpp`, `tests/{test_vulkan_layer_registration,test_shared_runtime_state}.cpp`, `test_build_flags.py`, and `llm-wiki/{current,process-ipc,overlay-rendering,build.py,log/recent}.md`.

### 2026-07-17 - Isolate the shared ABI that corrupted Vulkan logs, telemetry, and inject recording

- **Exact runtime evidence:** in supplied session `installed/captureengine/logs/20260717_133007`, media initialized the Vulkan frame-ring cursor to decimal `1819637339` = `0x6C75765B`, whose little-endian bytes are `[vul`. The pre-ABI-33 log buffer began exactly where the current frame-ring write index lives, so an old-layout `[vulkan-layer.log] ...` record overwrote capture metadata. The current logger then viewed that record 24 bytes late at the new log-buffer offset, lost the filename prefix, and wrote the tail as root `logs/hook_debug.log`. The screenshot's max-core value `3933208576` = `0xEA700000` exactly matches the low 32 bits of the host-published 11,943 MB VRAM capacity at the old reader's shifted max-core offset. The other visible values follow the same one-field shift introduced by `publicationSequence`: sequence as CPU, CPU as RAM, RAM as GPU, and GPU as VRAM. This proves mixed compiled layouts; available evidence does not distinguish a stale incremental object from an already-running/injected stale binary, so both recurrence paths are closed.
- **Root fix:** shared ABI 34 uses versioned main/discovery names, exclusive main ownership, placement construction, magic-last publication, exact struct size, and a fingerprint over major/nested sizes and offsets. Every controller/service/media/hook/Vulkan consumer validates before payload access. Stale retained discovery mappings are reused only when their advertised owner is dead; shutdown withdraws discovery first. Build object signatures now include compiler-reported project-header content, so an older-mtime checkout cannot retain layout-skewed objects.
- **Logging, telemetry, and recording behavior:** the logger snapshots `DiscoveryInfo.logsPath` per session instead of deriving root `logs`, owns and sanitizes the filename prefix, and closes mappings on shutdown. Vulkan shared-log writers reserve the MPSC ring with CAS. Media rejects corrupt frame windows with a recording-integrity code, while Vulkan emits early/periodic publication and rate-limited full-ring diagnostics. Hook telemetry rejects non-finite/out-of-range/incoherent host publications, host max-core calculation rejects counter regression/overflow/idle underflow, and sensor summaries expose the complete publication.
- **Coverage / verification:** new/updated tests cover exact ABI publication and fingerprint rejection, versioned names, wrap-safe/corrupt frame windows, session-log path selection and filename containment, CPU counter discontinuities, and dependency content signatures with a forced older mtime. Required build `0.1.5028` completed x64/x86 hooks, captureengine/mediaengine, both Vulkan layers, test apps, packaging/import/PE/PDB verification, and compile commands. Canonical no-build metadata `0.1.5029` passed the full native suite and every Python tool self-test. Fresh game-process restart plus live Vulkan overlay/inject-recording validation remains required.
- **Source anchors:** `common/{shared_defs,invariants}.h`, `captureengine/{inject_main,ipc,logger_service,media_main,sensor_service,host_metrics}.cpp`, `hook/common/{ipc_client,system_metrics}.cpp`, `hook/vulkan_layer/{layer_ipc,layer_main}.cpp`, `build.py`, `test_build_flags.py`, `tests/{test_shared_runtime_state,test_logger_service_policy,test_host_metrics_policy}.cpp`, and `llm-wiki/{process-ipc,overlay-rendering,build.py,current,log/recent}.md`.

### 2026-07-17 - Disable the dormant malformed AV1 NVENC timecode path without touching capture timing

- **Verified defect / reachability:** pinned FFmpeg 8.1.2 exposes `av1_nvenc`'s `s12m_tc` boolean with default true. Its shared NVENC path passes `ff_alloc_timecode_sei()` output—written with HEVC time-code syntax—as AV1 timecode metadata. NVIDIA's June 2026 public diagnosis confirms that FFmpeg payload error plus a separate driver byte-alignment limitation in AV1 metadata OBU injection. The bundled Matroska and NVENC CFR/B-frame patches do not touch this path. Repository-wide inspection found no CaptureEngine creation or forwarding of `AV_FRAME_DATA_S12M_TIMECODE`, so the supplied captures did not exercise the defect; this is a real but currently dormant future-input hazard, not an explanation invented for observed overload/cadence/audio behavior.
- **Minimal safety fix:** AV1 NVENC now adds required `s12m_tc=0` after custom options and logs that last-applied invariant. An attempted custom enable is overridden with a high-signal warning. H.264/HEVC NVENC and AV1 AMF/QSV/MF are unchanged. Generic SEI, A53 captions, HDR metadata, pixels, rate control, CFR scheduling, capture backends, mux timing, and every audio path remain untouched. The former post-`avcodec_open2` `repeat_pps=1` block was removed because it could not affect the opened codec, `av1_nvenc` exposes no such option, and AV1 has no PPS; the valid cached-texture re-encode diagnostic remains.
- **Coverage / verification:** focused encoder policy/source tests passed 39/39 and lock backend scoping, custom-versus-required precedence, application before `avcodec_open2`, and absence of the bogus PPS option. All seven custom-FFmpeg patch tests passed, including strict application to the exact pinned source. Required build `0.1.5026` passed x64/x86 compilation, packaging/import closure, Vulkan shader validation, PE hardening, and PDB verification. Canonical no-build validation at metadata `0.1.5027` passed all 1,656 native tests in 126 suites plus every Python tool self-test.
- **Source anchors:** `mediaengine/video_encoder{,_options}.{h,cpp}`, `tests/test_video_encoder_{options,source}.cpp`, `patches/ffmpeg/README.md`, pinned FFmpeg `libavcodec/{nvenc.c,nvenc_av1.c,utils.c}`, and `llm-wiki/{current,nvenc-encoding,log/recent}.md`.

### 2026-07-17 - Keep inject audio neutral and close DXGI/WGC CFR packet holes under overload

- **Runtime evidence:** inject session `20260717_015533` exported 13,623 contiguous 120-fps packets and three mutually exact 5,449,200-sample AAC tracks, but every healthy audio source stopped at the `-500 ppm` correction limit because roughly 250 ms of downstream encoder/lookahead delay was counted as missing source-clock lead. Comparable DXGI session `20260717_021603` correctly stopped every source at `0 ppm`, had no audio underrun/overflow/trim or mux/pool backpressure, and applied the requested NVENC options (`rc-lookahead=20`, spatial/temporal AQ, `max_b_frames=2` / reorder depth 2, and `b_ref_mode=middle`). Its 979/13,811 repeats were all source/sync-protective with zero policy-added excess, but packet-only inspection found only 13,812 packets over a 13,847-tick endpoint: one 18-tick live PTS hole and seventeen one-tick stop-tail holes. All three decoded audio tracks were mutually exact at 5,538,800 samples and the same endpoint.
- **Root fixes:** inject CFR now excludes downstream video pipeline/lookahead latency from both source-clock drift expectation and ring-buffer target, keeping only the common base ingestion reservoir; WGC/DXGI retain measured screen-content lag targets and VFR/non-CFR retains historical pipeline targeting. Screen-grab wall timestamps continue to choose the best fresh/held visual state, but every successful CFR encoder submission assigns exactly the next contiguous PTS. This preserves smoothest achievable selection/catch-up behavior while replacing playback timestamp jumps with evenly paced CFR repeats when GPU/source capacity is genuinely insufficient.
- **Analyzer boundary:** session completed-capture validation now scans video packets and decodes audio only; it no longer invokes `ffprobe -show_frames` implicitly. Audio duration is compared with the packet endpoint span even when visual coverage is sparse, so a missing-video-tick failure is not mislabeled an audio failure. The unrelated Windows GLib missing-SystemApps-manifest warning is retained as environment evidence but no longer makes a successful decoder look dirty; actual codec stderr remains strict.
- **Coverage/verification:** focused tests lock inject/WGC/non-CFR audio-target separation, contiguous explicit screen-grab CFR PTS assignment, sparse packet endpoint accounting, and narrow decoder-stderr filtering. Required `python build.py --skip-updates` passed as build `0.1.5024`, including x64/x86 products, packaging, imports, PE hardening, PDBs, and compile commands. Canonical `python build.py --no-build --run-tests --skip-updates` at metadata `0.1.5025` passed the full native suite and all Python tool self-tests. Fresh inject plus DXGI/WGC runtime confirmation remains required.
- **Source anchors:** `mediaengine/{audio_sync_utils.h,mediaengine.cpp,video_encoder.cpp}`, `tools/analyze_capture_av.py`, `tests/{test_audio_sync_utils,test_video_encoder_source}.cpp`, and `llm-wiki/{cfr-capture-sync,multi-audio-capture,current}.md`.

### 2026-07-17 - Make inject CFR overload recovery converge without touching audio or screen capture

- **Runtime evidence:** Fortnite DX12/DirectFlip/VRR inject session `installed/captureengine/logs/20260717_005930` produced 14,693 contiguous 120-fps CFR video packets. All three AAC tracks fully decoded to 5,877,200 samples (`122.4416667 s`) with equal endpoints, one-microsecond container rounding only, and no capture loss, ring overflow, mixer/resampler fault, discontinuity, clipping, or decoder error. The brief subjective sound may therefore be game percussion; the old build has no worker-service-gap telemetry that can prove or disprove scheduling starvation at that instant. Video cadence was not optimal: after the overload, inject recovery remained active at about 35-37 ticks of debt, used 4,296 recovery repeats with no fresh catch-up, and sustained only roughly 60-76 unique frames/s despite 125-147 source FPS and roughly 2-5.5 ms encode time.
- **Root cause/fix:** a two-output catch-up cycle was treated as one service sample, so its raw duration falsely tripped the slow-service gate. The catch-up output also advanced the normal wake deadline, making a two-output cycle wait two ticks and mathematically unable to repay debt. Inject now divides cycle service by actual outputs, assigns every live output QPC from the immutable `liveStart + outputCount * interval` grid, and does not advance the normal wake for its one bounded recovery output. Pure encode and authoritative encoder/mux pressure still stop unsafe catch-up. Episode start/best/current debt, duration, fresh/repeat work, per-output service, and raw cycle time are logged; a recovery still active after five seconds is strict visual triage evidence. WGC/DXGI keep their prior scheduling path and wake advancement.
- **Audio boundary:** no capture, mix, resample, drift, codec, mux, timestamp, priority/MMCSS, or silence policy changed. The audio worker now records rate-limited >=25 ms service gaps after startup, including ring/fanout/epoch queue pressure, and emits a summary. The analyzer exposes this as diagnostic `audio_worker_scheduling_stall`; packet continuity counters and decoded audio remain authoritative, so a scheduling gap alone is not mislabeled an audio artifact.
- **Coverage/verification:** policy tests simulate debt convergence and lock immutable output QPC, per-output capacity accounting, and unchanged WGC/DXGI wake behavior. Source-contract tests cover inject-only integration, audio diagnostic non-interference, and analyzer classification. `python build.py --skip-updates` passed as build `0.1.5022`, including x64/x86 products, test apps, Vulkan layers, packaging, imports, PE hardening, and PDB checks. Final `python build.py --no-build --run-tests --skip-updates` at metadata `0.1.5023` passed all 1,649 native tests plus every Python tool self-test. Fresh equivalent real-game overload validation remains pending.
- **Source anchors:** `common/capture_pipeline_policy.h`, `captureengine/media_main.cpp`, `mediaengine/mediaengine.cpp`, `tools/analyze_capture_av.py`, `tests/test_{capture_pipeline_policy,capture_coordinator_source,audio_capture_source}.cpp`, and `llm-wiki/{cfr-capture-sync,multi-audio-capture,current}.md`.

### 2026-07-17 - Remove the first 30 clang-tidy findings safely

- **Measured reduction:** the full 217-entry inventory fell from 1,159 to 1,129 raw findings and from 1,126 to 1,096 unique file/line/check locations. `unchecked-optional-access` fell 26 -> 0, `raw-memory-call-on-non-trivial-type` 1 -> 0, `signed-char-misuse` 4 -> 2, and `use-after-move` 10 -> 9; no check was disabled or suppressed.
- **Fixes:** encoder-option tests retain their fatal presence assertions but use analyzer-safe `value_or` reads; shared-memory tests value-initialize the atomic-rich layout instead of invoking undefined `memset`; font lookup normalizes high-bit bytes before indexing and has regression coverage; audio epoch logging snapshots the capture epoch before moving the packet into its deferred queue.
- **Verification:** the four targeted clang-tidy checks pass on their touched translation units. Cached `python build.py --skip-updates` build `0.1.5018` passed compilation, packaging, PE/import verification, and compile-command generation while reusing FFmpeg. `python build.py --no-build --run-tests --skip-updates` then passed the native and Python suites.

### 2026-07-17 - Make cached unit-test failures actionable and classify clang-tidy debt

- **Test result:** the prior plain-build `unit_tests.exe` exit code `1` remains non-reproducible; two cached no-build runs and the cached CE/test build passed all 1,645 native tests. The build harness previously preserved only the return code, so no failing GoogleTest name survived in the verification bundle.
- **Fix:** `run_tests()` now captures stdout/stderr, writes `unit_tests_failure.log` on failure, and logs the bounded diagnostic tail. A focused regression uses a mocked failed process to verify the artifact without polluting the real build log.
- **clang-tidy inventory:** 1,159 findings remain inside the project header boundary across 217 compile entries: 380 correctness/design reviews, 762 conversion/ABI/intent reviews, and 17 performance reviews. Source-area counts are hook 541, tests 269, testapp 215, mediaengine 82, captureengine 43, and common 9. External/generated/installed/FFmpeg trees remain excluded.
- **Verification:** `python build.py --run-tests --skip-updates` completed CE/test compilation, PE verification, 1,645 native tests, and all Python tool self-tests. The initial wrapper-timeout run produced Python exit code 120 from a closed stdout pipe; the long-timeout rerun passed and is the authoritative result. The final cached `python build.py --skip-updates` build `0.1.5017` also passed CE/test compilation, PE verification, and compile-command generation; FFmpeg was intentionally reused through the cached dependency closure.

### 2026-07-16 - Make plain python build.py rebuild the complete FFmpeg source closure

- **Requirement:** plain `python build.py` must freshly build every Windows FFmpeg runtime DLL from source, including the pinned dependency closure; `--skip-updates` remains the explicit reuse path.
- **Fix:** `SourceDependencyBuilder.ensure(force_rebuild=True)` now bypasses the verified-prefix cache and resets/rebuilds every manifest dependency. The plain build path passes that flag and initializes FFmpeg's rebuild decision as forced, while still updating the FFmpeg source repository before building. `--skip-updates` retains cache reuse and continues to rebuild missing, stale, or configuration-mismatched outputs.
- **Runtime/package boundary:** the final synchronization still copies only the newly built FFmpeg DLLs and private-prefix dependency DLLs into `installed/captureengine/ffmpeg`, removes stale bundle DLLs, validates the PE import closure, and never sources shipped runtime dependencies from the ambient MSYS2 bin directory.
- **Regression coverage:** dependency-cache tests cover forced rebuild versus verified-prefix reuse; build-policy coverage asserts the plain-build force path, safe Windows SDK include ordering, and the clang-tidy header boundary; the Python dependency tests also wrap the pre-existing overlong assertion that caused default lint to fail. A sanitizer run reached 1,644/1,645 tests and exposed a formatting-sensitive crash-handler source assertion, which now checks the registration call structure rather than one exact line layout.
- **Source anchors:** `build.py`, `ffmpeg_dependencies.py`, `test_build_flags.py`, `test_ffmpeg_dependencies.py`, and `llm-wiki/build.py.md`.
- **Verification status:** exact `python build.py` build `0.1.5009` rebuilt the complete dependency closure and custom FFmpeg from source; its sanitizer child reused the parent build number and passed. The first product unit-test launch returned nonzero, but the supported no-build rerun passed all 1,645 native tests and Python self-tests. The subsequent `python build.py --skip-updates` CE/test build `0.1.5012` passed compilation, test-app compilation, PE verification, and compile-command generation with no version-mismatch or Vulkan test-app errors. The run reports 1,159 non-fatal project clang-tidy findings; they remain classified warning debt rather than suppressed, and need separate category-by-category remediation.
- **Follow-up:** investigate why the first full-build unit-test launch was transiently nonzero even though the immediate no-build rerun passed all 1,645 tests; do not treat a single passing rerun as proof that any intermittent test issue is resolved.

### 2026-07-16 - Restore truthful legacy-overlay GPU, VRAM, and RAM telemetry

- **Root cause:** old graphics APIs did not prevent out-of-process sensor polling. The host ignored the selected game PID and required a nonzero hook LUID before initializing/filtering GPU counters; DirectDraw's overlay-only D3D9Ex helper did not publish its adapter; the consumer conflated numeric zero with unavailable data; and RAM copying was incorrectly gated on CPU load being greater than zero.
- **Fix:** hook LUIDs now carry publisher-PID provenance. Exact same-process LUIDs win; otherwise the host parses the target PID's Windows `GPU Engine` instances, selects the highest non-video-load adapter, and retains stable process evidence across zero-load ties or temporary gaps. GPU usage, VRAM usage, and VRAM capacity have independent validity bits plus coherent publication sequencing/source metadata, so real zeros remain visible and stale values clear on process/adapter replacement. RAM updates independently of CPU. DirectDraw publishes its D3D9Ex helper LUID without requiring recording.
- **Runtime/performance boundary:** no-recording 4K/150% DirectDraw7 runs for both x64 and x86 visibly reported `RTX 5070`, numeric GPU load, about 1.76/11.66 GB VRAM, and about 13/31.93 GB RAM. Each sensor log transitioned from target-PID inference (`0xBAB1`) to the same-process hook LUID. Recording was deliberately off because legacy capture is not zero-copy. DirectDraw/DX6/DX7 still pay the existing full-surface lock/copy/helper-Present cost; replacing that architecture remains outside this targeted patch.
- **Verification:** 14 focused telemetry/provenance tests pass, including conservative multi-GPU zero/tie handling. Required build `0.1.4989` passed x64/x86 products, test apps, Vulkan layers, packaging/import closure, PE hardening, and PDB checks. The no-build run passed 1,644 native tests when excluding only the shared-system-cursor test; `IDC_ARROW` was temporarily transparent while the ChatGPT Windows-control session was active, consistent with cursor substitution, so that environment-dependent failure is unrelated to this change.
- **Source anchors:** `captureengine/{host_metrics,host_metrics_policy,sensor_service,ipc}.*`, `common/{capture_base,shared_defs}.h`, `hook/apis/ddraw_hook.cpp`, `hook/common/{hook_common,overlay_adapter,system_metrics}.*`, `hook/vulkan_layer/layer_ipc.cpp`, `tests/test_host_metrics_policy.cpp`, and `llm-wiki/overlay-rendering.md`.

### 2026-07-16 - Harden Vulkan presentation/Reflex behavior and remove the DLSS suspension stall

- **DLSS suspension root cause/fix:** repeated key `2` previously called `slDLSSGSetOptions(eOff)` without retaining plugin resources. The call synchronously flushed/destroyed DLSS-G worker state, causing a roughly 2.5-second one-off stall and apparently bad suspended performance. The Vulkan app now sets `eRetainResourcesWhenOff` for its retained-proxy/context contract, logs option-call duration, and warns above 50 ms. Focused active -> suspended -> resumed validation measured 0.001 ms for each option call; suspended real-frame throughput stabilized around 144 FPS and generated counts stopped/resumed correctly.
- **Present path:** VSync-off now prefers `IMMEDIATE` before mailbox, matching the DX12 tearing-capable game route and leaving forced driver VSync/VRR in control. Opt-in `[Vulkan] async_present=1` / `--vk-async-present` reserves a distinct graphics+compute+present queue index in the game family, routes Native/Streamline/FFX replacement Present through it, and supplies it as FidelityFX's game queue while leaving FFX's internal async/present/acquire queues distinct. Render-complete semaphores are swapchain-image-owned so a separate presentation queue cannot still own a binary wait when a frame slot is reused.
- **Reflex contract/diagnostics:** Reflex is explicitly configured even for initial OFF, `slReflexSleep` runs at frame start before fence/acquire waits whenever feature functions are loaded, and `slReflexGetState`, requirement flags, DLSS-G VSync support, sleep failures, CPU-stage timing, and final counts are logged. `frameLimitUs` remains zero and build policy rejects an explicit cap. A focused borderless application-VSync-off run with driver VSync forced showed Native at about 144 real FPS and DLSS FG at about 72 real + 72 generated, not the expected roughly 138.5 output headroom. Focus/DirectFlip/profile application/Reflex calls were proven; public Streamline 2.11.1 guidance and the plugin log still call VSync-with-FG Vulkan-unsupported even though `bIsVsyncSupportAvailable` returned true, so no game-side limiter was added to mask this plugin/driver boundary.
- **Runtime evidence:** an all-state PresentMon capture recorded 5,591/5,594 Presents as independent flip (three compositor startup Presents). A focused debug-utils separate-queue run exercised OFF, DLSS active/suspended/resumed, FSR active/suspended/resumed, and OFF on queue `0:4`; it exited `frames=29001 presented=29001 generated=3969 transitions=3 failures=0 validationErrors=0 deviceLost=0`, with zero Reflex sleep failures and no lingering test process. The animated cube/status/HP HUD remained visually correct throughout.
- **Display boundary/source anchors:** the current path is SDR 8-bit sRGB; `dlss_hdr` is SR input semantics, not HDR10 presentation. HDR needs a separate RGB10/BT.2100/PQ path and fresh vendor validation. Source anchors are `testapp/vulkan_fg_policy.h`, `testapp/vulkan_fg_switch_{common,device,fidelityfx,renderer,resources,streamline,test,wsi}.*`, `tests/test_vulkan_fg_{policy,build_policy}.cpp`, and `llm-wiki/vulkan-fg-switch-test.md`.
- **Final verification:** the required clean build passed shader compilation/validation, x64 packaging, PDB, and PE-hardening checks. The first ordered full-suite run exposed a pre-existing GDI batching bug in cursor extraction: `DrawIconEx` could remain queued while its DIB section was read directly, yielding an all-transparent cursor. `mediaengine/cursor_renderer.cpp` now calls `GdiFlush` at that CPU/GDI boundary; the existing ordered regression then passed. Final no-build validation passed all 1,643 native tests in 126 suites plus all Python tool self-tests.

### 2026-07-16 - Polish shared overlay rendering and remove redundant legacy work

- **Appearance/layout:** replaced independent live row decisions with a single immutable snapshot/mask used by drawing, panel height, and graph placement. FG OFF/DLSS/FSR, 2x/4x, recording, notification, config, and reserved-space changes rebuild immediately; width reservations prevent digit/label growth from clipping. The graph keeps 180 raw samples but now uses exact endpoints, bounded miter/bevel geometry, and a one-physical-pixel AA fringe. GDI glyphs use actual ink extents, isolated two-texel gutters, clipped rasterization, `GdiFlush`, and one snapped text/shadow origin. The existing visual identity and x86 DX12 solid-span text behavior remain unchanged.
- **Legacy hot paths:** new draw-data-change notification lets DX8/DX9/DX10 cache unchanged VB/IB contents across cached Presents while failed uploads remain dirty and cannot draw stale geometry. DX8/DX9 retain one lazily created full state block but still Capture/Apply every draw and recreate on failure/reset; DX10 constants update only for viewport/HDR/paper-white changes. Valid GL 2.1 fixed-function contexts prefer client arrays/`glDrawElements`, retain immediate fallback, restore saved GL sentinels, re-probe per context, and no longer drain errors or emit success heartbeats per Present.
- **Telemetry correction:** fake 11.66 GB VRAM and 31.93 GB RAM capacities are removed. Used memory remains displayable without a total; RAM total is obtained by one lazy `GlobalMemoryStatusEx` query, while genuinely unavailable GPU/VRAM values render `--`.
- **Performance/root-cause boundary:** probes intentionally used launch-only injection, not recording, because old APIs lack the zero-copy capture route. Valid DX9/DX9Ex samples were low-microsecond; the GL test received a 4.6 compatibility context and therefore did not runtime-cover the 2.1 array path. DirectDraw7's short 4K CPU probe rose from about 45.7% of one core without overlay to 64.7-68.6% with it, with no material graph/no-graph difference: its required full-surface copy/helper-Present dominates. DX6/DX7/DX8 test apps respectively fell back after `0x80004002`, `0x88760082`, and `0x8876086C`, explaining their poor pacing; the old zero GPU/VRAM readings were later traced to the adapter-identity/validity bug documented above.
- **Coverage:** 23 focused tests pass for glyph padding, graph geometry, shadow snapping, cache notification/upload failure, state-block/constant/GL policy, atomic dynamic rows, and truthful memory policy. A live 4K DX9 screenshot rendered RAM consumption as `11.48 GB`, not `--`. Required build `0.1.4956` passed x64/x86 products, test apps, Vulkan layers, packaging/import closure, PE/PDB checks, and the no-build gate passed all 1,618 native tests in 123 suites plus every Python self-test. Source anchors and remaining native legacy-runtime stale-risk are in `overlay-rendering.md`.

### 2026-07-16 - Add and runtime-harden the Vulkan DLSS/FSR frame-generation switch app

- **App/build boundary:** added x64-only `vulkan_fg_switch_test.exe` without replacing either existing test app. DX12 remains on FidelityFX SDK 2.2.0; Vulkan downloads/verifies a separate SDK 1.1.4 source/minimal package, installs the official signed Vulkan provider/license, and compiles/validates/embeds six GLSL shaders. The shared API-neutral configuration preserves controls, automatic timings, SR quality and FSR4-to-3.1.4 fallback. Runtime integration is opt-in as `--api vulkan_fg`.
- **Rendering/diagnostics:** implemented a three-frames-in-flight Vulkan renderer with explicit layouts/barriers and realistic RGBA16F scene/HUDless, RG16F motion, D32 depth, R8 masks, RGBA8 UI, temporal history, and matching 8-bit dithered presentation resources. The visual scene mirrors the DX12 sky/checker/cube/status/animated HP HUD. High-signal logs cover support/configured/effective DLSS SR/FG, Reflex and FSR SR/FG, queue/device/provider topology, immutable owner/WSI routing, transition epochs/stages, suspension/rollback, named errors, validation/device-fault evidence, pacing and final counts.
- **Cross-owner root fixes:** FidelityFX 1.1.4 consumes the incoming raw swapchain and its replacement path cannot coexist with another live window swapchain. Native/Streamline -> FFX therefore creates a valid loader bridge, destroys the old proxy through its immutable owner table so Streamline observes the event, and lets FFX consume the bridge. FFX -> Native/Streamline disables FG, presents/drains, retires only the FFX proxy, retains contexts for rollback, then creates the target. FFX same-owner resize uses the same retained-context proxy retirement. This fixes FSR -> DLSS `VK_ERROR_NATIVE_WINDOW_IN_USE_KHR` without delays, polling, extra copies, or permanent feature disablement.
- **DLSS visual root fix:** the first borderless generated-frame captures exposed ghosting only on the moving HP HUD. The Vulkan Streamline path tagged HUDless/UI resources but omitted the acquired backbuffer and explicit render/display extents supplied by the DX12 path. Adding complete Vulkan metadata for `kBufferTypeBackbuffer` in present layout and extents for every tag made consecutive generated-frame HUD captures crisp while Streamline constants/tags/evaluation remained `eOk`.
- **Injected FFX root fix:** fresh dump `20260716_163627/...pid5504...dmp` bucketed the OFF -> FSR crash in `Hooked_ffxConfigure`, which passed Vulkan's `VkSwapchainKHR` to `DX12_TryCacheRuntimeOwnedCallbackHDRStateFromSwapchain` and faulted at `IDXGISwapChain::GetDesc`. FFX create descriptors now classify backend per context (including SDK 1.1.4 and modern Vulkan tags). Positively identified Vulkan configure/destroy calls forward without DXGI/DX12 HDR, callback-bridge, queue, or global-state processing, while retaining rate-limited diagnostics; unknown/legacy contexts preserve DX12 behavior. The opt-in runner now searches `vulkan_layer.log` as well as `hook_debug.log` for `vulkan_fg` activity.
- **Runtime validation:** foreground/topmost borderless hotkeys passed OFF, DLSS active/suspended/resumed, FSR active/suspended/resumed, FSR -> DLSS, DLSS -> OFF, OFF -> FSR, and FSR -> OFF, with graceful exits and no remaining process. Windowed <-> borderless transactional recreation passed twice under FSR and twice under DLSS. A 24-second automatic OFF -> FSR -> DLSS -> FSR run exercised repeated suspension/resume plus FFX internal/application callback routes and ended `frames=2833 generated=867 transitions=3 failures=0 validationErrors=0 deviceLost=0`. Fresh CaptureEngine-injected 4K hotkey captures retained the Vulkan overlay/FPS counter through Native -> Streamline -> FidelityFX -> Native; the app exited `frames=640 presented=640 generated=262 transitions=3 failures=0 validationErrors=0 deviceLost=0`, with no fresh dump or remaining process. Focused Vulkan tests passed 29/29 and combined FFX/Vulkan tests passed 43/43; build `0.1.4949` passed compilation, shader validation, packaging and PE/PDB checks. Final no-build validation passed 1,617 native tests in 123 suites plus all Python tool self-tests; AMD runtime remains the separate hardware gate.
- **Source anchors:** `build.py`, `hook/apis/ffx_hook.cpp`, `hook/common/ffx_api_parsing.h`, `testapp/{fg_switch_config,vulkan_fg_policy,vulkan_fg_switch_*}`, `testapp/shaders/vulkan_fg_*`, `tests/{test_ffx_api_parsing,test_vulkan_fg_policy,test_vulkan_fg_build_policy}.cpp`, and `llm-wiki/{vulkan-fg-switch-test,build.py,current,log/recent}.md`.

### 2026-07-16 - Remove registry repair from builds and exact-scope Vulkan registration lifetime

- **Build boundary:** `build.py` previously called a Python `winreg` cleanup after each x64/x86 layer link. Every build enumerated HKCU and both HKLM views, deleting values by CE manifest basename; non-elevated HKLM attempts produced permission noise, while elevated builds could remove a live registration belonging to another installation or controller session. The helper and call site are removed. Builds now stop after writing the layer DLL and portable relative-path manifest and have no registry API dependency or side effect.
- **Runtime boundary:** normal registration no longer enumerates, repairs, or basename-matches ambient registry values. `ApplyRegistrationPlan` writes only `plan.installTargets` on startup and deletes only those targets' exact `manifestPath` values on teardown. CaptureEngine retains the original startup plan instead of rebuilding ownership at shutdown, so deleted/moved artifacts cannot broaden or lose the teardown scope. The existing controller singleton is the concurrency boundary. Console-handler and normal exit use one blocking `call_once`; normal exit completes unregistration before clearing the handler's non-owning pointer, preventing duplicate work or plan lifetime races.
- **Repair boundary:** opposite hives/views, other CE installations, the legacy manifest name, and missing/stale entries are untouched by ordinary startup and shutdown. A same-path residue from a prior crash is naturally overwritten by the next same-mode registration and removed at its clean exit. Any broader legacy repair remains deliberately absent until it has an explicit user-facing repair/uninstall workflow and stronger ownership proof.
- **Validation:** `python -m unittest test_build_flags.py` passed all five policies. Focused `VulkanLayerRegistration*` validation passed five native tests. Canonical no-build validation passed all 1,597 native tests in 118 suites and every Python self-test. Required clean build `0.1.4939` completed x64/x86 products, test apps, Vulkan layers, packaging/import closure, PE hardening, and PDB verification; both manifests were generated with no registry cleanup or permission diagnostic.
- **Source anchors:** `build.py`, `captureengine/main.cpp`, `common/vulkan_layer_registration.{h,cpp}`, `test_build_flags.py`, `tests/test_vulkan_layer_registration.cpp`, and `llm-wiki/{build.py,current,log/recent}.md`.

### 2026-07-16 - Prewarm a repeated pure-DLSS proxy before its FG-off first Present

- **Exact gap / root cause:** supplied session `installed/captureengine/logs/20260716_135326` ran `OFF -> DLSS -> OFF -> DLSS` and exposed one frame only on the second activation. At `13:53:39.924` fresh Streamline proxy creation retired the swapchain-scoped backend. Its FG-off passthrough Present reached CE at `13:53:39.944`, before `slDLSSGSetOptions(ON)`, and line 8184 logged uncovered `present=473`, `gate=overlay-backend-uninitialized`, normal route, `ProcessFrameExternal`. SetOptions ON followed in the same millisecond; the first exact PostSL draw restored coverage at `13:53:40.111`, reporting one missed Present over 172 ms. The prior prewarm policy required FSR history, so a repeated pure-DLSS route discarded a ready retiring overlay despite already having proven a healthy PostSL topology.
- **Root fix / safety boundary:** the first real PostSL render+submit that finishes with a healthy device latches durable process-lifetime route proof. On a later fresh authoritative Streamline proxy, `hadFSR || priorHealthyPostSL` may authorize the existing creation-window prewarm only with live retiring overlay state, runtime ownership, exact queue association, inactive DLSS, and a DX12 swapchain. First-ever pure-DLSS startup still lacks both proofs and retains its strict cold-start guards. The existing exact one-Present proof preserves the complete ready RTV/sync/command backend across the proxy's pre-SetOptions passthrough and is consumed only by that identity; invalid ownership, FSR/native takeover, device failure, invalidation, or shutdown stays conservative. Shutdown clears the history. The path adds no copy, queue, submit, wait, delay, polling, title/game branch, FG-status change, Reflex/manual-limiter mutation, or steady-state work.
- **Regression oracle / validation:** `tools/run_dx12_fg_overlay_transition.py --sequence=repeat-pure-dlss` drives `OFF -> DLSS -> OFF -> DLSS`, validates both activation seams, requires the prewarm and first-Present-preservation breadcrumbs, and keeps all uncovered/visibility/device/Reflex markers at strict zero. Corrected runs `20260716_141024` and `141051` passed, as did mixed eight-edge `141107`, directed FSR->DLSS `141127`, and OFF->DLSS->OFF->FSR `141145`. Exact integrated clean-build session `20260716_142816` passed in 12.7 seconds with both required proofs once and every strict marker zero; cleanup found no CE, switch-app, or debugger process. Required isolated clean build `0.1.4` passed x64/x86 hooks and apps, Vulkan layers, packaging/import closure, PE hardening, and PDB checks, followed by all 1,571 native tests in 114 suites and every Python test group.
- **Source anchors:** `hook/apis/dx12_hook.cpp`, `hook/common/dx12_overlay_policy.h`, `tests/test_dxgi_shared.cpp`, `tools/run_dx12_fg_overlay_transition.py`, and `llm-wiki/{current.md,frame-generation/guardrails.md,log/recent.md}`.

### 2026-07-16 - Audit the x86 DX12 solid-glyph workaround and qualify historical root-cause claims

- **Current conclusion:** the accepted x86 DX12 solid-glyph path remains the best-supported production fix. Corrected uncapped rendering reproduced the hang without Alt+Tab, and recorded DRED isolation completed solid geometry before hanging on the first font-resource text draw across descriptor-free and Texture2D variants. Focus transitions amplified the failure but were not required. NVIDIA x86/WoW64 driver behavior is the leading explanation, not a vendor-confirmed root cause; retained evidence lacks a standalone non-injected reproducer and cross-vendor/driver matrix.
- **Annotation corrections:** the descriptor-free shader no longer claims that avoiding `SetDescriptorHeaps` fixed the stall; the legacy focus-edge predicate is renamed as telemetry and explicitly must not gate overlay rendering; the upload-ring test now documents fencing as an independent required hazard fix rather than the font-hang cause. Historical coexistence and DRED guidance distinguish observed Alt+Tab manifestations from the later resource-read trigger isolation.
- **Regression coverage:** the CPU/GDI glyph-span test now reconstructs quantized coverage for every printable ASCII glyph, checks span bounds/alpha buckets, and rejects overlapping spans. Runtime policy is unchanged: x86 DX12 keeps native direct rendering, skips font-resource text reads, and must not hide on mere focus loss.
- **Validation:** the required `python build.py --skip-updates` completed as build `0.1.4935`, including x64/x86 hooks, test applications, Vulkan layers, packaging/import closure, PE hardening, and PDB checks. Six focused CPU/unit tests passed for printable-ASCII span reconstruction, solid-only renderer commands, x86 backend/text policy, focus telemetry, and upload-slot fencing. No graphics test app was launched. Optional HKLM Vulkan-layer cleanup was skipped for insufficient permission; no required build step failed. Historical runtime log directories referenced by the June handoff are not present in the current tree.
- **Source anchors:** `hook/common/dx12_overlay_policy.h`, `hook/apis/dx12_hook.cpp`, `hook/common/custom_{font,overlay}.cpp`, `tools/compile_shaders.py`, `tests/test_{overlay_system,dxgi_shared}.cpp`, `llm-wiki/{handoff-dx12-32bit-crash,dx12-overlay-third-party-coexistence,debug-tools}.md`.

### 2026-07-16 - Preserve an exact prewarmed PostSL backend through its first proxy Present

- **User-visible failure / corrected oracle:** during isolated installed-build run `20260716_132719`, the user saw the overlay disappear even though the desktop pixel sampler reported PASS. The runtime log proves a real one-Present interruption: `[OVERLAY VISIBILITY] INTERRUPTED/UNPROVEN` at `13:27:29.086`, restored 188 ms later, with `gate=overlay-backend-uninitialized`. Pixel samples alone are therefore insufficient. The automated switch harness now fails on any visibility interruption, uncovered-streak start, device-removal signature, or `REFLEX-NOT-DETECTED`, in addition to its bright-R/G output check.
- **Root cause / ownership ordering:** on the second OFF->DLSS handoff after an FSR phase, creation had already captured the exact Streamline proxy queue and completed PostSL RTV/sync prewarm. The proxy's first Present arrived before `slDLSSGSetOptions(ON)`. Generic swapchain-change handling saw DLSS still inactive, discarded that exact ready backend, and armed the transition cooldown; SetOptions followed one millisecond later. This was a make-before-break ownership bug, not an inadequate delay or pixel threshold.
- **Root fix:** successful creation-time prewarm publishes a one-Present, identity-only proof for that exact proxy lifetime. Its first matching Present consumes the proof and preserves the complete backend only while overlay/sync/RTV/command-list state is ready, the exact captured queue still owns the swapchain, Streamline runtime ownership remains live, FSR/native presentation is absent, and the device is healthy. FSR enable, Streamline OFF, invalidation, shutdown, a newer handoff, or any failed guard clears/rejects the proof. The same creation proof also defeats raw COM-pointer ABA equality. No queue, copy, allocation, submit, wait, sleep, polling, title branch, FG-status mutation, or Reflex/manual-limiter change was added.
- **Validation:** strict-log runs `20260716_134205` and `20260716_134357` each completed the eight-edge `OFF -> FSR -> OFF -> DLSS -> OFF -> FSR -> OFF -> DLSS -> OFF` cycle in 11.7/14.2 seconds. Each armed and preserved both exact prewarmed PostSL handoffs and both authoritative DLSS-off native returns, with zero uncovered Presents, visibility interruptions, device failures, or Reflex mismatch markers. Directed `20260716_134438` (FSR->DLSS) and `20260716_134454` (OFF->DLSS->OFF->FSR) also passed the same strict gates. The isolated task-only clean build `0.1.5` passed x64/x86 hooks and test apps, Vulkan layers, packaging/import closure, PE hardening, and PDB verification. Canonical task-only metadata `0.1.4` passed all 1,555 native tests in 112 suites plus all Python tool self-tests.
- **Source anchors:** `hook/apis/dx12_hook.cpp`, `hook/common/dx12_overlay_policy.h`, `tests/test_dxgi_shared.cpp`, `tools/run_dx12_fg_overlay_transition.py`, and `llm-wiki/{current.md,frame-generation/guardrails.md,log/recent.md}`.

### 2026-07-16 - Treat an authoritative DLSS-off swapchain creation as a new lifetime even after pointer ABA reuse

- **Observed interruption / root cause:** supplied long-switch session `installed/captureengine/logs/20260716_125557` contains a real `[OVERLAY VISIBILITY] INTERRUPTED/UNPROVEN` span: 61 uncovered Presents over 453 ms, gated by `overlay-backend-uninitialized`. The exact sequence was `OFF -> FSR -> OFF -> DLSS -> OFF -> FSR -> OFF -> DLSS -> OFF`. Both authoritative DLSS-off native returns reused swapchain address `0000027B56C5FD30`; the first return differed from `g_LastSwapChain`, but the second new COM lifetime was ABA-equal. Creation had armed the exact one-Present native-return proof, yet `ProcessFrame` guarded all replacement handling with raw pointer inequality, so it missed the second lifetime and the late outer OFF observer tore down the backend into the 60-Present cooldown.
- **Root fix:** logical replacement is now `pointer changed || exact creation-time lifetime proof`. An ABA-equal authoritative return therefore runs the same guarded native ownership transaction, immediate first-Present rebuild, and late-observer preservation as an address-changing return. A high-signal `[OVERLAY VISIBILITY] ... reused the previous COM pointer address; processing it as a new lifetime` breadcrumb makes the exceptional branch explicit. It does not broaden heuristic identity: only the exact game-created authoritative return proof can defeat pointer equality, and the existing inactive-FG, normal-route ownership, non-runtime-owned, and healthy-device guards remain mandatory.
- **Automated reproduction / validation:** `tools/run_dx12_fg_overlay_transition.py --sequence=extended-aba-cycle` drives the exact eight transitions above and samples through the final native-return seam. Installed-build sessions `20260716_130906` and `20260716_130931` completed in 11.94/11.74 seconds, each logged one ABA lifetime breadcrumb, zero visibility interruption/restoration or device-failure markers, and retained 79.5%/74.7% of the DLSS overlay signal at final OFF. Directional `20260716_130949` (FSR->DLSS) retained 75.1%; complete-seam `20260716_131016` (DLSS->OFF) retained 70.9%, also with clean logs. A later `DLSS -> OFF -> FSR` run exposed a zero-duration telemetry double count: inline PostSL keep-alive and its enclosing ProcessFrame accounted the same Present separately. The inline callback now delegates draw accounting to the enclosing scope, while standalone PostSL callbacks remain separately accounted. These results did not cover the separate pre-SetOptions first-proxy-Present failure later caught in `20260716_132719`; see the newer entry above.
- **Performance / compatibility boundary:** the lifetime decision is one boolean on a replacement edge; it adds no copy, queue, allocation, wait, delay, polling, game branch, Reflex/manual-limiter mutation, FG-status rewrite, or steady-state GPU work. Unknown pointer reuse still remains conservative.
- **Source anchors:** `hook/apis/dx12_hook.cpp`, `hook/common/dx12_overlay_policy.h`, `tests/test_dxgi_shared.cpp`, `tools/run_dx12_fg_overlay_transition.py`, and `llm-wiki/{current.md,frame-generation/guardrails.md,log/recent.md}`.

### 2026-07-16 - Close the first-output gaps on protected FSR and explicit cold DLSS activation

- **Two independent visual gaps:** exact `OFF -> DLSS -> OFF -> FSR` session `20260716_121923` showed one final `protected-ffx-startup-quiesce` interruption. The official FFX create had already returned the game-facing proxy and registered its descriptor producer queue, but CE installed proxy Present prework only when later enabled `ffxConfigure` arrived. Baseline pixel run `20260716_124508` then isolated a second gap on the initial OFF->DLSS edge: the CE-specific bright-R/G signal reached exactly zero. Streamline official UI was submitted continuously, but the first two proven PostSL callbacks consumed that output budget and skipped exact proxy buffers; the first proxy output did not actually contain the tag.
- **Root fixes:** after a successful recognized swapchain `ffxCreateContext`, CE installs the same module-validated game-facing proxy Present hook immediately after exact queue registration. The first protected proxy Present can therefore draw via `protected-startup-backbuffer`, while the internal real-swapchain path remains tracking-only and cannot revive the GTA 0.x-FPS fence-wait route. For Streamline, official `UIColorAndAlpha` remains the bridge before PostSL can render. The first callback with explicit current pure-DLSS activation proof now draws its exact output backbuffer and retires official coverage only after successful rendering; early return/failure keeps the bridge, and GetState-only activation remains conservative. Neither path changes FG publication, Reflex/manual limiting, or runtime ownership, and neither adds a copy, CE/foreign queue, wait, delay, polling loop, title branch, or steady-state work.
- **Automated oracle / diagnostics:** `tools/run_dx12_fg_overlay_transition.py --sequence=dlss-off-fsr` independently scores the initial OFF->DLSS and final OFF->FSR seams using a CE-colored bright-R/G signature, rejects absolute zero visibility, enforces complete 750 ms target windows, and always reaps CE/test-app processes. Exact takeover logs include `first proven startup output received an exact backbuffer draw` with `explicitPureDLSSColdStart=1`; explicit cold activation must not log `PostSL output already covered`. Existing `[OVERLAY VISIBILITY] INTERRUPTED/UNPROVEN|RESTORED` and device-failure markers remain strict-zero gates.
- **Validation:** corrected installed-build runs `20260716_125211`, `125243`, `125254`, and `125305` retained 45.2-46.0% of the stable overlay color density through OFF->DLSS and 68.6-71.0% through final OFF->FSR, with no zero sample. Directional FSR->DLSS `125322` retained 73.3%; DLSS->OFF `125332` retained 70.6%. All six logs had zero visibility interruption/restoration, stale UI-skip, or device-failure markers, and no process remained. The earlier create-time FFX fix also passed exact-sequence sessions `20260716_123550`, `123636`, `123649`, and `123702` plus directional `123739`/`123750`. Required build `0.1.4918` passed both hook architectures, test apps, Vulkan layers, packaging, import closure, PE hardening, and PDB checks. Canonical metadata `0.1.4919` passed all 1,575 tests in 115 suites plus all Python tool self-tests.
- **Source anchors:** `hook/apis/{dx12_hook,dx12_streamline_ui_overlay,ffx_hook}.{cpp,h}`, `hook/common/dx12_overlay_policy.h`, `tests/test_dxgi_shared.cpp`, `tools/run_dx12_fg_overlay_transition.py`, and `llm-wiki/{current.md,frame-generation/guardrails.md,log/recent.md}`.

### 2026-07-16 - Keep the authoritative DLSS-off native return drawable on its first Present

- **Runtime proof / root cause:** supplied switch-app session `installed/captureengine/logs/20260716_114608` accepted explicit DLSS OFF, kept the PostSL proxy alive until a game-created native replacement arrived, authoritatively re-baselined that replacement queue, and retired runtime ownership. The first replacement Present nevertheless saw `fgActive=0` but `fgRecentFrames=1`; generic swapchain-change handling armed its 90-frame transition cooldown. Visibility accounting measured 91 uncovered Presents over 672 ms. Automated baseline `20260716_115313` reproduced the same 91-Present/671 ms interruption and zero retained seam edge. The normal-return proof ended inside the creation handler instead of reaching the first matching Present.
- **First-Present ownership transaction:** the creation handler now publishes a one-Present exact returned-swapchain identity. ProcessFrame bypasses only residual recent-FG cooldown when that exact object also has authoritative normal-route queue ownership, actual/Streamline/FSR/native composition are inactive, runtime ownership is absent, and the device is healthy; it consumes the proof while rebuilding immediately on the captured game queue. FSR or Streamline enable, swapchain invalidation, and shutdown clear stale proof. The first correction exposed a second same-Present bug: after the rebuild, the late outer Streamline-OFF observer still saw its old active snapshot and destroyed the fresh RTVs as stale PostSL state, producing one uncovered Present. A matching guarded policy now skips that second drain/reinit and preserves the already healthy native backend. Unknown topology, runtime/FSR ownership, and device removal retain conservative teardown. FG publication, Reflex/menu suspension, and the optional manual limiter are unchanged; the path adds no copy, queue, delay, CPU/GPU wait, or steady-state work.
- **Diagnostics / automated oracle:** high-signal logs mark arming the exact native takeover, immediate first-Present reinit, and same-Present preservation. Existing visibility accounting must report zero `INTERRUPTED/UNPROVEN`/`RESTORED` spans. `tools/run_dx12_fg_overlay_transition.py` now supports `--sequence=dlss-to-off`, posts DLSS/OFF keys directly to the test-app PID/window, records `dlss-transition`/`transition`/`off-after` phases, and force-verifies cleanup. It stops sampling at the app's explicit timed-exit marker and rejects an incomplete requested seam, preventing the desktop window underneath from becoming false pixel evidence.
- **Validation:** corrected sessions `20260716_120544`, `120715`, `120724`, `120734`, and `120743` all passed without visibility interruption/restoration or device-failure markers. After the final rebuild, complete-seam DLSS->OFF session `20260716_121752` retained 66.7% overlay-edge signal through 750 ms after native return; opposite-direction FSR->DLSS session `20260716_121815` retained 69.8%; both logs remained clean and the harness left no CE/test process. Required build `0.1.4911` passed x64/x86 hooks, test apps, Vulkan layers, packaging, import closure, PE hardening, and PDB checks. Canonical metadata `0.1.4912` passed all 1,567 native tests in 114 suites plus all Python tool self-tests.
- **Source anchors:** `hook/apis/dx12_hook.cpp`, `hook/common/dx12_overlay_policy.h`, `tests/test_dxgi_shared.cpp`, `tools/run_dx12_fg_overlay_transition.py`, and `llm-wiki/{current.md,frame-generation/guardrails.md,log/recent.md}`.

### 2026-07-16 - Make fatal-exit interception recursion-free through normal shutdown

- **Dump proof / root cause:** supplied session `installed/captureengine/logs/20260716_025345` contained seven x64 `0xC00000FD` dumps from DX9, DX9Ex, DX10, DX11, DX12 FG switch, OpenGL, and Vulkan tests. CDB resolved every small dump to the same recursion: `CapturePreTerminationDumpIfNeeded -> HookedRtlExitUserProcess -> kernel32!ExitProcessImplementation -> HookedRtlExitUserProcess` until stack exhaustion. `RtlExitUserProcess` shares/aliases another patched target on these systems, so its inline trampoline was intentionally absent; the fallback incorrectly called `ExitProcess`, which re-entered the Rtl hook. The large supplemental dump showed the external helper waiting through `HookedNtTerminateProcess`, a consequence rather than a separate crash.
- **Root fix / invariants:** `HookedRtlExitUserProcess` now falls back through the hook DLL's deliberately unpatched static ntdll import of `RtlExitUserProcess`, never through `ExitProcess`. Fatal IAT interception is limited to application modules: every module under the Windows directory is excluded, including forwarded system implementation DLLs. A new inline-hook publication API atomically exposes each trampoline before its target patch becomes callable and rolls publication back if activation fails; forwarded KernelBase/kernel32 and Nt/Zw functions remain fallback pairs rather than sharing one original slot across two live hooks. These changes affect only hook installation and exit/failure paths, not capture or rendering hot paths.
- **Validation:** required `python build.py --skip-updates` passed end to end as installed build `0.1.4904`, including x64/x86 hooks and test apps, Vulkan layers, packaging/import closure, effective x64 CFG, and PDB checks. Canonical metadata `0.1.4905` passed all 1,548 native tests in 112 suites, four FFmpeg patch tests, four build-policy tests, and all three Python self-tests. Intermediate exact-binary matrices on build `0.1.4902` cleanly injected and normally closed all seven x64 apps in session `20260716_030628` and all six available x86 apps in session `20260716_030724`, with exit code zero and no dumps. Final packaged build `0.1.4904` session `20260716_031533` injected x64 `dx12_fg_switch_test.exe` and x86 `vulkan_test.exe`; both remained alive until requested close, exited zero, and produced no dump. Final process audits found no CaptureEngine or test-app process.
- **Source anchors:** `hook/main.cpp`, `hook/wrappers/{iat_hook,inline_hook}.*`, `tests/{test_crash_handler,test_iat_hook}.cpp`, and `llm-wiki/{dx12-injection-bootstrap,frame-generation/guardrails,log/recent}.md`.

### 2026-07-16 - Make dynamic API and fatal-dump interception CFG-safe

- **Evidence / two root causes:** supplied session `installed/captureengine/logs/20260716_021732` intermittently terminated the x64 DX12 FG switch and Vulkan tests with `0xC0000409`. CDB reproduced `FAST_FAIL_GUARD_ICALL_CHECK_FAILURE` first in `IATHook::DetourGetProcAddress` while an NVIDIA/Vulkan component called its patched import: the detour called a genuine but export-suppressed `kernel32!GetProcAddress` through a cached dynamic pointer. After removing that path, `OutputDebugStringA` exposed a second bootstrap fault through the fatal-dump hooks: process imports already routed to CE while their callable originals were still raw dynamically resolved exception exports or not-yet-published trampolines.
- **Root fix / invariant:** the GetProcAddress detour calls the hook DLL's deliberately unpatched static import. Fatal termination/fail-fast inline trampolines are installed and published before any IAT route can reach them, and static Kernel32/ntdll imports provide safe fallbacks; raw resolved OS exports are never stored as callable originals. Live Raise/RtlRaise/NtRaise exception primitives are no longer inline-patched during process-wide initialization, eliminating that inherently racy byte-patch window while retaining VEH and import/dynamic interception coverage. The hook DLL explicitly links `ntdll`. This is bootstrap/failure-path work and does not add frame-path overhead.
- **Validation:** required `python build.py --skip-updates` passed end to end as installed build `0.1.4898`, including both hook architectures, Vulkan layers, packaging, import closure, effective x64 CFG, and PDB checks. Before the fix Vulkan reliably fast-failed within about 0.5 seconds under injection; afterward Vulkan sessions `20260716_024453`, `20260716_024606`, and `20260716_024616` remained alive through repeated probes, while DX12 FG sessions `20260716_024518` and `20260716_024643` remained alive through the automatic FSR and DLSS transitions. Canonical metadata `0.1.4900` passed all 1,547 native tests in 111 suites, four FFmpeg patch tests, four build-policy tests, and all three Python tool self-tests. No CaptureEngine, test-app, or debugger process remained after validation.
- **Source anchors:** `build.py`, `hook/main.cpp`, `hook/wrappers/iat_hook.cpp`, `tests/{test_crash_handler,test_dx12_sampler_policy}.cpp`, and `llm-wiki/{build.py,dx12-injection-bootstrap}.md`.

### 2026-07-16 - Extend zero-draw-overhead forced AF across legacy APIs and OpenGL

- **Validated D3D11 boundary:** installed x86 build `0.1.4878` session `20260716_001012` sustained roughly 25,000 BioShock Infinite Presents with ordinary 3-5 ms heartbeat gaps and no device-removal/stall signature. The user confirmed that textures visibly received the intended AF effect, performance was good, and no Blackwell corruption was visible. No further D3D11 hot-path work was justified by that trace.
- **Cross-API root fix:** D3D10 no longer uses PS/VS/GS bind-time replacement caches, locks, or sampler rebinds; immutable descriptors are conservatively changed once at creation with exact-original fallback. D3D9 now has a per-vtable, event-driven logical/physical state owner for SetTexture/Set/GetSamplerState, actual and autogen mip-chain classification, per-resource filter caps, one-time late/pure-device bootstrap, config-version reconciliation, and changed-state-only driver writes. D3D6-8 use the same mutable-state model; the work also corrected D3D7's inherited vtable slots and the distinct MAG anisotropy value 5. OpenGL now covers scalar/vector, sampler-object, core/EXT DSA, image/compressed/copy allocation, immutable storage, texture views, and mip generation while checking actual mip allocation and driver anisotropy limits; already-correct AF values avoid redundant driver writes. D3D12 and Vulkan remain unchanged because their creation-time implementations already satisfy the performance model.
- **Performance/safety invariant:** no new API uses a draw/dispatch hook for AF. D3D10/D3D12/Vulkan are creation-only; D3D9 does constant-size work only when its mutable texture/sampler state changes; D3D6-8 operate only on stage/config events; OpenGL works only at parameter/storage events and never intercepts `glBindTexture`. Comparison, border, fixed/no-mip, unsupported-capability, and API-specific special samplers remain protected. Getter bootstrap failure is bounded to one attempt instead of becoming recurring hot-path work.
- **Coverage / stale-risk:** policy units cover D3D10, D3D9 mip/resource/cap/address rules, D3D7's filter-value distinction, and OpenGL mip/address/cap rules. Source regressions require per-vtable D3D9 ownership, corrected D3D7 slots, autogen handling, allocation-event OpenGL coverage, and absence of draw/bind replacement paths. Native D3D10/D3D9/D3D6-8/OpenGL runtime matrices remain pending, especially legacy state-block-heavy and multi-context OpenGL applications.
- **Verification:** required `python build.py --skip-updates` passed as installed build `0.1.4892`, including x64/x86 hooks, Vulkan layers, packaging, import closure, PE hardening/architecture, and PDB checks. Canonical metadata `0.1.4893` passed all 1,546 native tests in 111 suites plus all Python tool self-tests.
- **Source anchors:** `hook/common/sampler_override_utils.h`, `hook/apis/{dx9_sampler_state,legacy_d3d_sampler_state,opengl_sampler_override,opengl_texture_storage_override,ddraw_hook,dx8_hook,dx9_hook,dx11_hook,opengl_hook}.cpp`, `hook/wrappers/{d3d9_device_wrap,d3d10_device_wrap}.*`, `tests/{test_sampler_override_utils,test_inject_capture_source}.cpp`, `llm-wiki/{cross-api-forced-af,dx11-forced-af,graphics-overrides-and-frame-pacing,current,log/recent}.md`.

### 2026-07-16 - Restore scoped CFG coverage and eliminate legacy DLL search mutation

- **Root cause / boundary:** Clang 22.1.8 correctly emits i686 `.gfids/.giats/.gljmp`, but the clang64-to-mingw32 CRT supplies an incompatible `_load_config_used`; LLD warns, emits zero Guard CF pointers/flags/targets, and the resulting probe faults with `0xC0000005`. The earlier workaround therefore needed to preserve only the x86 CFG exception, but had also removed x64 test-app CFG/CET codegen and both architectures' stack protector/fortify. Separately, screenshot, MediaEngine, and the disposable audio helper temporarily changed the process-global DLL directory, exposing concurrent loads to a legacy current-directory/PATH-capable search policy.
- **Hardening invariants:** x64 test apps again carry the production CFG and compile hardening policy; x86 test apps keep only explicit `--no-guard-cf` while retaining stack protector/fortify/ASLR/NX. FFmpeg's own sources now add `-fstack-protector-strong` and `_FORTIFY_SOURCE=2` alongside existing CFG, with configuration version 8 forcing one rebuild. Private FFmpeg lookup uses one persistent trusted `AddDllDirectory` registration under a restricted default search policy; product DLLs load by absolute path with explicit dependency flags, and named Windows DLLs use System32-only loading. No capture, overlay, audio, or codec hot loop gained per-frame work.
- **Verification:** the PE parser now requires `CF_INSTRUMENTED` in addition to a Guard CF image bit and nonempty table/count, and the build verifies project-owned x64 and x86 test executables separately from third-party SDK DLLs. Focused Python build/PE policy tests passed 12/12. The first native attempt hit a transient nonempty FFmpeg working-copy directory during cleanup; an unchanged retry completed the hardened FFmpeg rebuild and passed all three `SecureDllLoading*` native tests. Required `python build.py --skip-updates` passed end to end as build `0.1.4883`, including the scoped x64/x86 test-app PE checks. Canonical `python build.py --no-build --run-tests --skip-updates` then passed all 1,541 native tests in 111 suites plus all Python tool self-tests at metadata `0.1.4884`.
- **Dependency-policy follow-up:** source-package recipes now receive explicit, fingerprinted `-fstack-protector-strong` and `_FORTIFY_SOURCE=2` flags after makepkg configuration instead of relying on the mutable clang64 makepkg defaults. The changed fingerprint rebuilt all eight pinned source packages and FFmpeg. Generated Autotools/Meson/CMake compile rules retained both flags except libaom, whose upstream Release configuration intentionally disables fortify; overriding that codec-hot-path policy was rejected under the no-performance-regression invariant, while its stack-cookie strings and 2,163-target effective CFG table remain present. Focused dependency-policy tests passed 10/10. Required build `0.1.4885` passed; the first whole-suite run had one transient AVIF/libaom failure, while the isolated unchanged test and an unchanged canonical retry both passed. Final metadata `0.1.4888` passed all 1,541 native tests in 111 suites plus all Python tool self-tests, and explicit product/dependency/x64-test/x86-test PE verification passed.
- **CFG injection regression and root fix:** first injection into the newly CFG-enabled x64 `dx12_test.exe` exited before hook logging with `0xC0000409`. CDB reproduced `FAST_FAIL_GUARD_ICALL_CHECK_FAILURE` (subcode 10) in `InlineHook::FinalizeExecutableTrampoline`: the dynamically resolved `SetProcessValidCallTargets` invocation was itself guarded before it could register the generated target. MinGW does not provide that API in its Kernel32 import library. One x64 `guard(nocf)` bootstrap wrapper now calls only the exact export resolved from loaded KernelBase/Kernel32; all other hook code remains CFG-instrumented, the trampoline page remains initially target-invalid and W^X, and only the aligned entrypoint becomes valid. Required build `0.1.4890` passed. In session `20260716_014304`, the x64 DX12 app remained alive through injection and rendered the overlay; session `20260716_014350` proved the x86 path also remained alive. Canonical metadata `0.1.4891` passed all 1,546 tests in 111 suites plus all Python tool self-tests.
- **Source anchors:** `build.py`, `ffmpeg_dependencies.py`, `common/secure_dll_loading.*`, CaptureEngine/MediaEngine/helper DLL load sites, `hook/wrappers/inline_hook.cpp`, `tools/verify_pe_hardening.py`, `test_{build_flags,pe_hardening,ffmpeg_dependencies}.py`, `tests/{test_crash_handler,test_secure_dll_loading}.cpp`, and `llm-wiki/{build.py,dx12-injection-bootstrap}.md`.
