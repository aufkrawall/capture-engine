# llm-wiki Log — Archive 2026-W28a

### 2026-07-12 - Cross-API graphics overrides and frame pacing are made deterministic

- **Root defects:** sampler overrides had inconsistent eligibility and incomplete API coverage; DX9 could set only
  MAXANISOTROPY, D3D10/11 wrapper/raw paths could apply non-idempotent bias twice, DX12 missed sampler/root-signature
  revisions and factory-created devices, Vulkan prerender 1-6 was a no-op, and OpenGL sync was global. DXGI mapped all
  backbuffer overrides to a depth-one wait with short escape timeouts. The local limiter used a truncated QPC interval,
  exact-deadline timer wake, catch-up intervals, and global unsynchronized cadence; VFR disabled the general cap and
  inject capture incorrectly inherited presented-FG scaling. NGX GetI/GetUI used vtable slots 8/9 instead of 11/12.
- **Root fix:** add validated safe/aggressive sampler policy, exact-once wrapper forwarding, DX12 sampler2/root 1.2 and
  device-factory interception, capability-limited transactional Vulkan overrides, OpenGL sampler-object/per-context
  support, and per-stream device/queue fence ownership. `backbuffer_count=N` supplies equivalent maximum present depth
  `N-1` when flip buffers are preserved. Limiter pacing is a rational QPC grid with no short catch-up, adaptive p99
  early-wake margin, final-50us spin, and one cadence owner. Capture-source-aware FG scaling preserves inject CFR while
  WGC/DXGI pace final output. NGX originals are retained per vtable and missing originals fail explicitly.
- **Coverage / validation:** focused config, sampler-policy, NGX ABI, limiter-source, and VFR/general tests were added.
  The required full x64/x86 product build passed at `0.1.4657`; the final focused parser/sampler regression run passed
  4/4 tests; and the canonical no-build run passed all 1,387 native tests plus three Python self-tests at metadata
  `0.1.4658`. Hardware validation remains required across representative APIs and Kena/Blackwell.
- **Source anchors:** `llm-wiki/graphics-overrides-and-frame-pacing.md` and the primary sources listed there.

### 2026-07-12 - GTA DLSS OFF normal return authoritatively rebaselines FG queue detection

- **Evidence (`installed/captureengine/logs/20260712_223915`, build `0.1.4645`):** the preceding OFF fix worked: GetState accepted DLSS-G `ON -> OFF`, the matching `slDLSSGSetOptions(OFF)` reached Streamline, and the overlay stayed visible through the retained pre-SL route. At `22:41:12.486`, EOS forwarded a normal swapchain create on original game queue `0000022F666FE6D0`; CE correctly retired the distinct Streamline/PostSL queue `00000232123C6EF0`. On the first normal Present 81 ms later, however, the queue-change heuristic compared the returned game queue against that departed Streamline queue and falsely activated FSR FG. The resulting state was internally contradictory (`runtime=FSR_FG`, but `apiFSR=0`, `directFFX=0`, `nativeFGPath=0`, `runtimeOwns=0`, and `scQueue==origGame==cmdQ`), so normal overlay initialization was deferred forever. There was no device removal, `ERR_GFX_STATE`, or crash dump in this session.
- **Root cause:** the SL-OFF reset/grace epoch was armed when GetState reported OFF, about 13 seconds before the actual normal swapchain return. During the retained-proxy interval it had already recaptured the still-live Streamline queue as its initial queue and exhausted the frame-count grace. A later authoritative topology boundary therefore cannot rely on a timeout or an earlier transition reset.
- **Root fix:** every proven original-queue normal swapchain return now clears heuristic FSR state and publishes that retained queue as the authoritative baseline for a new queue-detection epoch before the first Present, whether or not a stale PostSL route remains armed. Queue and ECL-pattern FSR heuristics remain blocked while leftover ECL traffic names any other queue. Observing the authoritative queue consumes the boundary and establishes it directly; a subsequent distinct queue remains immediately detectable as a genuine FSR activation. Epoch replacement uses atomic compare/exchange, and shutdown clears the non-owning baseline before releasing the retained original queue. This is event-driven: no sleep, grace extension, polling delay, copy, new queue, or steady-state GPU cost was added.
- **Coverage / validation:** the policy regression covers leftover-queue rejection, authoritative-baseline acceptance, and the absence of a pending-boundary block after consumption; a source invariant verifies that every proven normal return seeds the baseline before optional PostSL retirement. The required full x64/x86 product build passed at `0.1.4648`; 67 focused normal-return/Streamline tests and the broader 449-test DXGI/FG transition matrix passed. The canonical no-build run executed all 1,382 native tests at metadata `0.1.4649`; 1,378 passed, while four failures were confined to unrelated concurrent dirty-tree sampler/config work (`MalformedMipBiasIsIgnored`, two audio-latency parsing tests, and DLSS-sharpening malformed-input parsing). Fresh GTA and Talos validation is still required for DLSS -> OFF and every late OFF -> FSR/DLSS direction.
- **Source anchors:** `hook/apis/dx12_hook.cpp`, `hook/common/dx12_overlay_policy.h`, `tests/test_dxgi_shared.cpp`, and `llm-wiki/frame-generation/guardrails.md`.

### 2026-07-12 - GTA DLSS OFF normal-swapchain return retires the stale PostSL queue epoch

- **Evidence (`installed/captureengine/logs/20260712_220704`, build `0.1.4635`):** the freeze dump captured GTA's `ERR_GFX_STATE` dialog and the crash dumps stop at GTA's deliberate fatal breakpoint rather than a CE access violation. The causal hook trace is earlier: GetState accepted DLSS-G ON->OFF, the matching SetOptions OFF was still suppressed by stale PostSL settling flags, and GetState later resurrected ON locally. EOS then forwarded `CreateSwapChainForHwnd` on original queue `0000016B23D67500`; Streamline stack presence mislabeled that normal return as a takeover, so PostSL submit `#3288` used retired runtime queue `0000016EAA9D4FB0` against the original-queue swapchain. The device immediately reported `DXGI_ERROR_DRIVER_INTERNAL_ERROR (0x887A002B)`.
- **Root fix:** an accepted GetState OFF now latches that the matching SetOptions OFF must reach Streamline regardless of stale startup-protection flags and blocks GetState-only resurrection until explicit ON. Wrapped swapchain classification treats Streamline stack presence as candidate evidence: a distinct queue remains a valid runtime handoff, while a known original-queue return is normal topology and retires any previously armed distinct PostSL route. Direct runtime callers, FFX authority, and an in-flight explicit Streamline enable are excluded so late OFF->DLSS/FSR and mixed enables remain intact.
- **Lifecycle safety / performance:** normal return disables PostSL, invalidates its queue epoch and confirmation, clears old locked/last-working queues, and retires only swapchain-scoped RTV/sync state while preserving the warm device/format adapter. An already-entered callback checks the lifecycle before synchronization and ECL submission; cleanup synchronizes through the PostSL render mutex without sleeps, copies, new queues, or steady-state overhead beyond the existing uncontended render lock.
- **Coverage / validation:** focused regressions cover the accepted GetState-OFF/SetOptions-OFF sequence, original-vs-distinct queue classification, explicit-enable exclusion for late activation, route retirement, and lifecycle-submit cancellation. The final required full x64/x86 build passed at `0.1.4645`; 65 focused tests passed; and `python build.py --no-build --run-tests --skip-updates` passed all 1,375 native tests plus three Python self-tests at metadata `0.1.4646`. Fresh GTA and Talos all-direction runtime switching remains required.
- **Source anchors:** `hook/apis/{streamline_hook,dx12_hook}.{cpp,h}`, `hook/common/{streamline_runtime_policy,dx12_overlay_policy}.h`, `tests/{test_streamline_runtime_policy,test_dxgi_shared}.cpp`, and `llm-wiki/frame-generation/guardrails.md`.

### 2026-07-12 - Classic D3D9 stays native and shared capture is capability-probed

- **Finding:** Microsoft documents Vista-era D3D9 shared textures through `pSharedHandle`, but this is not a portable classic-device guarantee on current drivers. Local NVIDIA 0x2f04 x64/x86 probes returned `D3DERR_INVALIDCALL` for classic shared creation and for opening a valid Ex-helper texture, even though caps/format checks passed. The old automatic `Direct3DCreate9` -> Ex promotion hid that limitation by replacing the game's device semantics and required incomplete managed-pool emulation, which caused corruption.
- **Root fix:** classic factory/device type and creation flags are now preserved. The game device is probed before any helper is constructed, so a rejecting runtime exits the shared path without repeated helper/device startup cost. A private same-adapter Ex factory/device is used only after an authoritative probe and is never exposed to the game. Automatic promotion, managed-pool emulation, `GetDirect3D` redirection, the executable-specific exception, and the alternate wrapper promotion path are removed. True Ex applications and DXVK/Vulkan remain separate.
- **Transport invariants:** A8/X8 BGRA and A2B10G10R10 policy is explicit and unit-tested. Create/open/D3D11-import probes are authoritative; caps are diagnostic only. A supported direct ring remains one GPU `StretchRect` with no CPU readback, publishes only after nonblocking D3D9 event-query completion, and preserves leased generations across reset. Unsupported classic devices retain staging inject fallback, while GPU-based WGC is the reliable no-CPU-readback choice and can coexist with overlay-only injection.
- **Coverage and validation:** policy tests cover device preservation and format selection; source-level regression asserts native `CreateDevice`, native-probe-before-helper ordering, absence of promotion/emulation/game-specific branches, helper-owned shared open, D3D11 validation, and direct-ring activation evidence. `python build.py --skip-updates` completed the final x64/x86 build at `0.1.4640`; `python build.py --no-build --run-tests --skip-updates` passed all 1,372 native tests plus three Python self-tests at metadata `0.1.4641`. The final injected DX9 harness rendered 1,440 frames in each x64/x86 run and confirmed both classic probes rejected sharing without constructing helpers. The helper-ordering fix reduced the single initialization maximum from 192.92/210.85 ms to 47.63/60.19 ms, but both runs still failed the harness's strict 33.33 ms worst-frame threshold; steady averages were 6.85/6.82 ms with one over-20-ms frame each.
- **Source anchors:** `hook/apis/dx9_hook.cpp`, `hook/common/d3d9_capture_policy.*`, `hook/wrappers/{d3d9_wrap,wrapper_hooks}.cpp`, `tests/{test_d3d9_capture_policy,test_inject_capture_source}.cpp`, and `llm-wiki/d3d9-capture.md`.

### 2026-07-12 - Blackwell-safe DX12 forced-AF policy and complete sampler creation coverage

- **Evidence:** Kena build `0.1.4574` on an RTX 5070 loaded `anisotropic_filtering=16x` and showed a broad red edge around a dark lighting/shadow-like region. The trace installed a shared `CreateSampler` vtable hook but logged no descriptor modifications, and Kena's dynamic `D3D12SerializeVersionedRootSignature` lookup explicitly reported `no hook registered`.
- **Root defects:** DX12 converted almost every non-border sampler to anisotropic, including comparison and min/max-reduction filters, with no texture/resource context. Non-wrapper builds did not intercept raw `D3D12CreateDevice`, serializer `GetProcAddress` calls bypassed CE, precompiled root-signature blobs were never rewritten, and one global sampler original could not safely represent multiple device vtables.
- **Fix:** a shared creation-time policy now promotes only linear min/mag, mip-using wrap/mirror material-style samplers. Comparison/reduction, border, clamp/mirror-once, point-min/mag, fixed/no-mip, and invalid descriptors are preserved for AF and mip bias. Raw device creation is intercepted without returning a COM wrapper; actual-device `CreateSampler`/`CreateRootSignature` hooks retain originals per vtable. Imported and dynamically resolved serializers cover v1.0/v1.1, while device root-signature interception deserializes and safely reserializes precompiled blobs. Diagnostics report bounded fingerprints, decisions, coverage, config changes, and shutdown summaries.
- **Coverage:** focused policy tests cover protected filter/address/LOD families, AF-off, mip bias, idempotence, static bindings, and source-level interception invariants. `dx12_test.exe` creates eligible and protected dynamic samplers plus v1.0/v1.1 static root signatures at startup to exercise injected runtime coverage.
- **Validation status:** targeted syntax checks pass. `python build.py --skip-updates` completed the final full x64/x86 build at `0.1.4635`; the focused 11-test sampler-policy run passed at metadata `0.1.4636`; and `python build.py --no-build --run-tests --skip-updates` passed all 1,366 native tests plus three Python self-tests at metadata `0.1.4637`. Fresh Kena RTX 5070 visual validation remains pending.
- **Source anchors:** `hook/common/dx12_sampler_policy.*`, `hook/apis/dx12_sampler_hooks.*`, `hook/wrappers/iat_hook.cpp`, `tests/test_dx12_sampler_policy.cpp`, `testapp/dx12_test.cpp`, and `llm-wiki/dx12-forced-af.md`.

### 2026-07-12 - Ordered app-audio exit and route-local compensation remove multi-track artifacts

- **Evidence (`installed/captureengine/logs/forti1staudiotrackartifacts`, build `0.1.4617`):** the MKV itself was structurally exact: 15,583/15,583 contiguous 120-fps video packets, and all three 48-kHz PCM tracks contained exactly 6,233,200 samples / 129.858333 seconds and decoded cleanly. Content diagnostics were not clean: Brave exited about 34.7 seconds into recording; a previously-active Brave route then kept the second mixed track waiting for packets that could never arrive, while that track's Fortnite route accumulated up to 1.641 seconds. The shared-capture maximum-backlog controller applied this delayed sibling's pressure to the healthy first-track Fortnite route, activating 0.5% speed-up against a nearly empty local ring and producing repeated underruns/fades. Track 1's Brave route finished with 9,070 underruns and 4,564,020 silence-padded samples. Exact mux duration therefore did not disprove the reported audio artifacts.
- **Root causes / fixes:** process-loopback capture had no ordered terminal packet, so downstream code could not distinguish temporary sparsity from a source that had exited after becoming strict. `AppAudioCapture` now enqueues an end-of-stream marker after its final drained data packet; fan-out preserves ordering on every route, and only after consuming the marker does a started route become nonblocking timeline silence for live/final pulls. A later real packet/epoch clears the ended state and uses the normal atomic live-join reset. Compensation now evaluates the route-local ring lead; the min/max sibling backlog is retained only as a rate-limited divergence diagnostic. This prevents a delayed mixed route from speeding up and starving a healthy sibling route.
- **WGC smoothness finding / fix:** video encode, mux, and retained pools were healthy, but WGC delivered a source-limited run (2,847 repeats, longest source-starved episode 5.344 seconds), so CFR can preserve the last frame but cannot invent missing motion. The separate identical-run randomness risk was in partial-reserve startup: minimum rate windows can include pre-live settling gaps, making equivalent startup candidate piles choose different permanent smoothness delay. At least three actual startup candidates with mean cadence at or above CFR now provide a second deterministic proof for capping accidental underfed active delay to the jitter floor. True below-target/no-span cases keep the deeper reservoir/fallback.
- **Validation:** focused regressions cover ordered marker placement/fan-out, ended-source live and final nonblocking behavior, epoch resume, route-local compensation under a delayed sibling, and startup-candidate cadence classification. `python build.py --skip-updates` completed the full x64/x86 build at `0.1.4622`; `python build.py --no-build --run-tests --skip-updates` passed all 1,355 native tests plus three Python self-tests at metadata `0.1.4623`. Fresh real capture remains required to verify subjective audio and WGC cadence on the user's hardware.
- **Source anchors:** `mediaengine/{audio_capture.h,app_audio_capture.cpp,audio_sync_utils.h,mediaengine.cpp}`, `common/capture_pipeline_policy.h`, `captureengine/media_main.cpp`, `tests/{test_audio_capture_source,test_audio_sync_utils,test_capture_pipeline_policy}.cpp`, and `llm-wiki/{multi-audio-capture,wgc-capture,cfr-capture-sync}.md`.

### 2026-07-12 - GTA post-FSR DLSS suspension no longer inherits stale native-FSR ownership proof

- **Evidence (`installed/captureengine/logs/gtaoverlaydisappearondlssfgsuspend`, build before `0.1.4619`):** active DLSS PostSL rendered continuously on queue `0000018505823B40` through submit `#3820`. At `19:59:41.649`, the authoritative GetState OFF edge correctly changed runtime to `STREAMLINE_NO_FG`, but `ShouldKeepConfirmedPostSLAliveAcrossStreamlineOff` rejected the established make-before-break route because `HookHasRuntimeOwnedNativeFGPresentPath()` was still true. CE disabled PostSL, forced `overlayInit=0`, spent 60 frames in reinit cooldown, then deferred forever as `runtime-owned native FSR Present teardown window` with `apiFSR=0`, `directFFX=1`, `nativeFGPath=1`, `callbackEver=0`. The repeated `Deferring overlay init` / `Overlay suppression exceeded 2s` lines are the disappearance, not a failed PostSL draw or device removal.
- **Root cause / fix:** `FGCompatibility::SetDLSSFGActive(true)` gave DLSS priority by directly clearing `fsrFGApiActive` and its multiplier, but did not clear `directFFXApiConfirmed`. That confirmation is activation-scoped FSR evidence; leaving it true let the DX12 ownership predicate reconstruct native-FSR Present ownership from the generic Streamline runtime-owned swapchain during the later DLSS suspend. DLSS activation now clears direct FFX confirmation first, then publishes FSR inactive, so readers cannot observe or retain the invalid `apiFSR=0 && directFFX=1` combination. The one-shot transition log is `FG: Cleared direct FFX API confirmation — DLSS FG took priority`.
- **Behavioral invariant:** a confirmed FSR configure proof survives repeated signals only within the same authoritative FSR activation. FSR OFF, context recreation, or DLSS taking priority ends that proof. Generic runtime-owned swapchain identity remains intact for Streamline routing, but cannot by itself become native-FSR presentation authority. The next confirmed DLSS OFF edge can therefore arm the existing PostSL make-before-break keep-alive and warm-resume path; real active/tearing-down FSR evidence still blocks it.
- **Validation:** `python build.py --skip-updates` completed the full x64/x86 build at `0.1.4619`. The focused regression passed at metadata `0.1.4620`; `python build.py --no-build --run-tests --skip-updates` then passed all 1351 native tests and three Python self-tests at metadata `0.1.4621`. Fresh GTA visual suspend/resume validation remains required.
- **Source anchors:** `hook/common/fg_detection.cpp`, `hook/apis/dx12_hook.cpp` (`HookHasRuntimeOwnedNativeFGPresentPath`, `DX12_OnStreamlineFGStateChanged`), `tests/test_fg_session_state.cpp`, `llm-wiki/{current.md,frame-generation/guardrails.md}`.

### 2026-07-12 - FFmpeg runtime-version drift fixed and startup dependency smoke-tested

- **Root cause:** `build.py` previously delay-loaded hard-coded FFmpeg DLL majors while the installed private FFmpeg tree had advanced to `avcodec-63.dll`, `avformat-63.dll`, and `avutil-61.dll`. Runtime synchronization also copied every matching version, allowing stale DLLs to remain ambiguous.
- **Fix:** resolve the active versioned names from the FFmpeg install tree, use those names for Windows delay-load flags, select the highest numeric version for the runtime bundle, and log absent optional dependencies. Test compilation also refreshes shared common objects with the current sanitizer/non-sanitizer flags to prevent cross-mode link contamination.
- **Validation:** the full default `python build.py` completed successfully at build `0.1.4614`, with lint/type checks green, sanitizer regression green, all 1350 native tests passing, FFmpeg checked/bundled at commit `a09be9b9`, and all product/test artifacts built. The controller reached `Ready` and the direct media process logged successful `mediaengine.dll` loading, FFmpeg-directory setup, encoder initialization, and WGC setup; no missing-FFmpeg-library error was observed. The FFmpeg source was already current, so no source rebuild was needed in that run.
- **Source anchors:** `build.py` (`resolve_ffmpeg_runtime_dll_names`, `sync_ffmpeg_runtime_dlls`, `compile_tests`, CaptureEngine link flags), `installed/captureengine/logs/runtimecheck_media_194855/media.log`, and `llm-wiki/build.py.md`.

### 2026-07-12 - GTA FSR-FG overlay uses the real queue beneath a proven Streamline wrapper

- **Evidence (`installed/captureengine/logs/gtafsrfgoverlayflicker`, build `0.1.4574`):** the FFX proxy Present hook was live on the game thread, but its creation descriptor's queue `000002188A9E4BC0` reported Streamline wrapper device `00000218817781B0`; the registered UI resource belonged to real device `0000021886F625E0`, also owned by CE's retained original game queue `0000021885EF83E0`. Exact device-identity validation therefore rejected every owner-queue draw. Each source registration fell into AMD's presenter-thread compatibility route twice (real and generated output), with a dedicated CE queue and CPU completion wait; first initialization made `DetourPresent` take 51.2 ms. The same mismatch rejected the presented-backbuffer route during FSR suspension, where AMD does not display the UI texture.
- **Root fix / safety:** StreamlineHook now retains the most recent D3D12 device accepted by `slSetD3DDevice`. Per-proxy FFX bindings retain both the exact descriptor queue and a lazily resolved original game queue. Selection always prefers an exact target-device match; it unwraps to the original queue only when the descriptor device is the accepted Streamline wrapper and the original queue device owns the actual UI/backbuffer target. Active FSR and retained suspension then share the fenced per-proxy renderer on that validated queue immediately before proxy Present. Unknown/cross-device topologies still refuse submission. Target snapshots and queue/device references are retained across recording/submission and released safely across replacement/shutdown.
- **Visibility/status matrix:** transition-model state now represents native-FSR suspension explicitly. A regression drives the requested sequence OFF -> FSR on -> FSR suspended -> DLSS on -> DLSS suspended -> DLSS resumed -> FSR -> DLSS -> OFF -> DLSS, checking at every step that overlay rendering remains allowed, runtime mode/activity and visible FG status are correct, and no transition selects a hidden render mode. Queue-policy/source tests cover exact/proven-wrapper selection, active UI versus suspension backbuffer targeting, one-time presenter fallback, and proxy-prework-only re-registration.
- **Performance audit:** the FSR fast path is one owner-queue command-list submit per accepted source registration with no copy, foreign queue, cross-queue handoff, or CPU wait; the presenter fallback deduplicates AMD's real/generated Presents. DLSS's warm PostSL path already submits one native command list, but allocator selection could wait up to 1 ms on the preferred round-robin slot even if another of 16 slots was complete. It now scans the full pool first and reserves the bounded wait for genuine all-slots-busy pressure. Focused policy/sequence coverage passed; the required final full x64/x86 product build passed at `0.1.4600`, and the canonical no-build run passed all 1350 native tests plus three Python self-tests at metadata `0.1.4601`.
- **Runtime status:** the implementation and deterministic regression suite are green, but fresh visual/cadence validation in GTA, Talos, and the injected `dx12_fg_switch_test` matrix remains required; no claim of on-hardware no-flicker confirmation is made from unit/model tests alone.
- **Source anchors:** `hook/apis/{dx12_hook,streamline_hook,dx12_ffx_suspend_overlay}.{cpp,h}`, `hook/common/{dx12_overlay_policy,dx12_fg_transition_model}.{h,cpp}`, `tests/{test_ffx_ui_composite_driver_policy,test_dx12_fg_transition_sequences,test_dxgi_shared}.cpp`, and `llm-wiki/{current,frame-generation/guardrails}.md`.

### 2026-07-12 - Process-loopback restarts atomically live-join every routed track

- **Evidence (`installed/captureengine/logs/hots`, build `0.1.4574`):** the 1,437.783 s DXGI-duplication capture itself had an exact 120 fps packet grid and all three PCM tracks ended sample-exactly at the video endpoint, but those equal durations hid a real content-path fault. `Brave.exe` PID 11020 exited at 16:42:38 and returned as PID 22668 at 17:03:55. The shared physical capture correctly fanned packets to tracks 1 and 2, but both routes retained the old process epoch/source-local cursor. Their first new packet became a 61,254,300-sample timeline gap, filled the 30-second rings, caused two `[A/V ZERO DRIFT WARNING]` events, 41 catastrophic resyncs on the mixed route, large normal trims on the pure route, and late-content risk despite exact mux durations.
- **Root fix / invariant:** every successful process-loopback activation now increments an epoch embedded in its immutable packets. Each fanned route independently observes the transition in the same audio-worker pass, clears only route-local ring/resampler/backlog-control state, and resets its source timestamp so the existing late-app live-join policy overlaps already-encoded silence. Initial activation, endpoint audio, and ordinary packet gaps do not transition. This covers name-monitored PID replacement plus fatal/silent-stall WASAPI re-activation without process-specific logic, sleeps, unbounded buffering, or duplicate physical captures.
- **FFmpeg master compatibility:** the already-present project invariant is restored in `audio_encoder.cpp`: sample formats, sample rates, and channel layouts are queried through `avcodec_get_supported_config`, because current FFmpeg master/libavcodec 63 no longer exposes those `AVCodec` arrays. Normal builds still use `python build.py --skip-updates` to reuse the already-built private FFmpeg prefix; this does not rebuild FFmpeg.
- **Diagnostics/tests:** activation logs include `epoch=N`; a transition emits `[AudioRoute] App capture epoch transition ... resetting route-local buffers for atomic live rejoin`, followed by the normal late-join line. Pure policy tests distinguish initial/same/endpoint/changed epochs and compose the 1,430-second restart with live join; source integration coverage pins epoch publication and route reset wiring. The focused FFmpeg-master-linked suite passed 132/132 audio sync/capture-source/encoder tests. The required `python build.py --skip-updates` build passed as `0.1.4593`, reusing the existing private FFmpeg build, and `python build.py --no-build --run-tests --skip-updates` passed 1,341 tests with the one established skipped binary-string test plus all Python tool self-tests (`0.1.4595` metadata bump only).
- **Source anchors:** `mediaengine/{audio_capture,app_audio_capture,audio_sync_utils,audio_encoder,mediaengine}.*`, `tests/{test_audio_sync_utils,test_audio_capture_source}.cpp`, and `llm-wiki/{current,multi-audio-capture}.md`.

### 2026-07-12 - Capture method now selects video acquisition without suppressing injection features

- **Root cause:** `BuildInjectorConfigState` classified both explicit screen-grab methods as overlay-only injection modes. With `capture_method=wgc` or `capture_method=dxgi_dup`, it cleared the normal game `whitelist`, so the selected video backend also disabled the injected overlay and graphics overrides. Conversely, simply retaining full injection without another state boundary would make hooks publish expensive shared textures during screen-grab recordings even though the media path rejects inject frames.
- **Fix / invariants:** injection target policy no longer depends on `capture_method`; both normal and overlay-only target lists remain intact. The raw `captureRequested` session bit still drives recording UI and capture-synced behavior, while the new `kCaptureRuntimeFlagInjectVideoCaptureRequested` independently gates hook-side capture work in the common IPC path, capture pacing, and Vulkan. The injector clears the path flag before each new raw start request and on stop, preventing stale publication after an interrupted media session. Explicit WGC/DXGI keep injection features but do no unused inject copies. `auto` selection is unchanged, and its live inject-to-WGC fallback clears inject publication only after WGC first-frame proof commits the replacement path.
- **Diagnostics/tests:** startup logs identify screen-grab video with retained full/overlay-only injection target counts; media logs every inject-video publication transition and reason. Policy tests cover WGC, DXGI duplication, unchanged auto lists/rescan behavior, the two-part session/path gate, generated config guidance, and auto handoff ordering. The freshly linked focused suite passed 27/27 at metadata `0.1.4571`; the final required x64/x86 build passed at `0.1.4574`; and the canonical no-build run passed all 1339 native tests plus three Python tool self-tests at metadata `0.1.4575`.
- **Workflow:** `AGENTS.md`, `build.py.md`, and `codestyle.md` now prohibit whole-file in-place formatting of existing sources unless explicitly requested; require the full build once after the final change set rather than after each intermediate edit; stage task-owned paths instead of blind `git add -A`; scope dump/LSP cleanup to relevant work; make wiki edits conditional on durable knowledge; and require high-signal, rate-limited logging. These refinements remove redundant or unsafe work without weakening final build/test/commit discipline.
- **Source anchors:** `common/{inject_overlay_policy,shared_defs,config}.*`, `captureengine/{inject_main,media_main}.cpp`, `hook/common/{ipc_client,capture_pacing}.h`, `hook/vulkan_layer/{layer_ipc,vulkan_layer}.cpp`, and focused tests.

### 2026-07-12 - HAGS-on capture contention hardening

- **Scheduling/QoS:** `gpu_scheduling_priority=auto` is now the generated/missing default. WDDM 2.7/2.9 caps are queried by actual adapter LUID; confirmed HAGS selects HIGH and other/unavailable states select ABOVE_NORMAL. Adapter/driver/build/query/readback evidence is logged. Missing defaults now match shipped high/+7/same-device values, WGC reapplies and verifies device priority after rebuilds, and encoder/capture threads use checked MMCSS plus disabled execution-speed throttling.
- **Inject correctness:** controller-PID frame-ready events replace yield/`Sleep(1)` polling. Publishers signal only empty-to-nonempty transitions after release publication. Event coalescing uses an independent metadata-ingest cursor, not the deliberately delayed texture-lease acknowledgement; the first hardware gate exposed the distinction when a refill failed to wake after the initial sixteen leased frames, and a focused regression plus inject rerun lock the fix. DX12/DX11/DX10/Vulkan scan for slots that are both media-unleased and GPU-complete; Present never waits, and all-busy drops safely for CFR repeat. ABI 30 adds the ingest cursor and independent contention/event/delay counters.
- **WGC/DXGI memory:** the 3 GB FP16/R10 target remains fixed. Shaders and retained pools prewarm before callbacks/start; duplication validates the first texture. Full allocation is attempted first and only true memory exhaustion reduces optional depth/source buffers. Local video-memory budget/usage/reservation is sampled; diagnostic reservation remains off by default and resets on teardown.
- **Attribution/tests:** analyzer verdicts separate WGC producer, duplication consumer, capture GPU queue, hardware encoder, and media CPU starvation. The HAGS contention matrix profile constructs three-run 4K120/10-bit p1/p5/p6/p7 DXGI/WGC/inject scenarios with deterministic CPU workers. Final full x64/x86 product build passed at `0.1.4564`; the canonical test-only run passed all 1336 native tests plus three Python self-tests at metadata `0.1.4566`. Targeted hardware inject rerun `avsync_runs/20260712_012347` proved continuous visuals and event progress (1691-2255 signals, no ring-full/corruption/missing-marker failures); one of three scenarios passed fully, while two remained strict failures solely because their synthetic app-audio source logged 1/4 live underrun warnings. The full contention matrix and 40-minute soak remain runtime release gates, not claimed green here.
- **Source anchors:** `captureengine/{media_main,wgc_capture,windows_gpu_scheduling,dxgi_dup_capture}.*`, `common/{config,shared_defs,capture_base,gpu_scheduling_policy}.*`, `hook/{common,apis,capture,vulkan_layer}/*`, `tools/{analyze_capture_av,run_av_sync_matrix}.py`, and focused tests.

### 2026-07-12 - Talos GetState-only DLSS-G activation adopts a pre-tagged official UI overlay

- **Evidence (`logs/20260711_235733`, Steam loaded):** the core `slSetTagForFrame` hook was installed, but Talos never resolved/called `slDLSSGSetOptions`; its export remained unhookable and only `slDLSSGGetState` was delivered at `23:57:42.716`. GetState reported OFF at `42.718`; normal backbuffer overlay submits continued through Present 34 at `42.798`; then the same thread's GetState first reported options ON and armed the old activation-only UI bootstrap. No later frame tag arrived before DLSS output. Presents 35-42 formed an eight-Present/109 ms `postsl-inactive` uncovered streak until the first healthy same-queue PostSL draw at `42.916`. There was no device removal, queue/resource failure, freeze, or Steam hook-chain failure. The already-overlaid backbuffer did not protect DLSS's separately recomposed UI output.
- **Runtime refinement (`logs/20260712_001805`, build `0.1.4545`):** GetState-delivery standby armed successfully at `00:18:16.266`, but no `UIColorAndAlpha` record or submitted/pending adoption existed when GetState reported ON at `16.372`; the state machine correctly fell back to a fresh activation-only arm. Four Presents/94 ms were still uncovered before healthy PostSL at `16.469`. Thus the implementation worked as designed but its trigger was still too late: Talos had published the reusable UI tag before resolving GetState. Steam, device, queue, and PostSL remained healthy.
- **API-path refinement (`logs/20260712_002341`, build `0.1.4547`):** D3D12-device standby armed at `00:23:49.996`, more than one second before GetState OFF and activation, but the new bounded opportunity diagnostic logged zero `slSetTagForFrame` invocations. GetState ON at `51.152` therefore fell back to a fresh activation-only arm; eight Presents/94 ms were uncovered until healthy PostSL at `51.261`. No double-draw, device removal, Steam-chain, queue, or PostSL failure explains the visible pulse. Local PE inspection shows Talos's `sl.interposer.dll` exports both `slSetTag` and `slSetTagForFrame`; NVIDIA documents the former as the deprecated global-tag API. CE had only intercepted the latter.
- **Local-tag refinement (`logs/20260712_003042`, build `0.1.4549`):** both global/frame tag hooks installed before the D3D12 standby, yet neither API was called: there were zero official-UI opportunities. GetState changed OFF→ON at `00:30:51.514`; the activation again had no standby record and seven Presents/94 ms were uncovered before healthy PostSL. This falsifies both exported-tag hypotheses. NVIDIA's bundled `sl_core_api.h` explicitly permits `ResourceTag` structures as frame-local `slEvaluateFeature` inputs and states they do not interact with either global tag API. Talos therefore requires interception at evaluation, not another timing change.
- **Decisive handoff evidence (`logs/20260712_004201`, build `0.1.4552`):** the evaluate hook installed and `GetProcAddress` returned CE's detour, but Talos made no evaluate call before activation. GetState reported ON at `00:42:12.050`; five presents/78 ms were genuinely uncovered until PostSL's first successful same-queue draw at `12.134`. Talos initialized DLSS SR roughly 1.6 seconds later and only then called `slSetTagForFrame`; the usable type-23 UI tag arrived at `13.709`, so no official resource path can cover the earlier activation seam. Crucially, PostSL already proved `scQueue==origGame`, no SL wrapper/command queue, no FSR history, and a healthy device, but waited for the 100 ms `ProcessFrame`-dormant timer before activating. The timer—not resource-tag interception—was the remaining blank.
- **Root fix / invariant:** official tag/evaluate interception remains as opportunistic first-output coverage, but a proven same-queue pure-DLSS PostSL callback is now the authoritative handoff event. It activates PostSL immediately even when the previous normal `ProcessFrame` timestamp is recent; separate-queue/GTA and post-FSR families retain the dormant/cooldown protections. Make-before-break uses the existing presented-frame draw counters: if the normal route already drew since the last accounting boundary, the current present is counted as covered and the first PostSL draw moves to the next callback, preventing both a blank and a double draw. If no normal draw is pending—as in `004201`—PostSL draws on that first callback. No sleep, timeout reduction, extra queue, copy, or wait is involved.
- **Diagnostics / validation:** expect `PostSL synthetic startup immediate same-queue takeover`; if a normal draw already covered the boundary, also expect `postsl-same-queue-make-before-break` and the explicit no-double-draw line. The evaluate/tag hooks retain their prior bounded diagnostics and unload/reload safety. Source and policy regressions cover the recent-ProcessFrame same-queue override plus make-before-break integration. The first build attempt at metadata `0.1.4554` was temporarily blocked by concurrent capture-schema edits; after those settled, the required full x64/x86 build passed at `0.1.4558`, followed by all 1334 native tests and three Python self-tests at metadata `0.1.4559`. Real Talos no-flicker validation remains required.
- **Source anchors:** `hook/apis/{streamline_hook,dx12_hook,dx12_streamline_ui_overlay}.{cpp,h}`, `tests/test_dxgi_shared.cpp`, `llm-wiki/{current.md,frame-generation/guardrails.md}`.

### 2026-07-11 - First DLSS-G output inherits the inject overlay through Streamline's official UI layer

- **Evidence (`logs/20260711_221857`, build `0.1.4523`):** FSR teardown completed at `22:19:10.336-10.366`; the replacement six-buffer Streamline proxy reached `PostSL handoff prewarm READY before DLSS enable` at `13.523`; `slDLSSGSetOptions(ON)` succeeded at `13.582`; and the first successful PostSL callback/draw arrived only at `13.763`, about 181 ms later. There was no resource rebuild, probe rejection, device removal, freeze, or crash. The remaining flash is therefore causally before PostSL can render, not a failed post-FSR prewarm. FSR makes the cold topology more likely but is not the fundamental dependency.
- **Root fix / invariant:** on a real DX12 DLSS OFF-to-ON edge, the core Streamline hook now intercepts `slSetTagForFrame` and records one overlay draw into the official full-frame `UIColorAndAlpha` resource before forwarding the tag. The draw is appended to the app-provided command list, restores the tagged resource state, and adds no copy, command queue, separate submission, or wait. Exact ECL bracketing arms bounded output coverage before any wrapped-queue Present re-entry and signals a private completion fence afterward. Coverage is tied to the current activation command list, so a delayed prior-epoch list cannot arm the new epoch. PostSL skips duplicate activation-output draws and resumes on the next frame tag. D3D12 device identity, texture/RTV/blend support, dimensions, state, device ownership, and direct initialization queue are validated; unsupported/missing UI tags fall back to PostSL, and D3D11/Vulkan remain untouched.
- **Diagnostics / validation:** new breadcrumbs cover hook installation, activation arm/disarm, renderer initialization, UI record, exact command-list submission, bounded PostSL duplicate suppression, slot pressure, signal failure, safe shutdown retirement, and no-usable-tag fallback. The hot path uses one atomic branch when no activation record is pending; resource/format/queue inspection is strictly one-shot. The source regression verifies hook order, DX12 gating, retained target lifetime, exact ECL bracketing, and absence of copy/queue/wait/extra-submit operations. Required clean x64/x86 build passed at `0.1.4541`; the canonical no-build run passed all `1328` native tests plus three Python self-tests at metadata build `0.1.4542`. Interactive switch-test and GTA/Talos confirmation remain required.
- **Source anchors:** `hook/apis/{streamline_hook,dx12_hook,dx12_streamline_ui_overlay}.{cpp,h}`, `tests/test_dxgi_shared.cpp`, `llm-wiki/frame-generation/guardrails.md`.

### 2026-07-11 - Multi-track audio capture fan-out and startup backlog elimination

- **Evidence (`logs/20260711_221545`):** DXGI video was healthy (`902/902` CFR packets, one-tick maximum gap, no black intervals, fast conversion/encode, no keyed-mutex failures). All three PCM tracks decoded to exactly 360800 samples. The remaining defect was content timing: Brave process loopback averaged 261 ms delay on track 1 and 284 ms on track 2, while only track 1 activated its 0.5% backlog drain. The one microphone configured for tracks 1+3 was captured twice; the second endpoint instance discarded a different startup packet and advanced 25920 samples (540 ms), producing leading silence despite exact final duration.
- **Root causes:** route expansion created a separate `AudioCapture`/`AppAudioCapture` for every target track, giving identical source content independent WASAPI clients, queues, startup phases, and QPC histories. After the WGC anchor opened, `AudioLoop` also discarded only one pre-anchor packet per source per outer pass, so preserved process-loopback queues could remain hundreds of milliseconds behind live. App drain decisions were route-local, allowing duplicated routes to disagree. Pre-trailer `AVStream::duration` was sampled before `av_write_trailer`, generating a false zero-duration warning even though the authoritative packet scan was exact.
- **Fix / invariants at that point:** each normalized physical identity (app process or typed endpoint device) owns one capture across sections/routes; immutable raw packets fan out to route-local rings/resamplers/encoders. App loopback requests the widest routed layout once and converts per track. Pre-anchor packets drain in bounded batches up to the capture queue's 256-packet retention bound before resampling, with pre-start evidence propagated to every route. The initial group-maximum compensation rule was later disproved by `forti1staudiotrackartifacts` and superseded by route-local compensation, as documented in the newer entry above. Final metadata diagnostics run after trailer, report available/declared fields, and defer to packet timelines/post-mux probing when muxers leave in-memory durations unset.
- **Diagnostics / validation:** `[AudioRoute]` logs topology, shared owners, selected app format, periodic fan-out, stop counts, and pending follower queues; `[AudioLoop] Batched pre-start discard` logs cleanup totals and the next packet timestamp. Source tests cover capture coalescing/fan-out, widest app format, bounded startup cleanup, shared-route backlog pressure, and post-trailer metadata ordering. Required x64/x86 product build completed at `0.1.4534` (the concurrently running `dx12_fg_switch_test.exe` alone could not be replaced and produced a nonfatal test-app permission warning); all `1328` native tests and three Python tool self-tests passed at metadata build `0.1.4536`.
- **Source anchors:** `mediaengine/{mediaengine,video_encoder}.cpp`, `tests/{test_audio_capture_source,test_video_encoder_source}.cpp`, `llm-wiki/{multi-audio-capture,cfr-capture-sync}.md`.

### 2026-07-11 - DXGI split-device black CFR repeats and abandoned keyed-mutex slots

- **Evidence:** real-hardware session `installed/captureengine/logs/20260711_200954` proved the dedicated canonical DXGI path restored fast conversion/encoding and exact CFR packet coverage (`2489/2489`, one-tick maximum PTS gap), but the video visibly flickered black. Decoding `capture_13252281.mkv` with FFmpeg `blackdetect` established the signature: each fresh frame was valid, while the following CFR repeat frames were black until another fresh source frame arrived. The capture log simultaneously reported up to thousands of producer `KMFail` events per second, 1304 safe pool drops, and 1662/2488 repeats despite a fast encoder.
- **Root causes:** cursor-aware CFR repetition copied the split-device source into `repeatSourceFrameTexture` after fresh conversion had returned the shared surface to key 0, but did not reacquire that key; the invalid cross-device read populated the repeat cache with black pixels. Separately, a retained frame discarded by selection/CFR released its CPU lease without ever reaching the encoder's key-1 consumer, leaving its surface at key 1 forever while the producer retried only key 0. These abandoned surfaces progressively poisoned the retained pool.
- **Fix / invariant:** the repeat-source cache now performs an explicit nonblocking key `0→0` read and flushes command submission before returning ownership; failure falls back to the already-valid converted-frame repeat cache. A producer holding the sole free slot lease first acquires key 0, then safely reclaims key 1 when a previously published frame was abandoned, and republishes successful canonical work at key 1. Failed canonicalization always returns key 0 and is never published. No GPU completion wait, sleep, or 8-bit fallback was added.
- **Diagnostics / validation:** `[WGC Perf]` now separates real `KMFail` from `KMReclaim`, logs the first abandoned-slot reclaims, and the encoder logs `Cursor-aware repeat source cache synchronized at keyed mutex 0->0` or explicit acquire failures. Clean x64/x86 build passed at `0.1.4523`; focused `CaptureCoordinatorSourceTest.*` passed 11/11 at metadata `0.1.4524`; the full suite passed 1323/1323 native tests plus three Python self-tests at metadata `0.1.4525`. A new DXGI recording is required for pixel-level runtime confirmation.
- **Source anchors:** `captureengine/wgc_capture.cpp`, `captureengine/wgc_capture.h`, `captureengine/media_main.cpp`, `mediaengine/video_encoder.cpp`, `mediaengine/video_encoder.h`, and `tests/test_capture_coordinator_source.cpp`.

### 2026-07-11 - Dedicated DXGI canonical handoff, immutable CFR ticks, and packet-authoritative audio length

- **Evidence:** paired 4K120 AV1 p5 sessions `20260711_192720` (WGC) and `20260711_192800` (DXGI duplication) used the same P010/NVENC settings. WGC wrote 1029/1029 CFR packets; DXGI acquired 1904 source frames but its raw retained R10 path reached 5-21 ms conversion and 12-34 ms encoder submission, writing only 385/1564 packets with a 22-tick maximum gap. Both runs had an audio stream truncated to exactly 240000 samples despite longer duration metadata.
- **DXGI root fix:** duplication always uses a dedicated same-adapter D3D11 device/context and shader-canonicalizes raw surfaces into a CE-owned R10 shared pool. Cross-device keyed-mutex ownership is explicit (producer 0→1, screen encoder 1→0); inject retains key 0. Direct-RGB and VP paths use the backend-correct key, direct VP is limited to CE-owned screen surfaces, and staging remains the driver fallback. R10-to-R10 canonical work is included in telemetry and logs identify backend/staging/timing basis. WGC keeps its same-device fast path.
- **CFR/audio correctness:** live scheduler rebase no longer skips output ticks; every CFR backend drains its accrued contiguous prefix at stop. Written packet timelines track maximum PTS gaps and finalization requires exact packet coverage plus <=1.01 ticks. Desired durations are no longer written over stream metadata. Packet/audio frame scans are the analyzer default. Packetless timeline-valid co-mixed sources become aligned silence; late sources join the current cursor. `AudioEncoder::EncodeSamples` reports accepted/submitted samples, track cursors advance only by accepted samples, deferred start preserves an assigned end, the first accepted batch is no longer cleared, and batches beyond the former five-second FIFO ceiling are retained/drained.
- **Configuration:** `wgc_allow_lossy_bgra8_pool=false` is canonical. `wgc_prefer_compact_10bit_pool` remains a deprecated alias only when the canonical key is absent. Explicit 10-bit/HDR ignore lossy BGRA8; DXGI never uses it.
- **Validation:** required clean build passed at `0.1.4521`; the canonical no-build run passed all `1322` native tests plus three Python tool self-tests at metadata build `0.1.4522`. Focused coverage includes the 625600-sample batch, packetless/sparse audio, CFR no-rebase/stop drain, reordered packet PTS gaps, and config alias precedence. A paired real-hardware rerun remains required.
- **Source anchors:** `captureengine/{wgc_capture,media_main}.cpp`, `mediaengine/{video_encoder,audio_encoder,mediaengine}.cpp`, `mediaengine/{audio_sync_utils,mux_invariants}.h`, `common/{capture_pipeline_policy,config}.{h,cpp}`, `tools/analyze_capture_av.py`, and focused tests.

### 2026-07-11 - Post-FSR Streamline prewarm cannot be bypassed by the no-FG startup-preservation heuristic

- **Evidence (`logs/fsrfgtodlssfgoverlaybrieflydisappear`, manifest session `logs/20260711_175014`, build `0.1.4502`):** FSR shut down at `17:50:22.201-22.226`; Streamline created a fresh six-buffer proxy at `17:50:25.048-25.054`; CE then logged `Fresh authoritative Streamline no-FG handoff preserved live overlay backend`. There was no `PostSL handoff prewarm READY before DLSS enable` breadcrumb before `slDLSSGSetOptions(ON)` at `17:50:25.115`. The first PostSL callback at `17:50:25.282` submitted successfully, but the intended pre-enable replacement-swapchain preparation had never run.
- **Root cause:** the runtime-inactive startup-preservation predicate relied on volatile `s_startupOverlayObservedAnyFG`. That state had reset by the post-FSR Streamline handoff, so a real FSR-to-DLSS transition was misclassified as the harmless Social/EOS no-FG startup-swapchain case. Preservation bypassed retiring the old swapchain-scoped RTV/sync resources, which in turn bypassed the prewarm introduced for this transition.
- **Fix / invariant:** durable `g_HadFSRFGPhase` history now explicitly disqualifies runtime-inactive startup preservation at both policy call sites. After any FSR phase, a fresh authoritative Streamline proxy retires the old swapchain-scoped resources while retaining the device/format-scoped adapter, then runs the existing pre-enable PostSL prewarm. Pure no-FG startup handoffs without FSR history retain their no-blank preservation behavior.
- **Regression / validation:** `SettledStartupOverlayStaysVisibleAcrossRuntimeInactiveStreamlineHandoff` now covers both sides of the distinction: no prior FG preserves the live startup backend, while prior FSR forces the real handoff/prewarm path. Required full x64/x86 build passed at `0.1.4509`; the focused policy test passed; canonical validation then passed all `1315` native tests and three Python tool self-tests at metadata build `0.1.4511` (one unrelated audio-ring publication test failed on the first run and passed on the complete rerun). Interactive FG switching remains with the user's test session to avoid process/log interference.
- **Source anchors:** `hook/apis/dx12_hook.cpp`, `hook/common/dx12_overlay_policy.h`, `tests/test_dxgi_shared.cpp`, `llm-wiki/frame-generation/guardrails.md`.

### 2026-07-11 - FSR-to-DLSS first-Present flicker: warm backend reuse plus pre-enable PostSL prewarm

- **Evidence (`logs/20260711_172959`, build `0.1.4494`):** the FSR context shut down cleanly at `17:30:20.477`; Streamline created its fresh six-buffer proxy at `17:30:22.921-22.935`; explicit `slDLSSGSetOptions(ON)` followed at `17:30:22.994`; the first PostSL callback arrived at `17:30:23.152`. Although that callback submitted successfully before returning to Present, CE spent about 17-20 ms inside it shutting down and rebuilding the DX12 overlay adapter (`InitImGui` `23.152 -> 23.169`, first draw `23.172`). DLSS's presenter can already emit output while that callback is blocked, matching the user's very brief overlay disappearance exactly at activation. There was no cooldown/probe miss or device removal.
- **Root cause:** fresh post-FSR Streamline handoff invalidated the old swapchain RTV/sync state but failed to request reuse of the already-live device/format-scoped overlay adapter. `InitImGui` therefore treated the queue change as a full backend replacement even though the adapter does not retain swapchain buffers or use its stored queue for submission. The replacement proxy and exact owner queue existed roughly 59 ms before DLSS enable, but CE left all replacement setup until the first generated Present.
- **Fix / invariant:** a fresh authoritative post-FSR Streamline handoff with a previously live overlay now preserves the warm adapter and prebuilds the replacement proxy's RTV heap plus allocator/list/fence state on the proxy's exact queue before DLSS is enabled. This preparation records/submits no overlay draw and does not touch a backbuffer state; the first PostSL callback only records the real overlay draw. The gate requires FSR history, runtime-owned DX12 proxy, DLSS still OFF, a genuinely live retiring overlay, and a fresh authoritative queue. Pure-DLSS cold startup, already-running DLSS, reused/non-authoritative queues, non-DX12 swapchains, and incomplete old overlays retain their prior guarded paths. Any prewarm failure leaves the ordinary first-PostSL bootstrap available.
- **Diagnostics:** expect `Fresh authoritative Streamline handoff ... preserving the warm device-scoped backend`, followed before `slDLSSGSetOptions(ON)` by `PostSL handoff prewarm READY before DLSS enable`. The first `PostSL WARMUP COMPLETE` should then report `overlayInit=1 syncInit=1` and should not log `PostSL bootstrap — rebuilding torn-down overlay state`.
- **Validation:** required clean x64/x86 build passed at `0.1.4499`; focused warm-reuse/prewarm policy tests passed; canonical no-build validation passed all `1315` native tests and three Python tool self-tests at metadata build `0.1.4500`. Interactive validation is intentionally left to the user's already-running switch tests to avoid process/log interference.
- **Source anchors:** `hook/apis/dx12_hook.cpp`, `hook/common/dx12_overlay_policy.h`, `tests/test_dxgi_shared.cpp`, `llm-wiki/frame-generation/guardrails.md`.
### 2026-07-11 - Strict true-10-bit DXGI/WGC capture and CFR packet-coverage enforcement

- **Observed root cause:** the short DXGI capture retained BGRA8 and paid the expensive BGRA8→P010 path while the encoder took roughly 28–89 ms per frame. Its MKV declared about 6.19 seconds at 120 fps but contained only 170 video packets instead of about 743, with a 48-tick maximum PTS gap. The comparison WGC capture delivered 746 contiguous packets and used FP16 source → retained R10 → P010.
- **DXGI precision contract:** explicit `capture_method=dxgi_dup` plus `bit_depth=10` offers R10 then FP16 for SDR and FP16 for HDR, disables legacy BGRA duplication, waits up to two seconds for the first acquired texture, and validates that texture rather than `ModeDesc.Format`. BGRA8/unknown first frames fail loudly. Explicit DXGI 10-bit failures cannot silently switch to WGC; auto and 8-bit policies retain compatibility fallback. R10 stays compact end-to-end until P010 conversion; FP16 SDR converts to retained R10 without an 8-bit intermediate.
- **WGC correction:** WinRT still rejects R10 pools, so explicit 10-bit WGC uses FP16 staging → retained R10 → P010. The compact BGRA staging option is now default-off and is gated out for explicit 10-bit/HDR even if configured true. Therefore the user's `wgc_prefer_compact_10bit_pool=false` log was a genuine 10-bit chain; the earlier contrary diagnosis was wrong.
- **CFR/A/V validation:** encoder shutdown logs expected/emitted/missing packet coverage. `analyze_capture_av.py` scans/sorts packet PTS and treats missing CFR ticks or >1.01-tick gaps as strict faults. The A/V matrix now covers DXGI duplication, WGC, and inject; the codec pass runs AAC/ALAC/FLAC/Opus/PCM for all three backends. Source policy/config regressions cover strict fallback and precision gating.
- **Validation:** the independent clean x64/x86 build passed at `0.1.4511`; after integration with the concurrent Streamline changes, the main-workspace clean build/install passed at `0.1.4512`, followed by all `1317` native tests and Python tool self-tests at metadata build `0.1.4513`. Packet scanning classifies the reference WGC capture as complete (`746/746`, max gap ≈1 tick) and the broken DXGI capture as strict failure (`170/743`, 573 missing, max gap 48 ticks).
- **Ancillary pre-existing test repairs:** the owner-queue suspension source test now accepts both LF and CRLF checkouts. The audio ring deferred-growth race test no longer compares two independent lock-free getters as if they formed one atomic snapshot; each getter must publish a complete old/new value, while a mixed pair across the growth boundary is valid.
- **Source anchors:** `captureengine/{dxgi_dup_capture,wgc_capture,media_main}.cpp`, `common/{capture_pipeline_policy,config}.{h,cpp}`, `mediaengine/video_encoder.cpp`, `tools/{analyze_capture_av,run_av_sync_matrix}.py`, `tests/{test_capture_pipeline_policy,test_capture_coordinator_source,test_config,test_dxgi_shared}.cpp`.

### 2026-07-11 - Startup-cached FFX routing fixes permanent suspension loss and reduces FSR-to-DLSS transition churn

- **Root cause (`logs/20260711_140926`):** the switch app resolved and cached the original `ffxCreateContext`, `ffxConfigure`, and `ffxDestroyContext` exports during a startup race. CE's one-shot protected-runtime breakpoint observed startup traffic and then retired, so the later `ffxConfigure(frameGenerationEnabled=0)` suspension never reached CE. Creation interception was also missed, leaving no exact proxy `gameQueue`; the owner-queue renderer correctly refused to guess. The overlay therefore remained on the active UI-resource route and disappeared permanently in FSR passthrough.
- **Fix:** `ffx_cached_pointer_router` enumerates eligible loaded client PE images and atomically routes only exact original-export pointers found in writable, non-executable, non-discardable, pointer-aligned slots. System, CE, overlay, Streamline, and FFX runtime images are excluded. Routed slots are tracked for guarded restoration. Captured create calls now bind the exact owner queue; cached configure calls expose every suspend/resume edge without modifying AMD executable code.
- **Durable transition handling:** routing a cached `ffxConfigure` slot immediately restores/retires the temporary protected-runtime breakpoint. Context destruction keeps it retired while the durable caller-owned route exists, avoiding the prior teardown rearm and repeated code-byte mutation. Per-context state serializes and deduplicates global enable/disable and callback-routing publication while every provider descriptor is still forwarded. Proxy prework logs target changes on the first Present (`active-ui-resource` versus `suspend-backbuffer`) plus submission proof.
- **Runtime evidence (`logs/20260711_142448`, intermediate build):** CE routed all three cached exports, captured the exact queue, submitted the active UI overlay, observed the manual FSR suspension, switched to owner-queue backbuffer submissions, and released bindings on destroy. The same run showed an approximately 25-second FSR-to-DLSS startup gap, but no dump, no device removal, and successful PostSL activation/overlay submission afterward. That intermediate build still rearmed the protected breakpoint and replayed full transition bookkeeping on repeated configure packets; the durable-retirement/dedupe follow-up directly removes those two observed sources of teardown churn. Final on-hardware rerun remains required; CE did not launch another interactive app while the user's tests could conflict.
- **Build validation:** required clean x64/x86 build completed at `0.1.4494`; the final no-build run passed all `1314` native tests plus Python tool self-tests at metadata build `0.1.4496`. No additional interactive FG test was launched to avoid conflicting with the user's running validation.
- **Source anchors:** `hook/apis/{ffx_cached_pointer_router,ffx_hook,dx12_hook}.{h,cpp}`, `tests/{test_ffx_api_parsing,test_dxgi_shared}.cpp`, `llm-wiki/frame-generation/guardrails.md`.

### 2026-07-11 - DX12 FFX owner-queue overlay: active FG + suspension, transactional UI generations

- **Root cause / queue ownership (refined by the 2026-07-12 GTA entry above):** FFX 3.1.6 FrameGenerationSwapChain DX12 create descriptors expose the logical `gameQueue` that AMD uses for the game-to-present handoff. The earlier suspension attempt wrote a proxy swapchain backbuffer from CE's independent queue (illegal cross-queue swapchain access), while the active UI path also used an independent queue plus a per-present CPU wait and lacked an incoming game-queue dependency. CE parses Wrap/New/ForHwnd creation descriptors, binds `{context, raw proxy identity, retained gameQueue}`, and records both active-UI and suspension-backbuffer overlay command lists immediately before proxy Present. The initial implementation required exact queue/resource device identity; the later GTA trace proved Streamline can wrap that logical queue with a different device identity, so the current rule is the target-validated exact/proven-wrapper owner route documented above. Queue order is game draw -> CE overlay -> AMD handoff; there is no new queue, cross-queue copy, guessed owner, or per-frame CPU wait.
- **Per-proxy renderer:** `dx12_ffx_suspend_overlay.{h,cpp}` owns device/queue/format-scoped overlay state with 16 allocator/list/RTV slots keyed to the proxy's current buffer index, a CE fence, and forced matching VB/IB upload slots. Reuse is nonblocking and requires real fence completion. Each slot retains its target resource until completion because D3D12 command lists do not retain resource references. Signal-unknown/in-flight generations are quarantined instead of freed or overwritten; context retirement is proxy-keyed so contexts may safely share a game queue.
- **Transition/lifecycle safety:** Present/Present1 nesting is deduplicated; a freshly patched proxy is not treated as the live driver until a successful composite is observed, so the real-present fallback remains active during handoff. Removal first quiesces prework, restores the vtable, and drains already-entered detours before queue/renderer/UI teardown. Weak `g_LastSwapChain` execution fallback was removed from PostSL startup activation; execution uses only the retained COM owner.
- **Transactional UI registration:** UI targets/substitutes are staged and published only after `ffxConfigure(RegisterUiResource)` succeeds; rejected/stale generations preserve the last known-good target. Substitute reuse now keys device identity, dimensions, format, and exact D3D12 initial state; COMMON/PRESENT numeric zero is preserved. Successful game-texture registration clears stale substitute re-registration, and re-registration failure no longer counts an unconsumed draw as overlay coverage.
- **Regression coverage / validation:** parser tests cover all three DX12 swapchain creation descriptors. Source invariants cover exact-owner suspension, target retention/nonblocking submission, configure commit/rollback, device/state generation keys, first-prework fallback, and detour quiesce/drain. Required full x64/x86 build completed successfully at build `0.1.4479`; all `1311` native tests and Python tool self-tests passed at metadata build `0.1.4480`. Hardware GTA/Talos/FG-switch validation remains required for visual/pacing confirmation.
- **Source anchors:** `hook/apis/{ffx_hook,dx12_hook,dx12_ffx_suspend_overlay}.{h,cpp}`, `hook/common/{ffx_api_parsing,custom_overlay_dx12,overlay_adapter}.{h,cpp}`, `tests/{test_ffx_api_parsing,test_dxgi_shared}.cpp`.

### 2026-07-11 - Capture reliability audit: lifetime-safe WGC/DXGI/inject and lossless audio stop

- **WGC/coordinator lifetime:** queued WinRT callbacks now use per-instance callback epochs and drain before teardown; active capture publication uses atomic shared ownership plus an exclusive lifecycle gate. Retarget is transactional with prior-source rollback. Producer-assigned source epochs and per-frame cursor metadata prevent stale source/cursor lineage after retarget. Auto inject fallback is first-frame-proven and keeps inject alive on failure/timeout; unresolved process targets never default to primary-monitor capture.
- **DXGI duplication:** reset/unusable state parks on a condition variable rather than polling; thread-creation failure, callback exceptions, MMCSS cleanup, format publication, and running-state teardown are explicit.
- **Inject transport correctness:** media queueing no longer acknowledges shared ring metadata early. Move-only leases hold each producer texture until successful synchronous media copy or deliberate discard, and `readIndex` advances only across a contiguous completed prefix. DX8/9/10/11/12, DDraw, OpenGL, Vulkan, and DX9 shmem producers all preflight outstanding texture references before overwriting. D3D11/OpenGL/Vulkan/D3D12 capture setup and submission paths gained transactional cleanup/error handling; real-time GPU retry sleeps/flushes were removed.
- **CFR failure handling:** fresh-frame metadata commits only after successful non-deferred encode. A failed scheduled fresh encode repeats the prior successful media cache at the same QPC when possible, avoiding a CFR hole without mislabeling failed pixels. Source transitions reset all repeat-cache layers. Cursor scaling/cropping now uses signed outward rounding and proportional source cropping rather than squashing partially clipped cursors. Post-mux probing is bounded and synchronous on its owner thread.
- **Audio:** endpoint WASAPI COM/client lifecycle is worker-owned; reactivation is serialized with stop. Endpoint and app loopback validate packet counts/byte sizes/pointers and contain allocation/queue failures. Stop drains already committed packets once without waiting/reactivation and within an endpoint-buffer bound. Capture sources drain while `AudioLoop` remains alive, including audio-only resampler/track tails; the audio worker exception boundary always releases drain waiters.
- **Injection manager:** WMI callback epochs drain before manager destruction; worker ownership is acyclic and every worker is joined before eject/COM teardown.
- **Regression coverage / validation:** callback epoch, atomic owner, handoff state, inject ring lease, cursor geometry, audio source/lifecycle, coordinator/source, encoder source, queue, policy, and ring-buffer tests. Required full x64/x86 build passed at build `0.1.4470`; all `1308` native tests and all Python tool self-tests passed at metadata build `0.1.4471`.
- **Source anchors:** `captureengine/{media_main,wgc_capture,dxgi_dup_capture,injection}.cpp`, `common/{callback_epoch,atomic_shared_owner,capture_handoff_state,inject_frame_ring_lease,frame_queue}.h`, `hook/apis/{ddraw,dx8,dx9,dx11,opengl}_hook.cpp`, `hook/capture/shared_capture.cpp`, `hook/vulkan_layer/*`, `mediaengine/{audio_capture,app_audio_capture,audio_ring_buffer,mediaengine,video_encoder}.cpp`, and the corresponding `tests/test_*` additions.
