name: release-stable

# Manual-only stable release builder: never runs on push or pull_request.
# Trigger with the desired version (0.1.N) or leave it empty to derive the
# next build number from the newest existing v0.1.* release tag.
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Stable release version (0.1.N). Empty = next after the newest v0.1.* release tag."
        required: false
        type: string
        default: ""
      attest:
        description: "Generate GitHub artifact attestation for release assets. auto = only when the repository is public (GitHub Free/Pro/Team cannot attest private repositories)."
        required: false
        type: choice
        default: auto
        options:
          - auto
          - always
          - never

permissions:
  contents: write
  id-token: write
  attestations: write

concurrency:
  group: release-stable
  cancel-in-progress: false

jobs:
  build-release:
    # Native Windows release build on the maintainer's self-hosted runner.
    # The workspace must be persistent so the ignored toolchain trees
    # (build/msys64, external/, installed/) survive and
    # the artifact matches a local native build (PDBs, effective x64 CFG,
    # custom-patched FFmpeg, FG runtime payloads).
    #
    # actions/checkout is deliberately NOT used: it deletes the entire
    # workspace contents whenever the expected repository URL differs from the
    # configured fetch URL (e.g. the trailing ".git"), and on Windows rmRF
    # follows directory junctions, which destroyed the toolchain trees during
    # the first test. The sync step below only fetches/switches/resets tracked
    # files and never touches ignored trees or junctions. See llm-wiki.
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 180
    # This self-hosted runner (broker flow) does not populate the GITHUB_TOKEN
    # environment variable automatically, although the github.token context is
    # available. Pass it explicitly so steps/actions that read the env var work.
    env:
      GITHUB_TOKEN: ${{ github.token }}

    steps:
      - name: Sync repository (persistent workspace, never cleans)
        shell: pwsh
        run: |
          if (-not (Get-Command gh -ErrorAction SilentlyContinue)) {
            throw "GitHub CLI (gh) is required on the self-hosted runner; install it from https://cli.github.com/ and make sure it is on PATH."
          }
          gh auth setup-git
          git config --local --unset-all http.https://github.com/.extraheader 2>$null
          if (-not (Test-Path .git)) {
            git init -b main
            git remote add origin "https://github.com/$env:GITHUB_REPOSITORY.git"
          }
          git remote set-url origin "https://github.com/$env:GITHUB_REPOSITORY.git"
          git fetch --prune origin main
          git switch -C main origin/main
          Write-Output "Workspace synced to $env:GITHUB_SHA; persistent ignored trees preserved"

      - name: Prepare persistent toolchain junctions
        shell: pwsh
        env:
          TOOLCHAIN_ROOT: ${{ vars.CE_TOOLCHAIN_ROOT }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:TOOLCHAIN_ROOT)) {
            throw "Repository variable CE_TOOLCHAIN_ROOT must point to the maintainer's dev checkout (e.g. %USERPROFILE%\Programme\build\captureproject)."
          }
          $root = [System.IO.Path]::GetFullPath($env:TOOLCHAIN_ROOT)
          function Get-Redacted([string]$path) {
            if (-not $env:USERPROFILE) { return $path }
            return $path.Replace($env:USERPROFILE, '%USERPROFILE%').Replace($env:USERPROFILE.Replace('\','/'), '%USERPROFILE%')
          }
          foreach ($required in @('build\msys64', 'external')) {
            if (-not (Test-Path (Join-Path $root $required))) {
              throw "Toolchain root '$(Get-Redacted $root)' is missing $required; restore the local build environment first."
            }
          }
          New-Item -ItemType Directory -Force -Path build | Out-Null
          # ffmpeg_build is deliberately NOT junctioned: FFmpeg and its source
          # dependency closure (libaom, opus, libva, libvpl, SVT-AV1, libiconv,
          # libwinpthread, llvm-runtime) must be compiled by this job, so that
          # every shipped binary was produced by the run that publishes it. It
          # used to be junctioned to the maintainer's dev tree, which meant the
          # release reused DLLs compiled locally days earlier - self-built, but
          # not built here, and therefore not attestable.
          foreach ($pair in @(
            @('build\msys64', (Join-Path $root 'build\msys64')),
            @('external', (Join-Path $root 'external'))
          )) {
            if (-not (Test-Path $pair[0])) {
              New-Item -ItemType Junction -Path $pair[0] -Target $pair[1] | Out-Null
              Write-Output "Created junction $($pair[0]) -> $(Get-Redacted $pair[1])"
            } else {
              $item = Get-Item -LiteralPath $pair[0] -Force
              if ($item.LinkType -ne 'Junction') {
                throw "$($pair[0]) already exists but is not a junction to the persistent toolchain; refusing to proceed."
              }
            }
          }
          Write-Output "Toolchain junctions ready (root=$(Get-Redacted $root))"

      - name: Reset FFmpeg build tree (every release compiles it from source)
        shell: pwsh
        run: |
          $tree = 'ffmpeg_build'
          if (Test-Path $tree) {
            $item = Get-Item -LiteralPath $tree -Force
            if ($item.LinkType -eq 'Junction') {
              # Delete only the reparse point. Remove-Item -Recurse follows
              # junctions on Windows and would erase the maintainer's real
              # toolchain tree behind it - the same failure mode that made this
              # workflow avoid actions/checkout.
              [System.IO.Directory]::Delete($item.FullName, $false)
              Write-Output "Removed inherited ffmpeg_build junction (release builds must compile FFmpeg themselves)"
            } else {
              Remove-Item -LiteralPath $tree -Recurse -Force
              Write-Output "Removed previous ffmpeg_build tree from this workspace"
            }
          }
          Write-Output "FFmpeg and its full source dependency closure will be built by this job"

      - name: Run build policy tests
        shell: pwsh
        run: python -m unittest -v tools.tests.test_build_flags tools.tests.test_build_testapp_tasks tools.tests.test_pe_hardening tools.tests.test_privacy_paths tools.tests.test_ffmpeg_dependencies

      - name: Resolve release version
        id: version
        shell: pwsh
        run: |
          $inputVersion = "${{ inputs.version }}".Trim()
          if ($inputVersion) {
            if ($inputVersion -notmatch '^0\.1\.(\d+)$') {
              throw "Version input must match 0.1.N, got '$inputVersion'"
            }
            "version=$inputVersion" >> $env:GITHUB_OUTPUT
          } else {
            $numbers = @()
            git ls-remote --tags origin "refs/tags/v0.1.*" | ForEach-Object {
              if ($_ -match 'refs/tags/v0\.1\.(\d+)$') {
                $numbers += [int]$Matches[1]
              }
            }
            if ($numbers.Count -eq 0) {
              throw "No existing v0.1.* release tags found; pass the 'version' input (e.g. 0.1.5266)."
            }
            $next = ($numbers | Measure-Object -Maximum).Maximum + 1
            "version=0.1.$next" >> $env:GITHUB_OUTPUT
          }

      - name: Seed build number
        shell: pwsh
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          if ($env:VERSION -notmatch '^0\.1\.(\d+)$') {
            throw "Unexpected version '$env:VERSION'"
          }
          $buildNumber = [int]$Matches[1]
          if ($buildNumber -lt 1) {
            throw "Build number must be >= 1"
          }
          New-Item -ItemType Directory -Force -Path build | Out-Null
          Set-Content -Path build/build_number.txt -Value ($buildNumber - 1) -NoNewline
          Write-Output "Seeded build number $($buildNumber - 1); build.py will mint 0.1.$buildNumber"

      - name: Build release product
        shell: pwsh
        env:
          # build.py redacts developer-identifying paths from its console output
          # while this is set, so GitHub Actions run logs stay free of machine
          # paths. actions/setup-python is deliberately not used: it dumps the
          # toolcache path (under the developer profile) into the run log.
          CE_PRIVACY_SANITIZE_LOGS: "1"
        run: |
          python -c "import sys; raise SystemExit(0 if sys.version_info >= (3, 12) else 1)"
          if ($LASTEXITCODE -ne 0) {
            throw "Python 3.12 or newer is required on the self-hosted runner."
          }
          python build.py --skip-updates

      - name: Verify built version
        shell: pwsh
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          $expected = $env:VERSION
          $header = Get-Content -Raw common/build_version.h
          $pattern = '#define CAPTURE_VERSION "' + [regex]::Escape($expected) + '"'
          if ($header -notmatch $pattern) {
            Write-Output $header
            throw "Built CAPTURE_VERSION does not match requested $expected"
          }
          Write-Output "Built version confirmed: $expected"

      - name: Determine attestation mode
        id: attest
        shell: pwsh
        env:
          ATTEST_INPUT: ${{ inputs.attest }}
        run: |
          $mode = $env:ATTEST_INPUT
          if ($mode -notin @('auto', 'always', 'never')) {
            throw "Unexpected attest input '$mode'"
          }
          $visibility = gh api "repos/$env:GITHUB_REPOSITORY" --jq '.visibility'
          if ($LASTEXITCODE -ne 0) {
            throw "Could not query repository visibility via gh"
          }
          $runAttest = $false
          if ($mode -eq 'always') {
            $runAttest = $true
            if ($visibility -ne 'public') {
              Write-Warning "Attestation forced (always) but repository is '$visibility'; GitHub Free/Pro/Team only supports artifact attestation in public repositories, so the attestation step will fail closed."
            }
          } elseif ($mode -eq 'auto' -and $visibility -eq 'public') {
            $runAttest = $true
          }
          if ($runAttest) {
            Write-Output "Artifact attestation enabled (mode=$mode, repository_visibility=$visibility)."
          } else {
            Write-Output "Artifact attestation skipped (mode=$mode, repository_visibility=$visibility). GitHub Free/Pro/Team artifact attestation requires a public repository."
          }
          if ($runAttest) {
            "run=true" >> $env:GITHUB_OUTPUT
          } else {
            "run=false" >> $env:GITHUB_OUTPUT
          }

      - name: Attest release assets
        if: steps.attest.outputs.run == 'true'
        uses: actions/attest@v4
        with:
          subject-path: |
            build/packages/captureengine.7z
            build/packages/testapps.7z
            build/verification/latest_manifest.json
            build/verification/latest_summary.txt

      - name: Publish stable tag and GitHub release
        shell: pwsh
        env:
          VERSION: ${{ steps.version.outputs.version }}
          COMMIT: ${{ github.sha }}
        run: |
          if (-not (Get-Command gh -ErrorAction SilentlyContinue)) {
            throw "GitHub CLI (gh) is required on the self-hosted runner; install it from https://cli.github.com/ and make sure it is on PATH."
          }
          $version = $env:VERSION
          $tag = "v$version"
          $existing = git ls-remote --tags origin "refs/tags/$tag"
          if ($LASTEXITCODE -ne 0) {
            throw "git ls-remote failed"
          }
          if ($existing) {
            throw "Tag $tag already exists on origin; refusing to overwrite."
          }
          $assets = @(
            "build/packages/captureengine.7z",
            "build/packages/testapps.7z",
            "build/verification/latest_manifest.json",
            "build/verification/latest_summary.txt"
          )
          foreach ($asset in $assets) {
            if (-not (Test-Path $asset)) {
              throw "Missing release asset: $asset"
            }
          }
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a $tag -m "Stable release $version"
          git push origin $tag
          gh release create $tag `
            --title "Stable release $version" `
            --notes "Stable release $version built from $env:COMMIT.`n`nAssets: captureengine.7z (product), testapps.7z (test apps), latest_manifest.json / latest_summary.txt (build verification evidence)." `
            @assets
